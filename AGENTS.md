# AGENTS.md

本文件只记录长期稳定的 Codex 协作规则。业务范围、产品功能、架构和技术版本必须记录在正式设计文档中，不得写入本文件形成隐性需求。

## 工作原则

1. 使用中文沟通，先给结论，再给验证证据、风险和下一步。
2. 开始前确认 Git 根目录、分支、工作区状态和实际项目文件；只处理用户明确要求的范围。
3. 优先交付可验证的最小闭环，不预建未来目录、兼容层、微服务或抽象层。
4. 调研结论区分公开事实、合理推断和待确认事项；时效性信息优先核验官方或一手来源。
5. 破坏性操作必须有明确授权和精确目标；保留用户已有修改，不擅自回滚、覆盖或清理。
6. 不读取、打印、提交或复制无关凭据、`.env`、本地数据和生成产物。

## 设计与实现

1. 架构或复杂功能先形成可审阅的 Design，明确问题、范围、非目标、模块边界、数据与接口、状态和失败路径。
2. 用户接受 Design 后再细化需求、实施计划和验收；文档数量与形式服从实际问题，不使用固定模板堆砌内容。
3. 核心业务逻辑默认遵循 Red → Green → Refactor；无法先写测试时说明原因，并给出最接近的可执行验收。
4. 业务模块按职责组织并显式声明依赖，避免全局 `utils`、重复 DTO、重复异常体系和仅转发调用的空层。
5. 复用已接受的架构与工具链；设计发生变化时先更新事实来源，再继续实现。

## Go 工程规范

1. 所有 Go 代码必须遵循 Go 官方惯用法以及 Google Go Style Guide、Uber Go Style Guide；新增或修改代码优先保持简单、清晰、可测试，并遵守现有目录边界和项目约定。
2. 代码按业务模块组织，采用模块化整洁架构，严格保持 `adapter → application → domain` 的单向依赖，不得由内层反向依赖外层。
3. 依赖必须通过显式构造函数注入；接口由消费方按实际需要定义并保持小而专一；跨层数据使用边界明确的数据模型和显式转换。
4. 禁止照搬 Java 式全局 Controller/Service/DAO 分层，禁止使用 `Ixxx`/`Impl` 命名，禁止创建职责不明的 `utils`、`common` 等通用包。
5. 错误必须显式返回并在增加语义时进行包装，同时保留可判定的错误链；`context.Context` 必须沿调用链传递，不得以字段方式长期保存或无故替换。
6. goroutine 必须具有明确的所有权、取消机制、退出条件和等待策略，避免失控并发、泄漏和无法收敛的后台任务。
7. 日志必须采用结构化记录，包含排障所需上下文，同时不得泄露凭据、隐私数据或其他敏感信息。
8. 核心业务逻辑须有单元测试，跨边界行为须按风险补充集成测试；新增或修改 Go 代码应通过项目适用的 `gofmt`、`goimports`、`go vet`、`golangci-lint`、Race Detector 和 `govulncheck` 质量门禁，未执行、被跳过或受环境限制的检查必须如实说明。

## 验证与 Git

1. 验证范围与风险匹配，记录真实命令和结果；中断、跳过或缺少外部条件的验证不得报告为通过。
2. 修改前后检查 diff 和工作区，排除无关文件、secret、缓存、日志和构建产物。
3. 提交只包含当前任务文件，标题统一使用 `type(scope): 中文说明`；`type` 只允许 `test`、`docs`、`impl`、`feat`、`refactor` 或 `chore`。
4. `scope` 必须使用小写英文业务模块或工程领域名，例如 `auth`、`frontend`、`storyboard`、`docs` 或 `ci`，不得使用 `app`、`misc` 等含义不明的范围。
5. 提交标题和正文使用中文，命令、路径、代码标识符与 `Co-authored-by` 等固定格式除外；例如 `test(auth): 固定邮箱登录失败契约`、`impl(auth): 接入真实邮箱登录`。
6. 未经用户明确要求，不创建或推送分支、PR、标签，也不执行合并或发布。
7. 最终答复前运行 `git status --short`，如有遗留修改必须逐项说明。

## 交付说明

每次完成任务时简要说明：

1. 结果与关键文件。
2. 验证命令和结果。
3. 未决问题或残余风险。
4. Git 提交、工作区和发布状态。

# AGENTS.md

本文件只记录长期稳定的 Codex 协作规则。业务范围、产品功能、架构和技术版本必须记录在正式设计文档中，不得写入本文件形成隐性需求。

## 工作原则

1. 使用中文沟通，先给结论，再给验证证据、风险和下一步。
2. 开始前确认 Git 根目录、分支、工作区状态和实际项目文件；只处理用户明确要求的范围。
3. 优先交付可验证的最小闭环，不预建未来目录、兼容层、微服务或抽象层。
4. 调研结论区分公开事实、合理推断和待确认事项；时效性信息优先核验官方或一手来源。
5. 破坏性操作必须有明确授权和精确目标；保留用户已有修改，不擅自回滚、覆盖或清理。
6. 不读取、打印、提交或复制无关凭据、`.env`、本地数据和生成产物。

## 设计与实现

1. 架构或复杂功能先形成可审阅的 Design，明确问题、范围、非目标、模块边界、数据与接口、状态和失败路径。
2. 用户接受 Design 后再细化需求、实施计划和验收；文档数量与形式服从实际问题，不使用固定模板堆砌内容。
3. 核心业务逻辑默认遵循 Red → Green → Refactor；无法先写测试时说明原因，并给出最接近的可执行验收。
4. 业务模块按职责组织并显式声明依赖，避免全局 `utils`、重复 DTO、重复异常体系和仅转发调用的空层。
5. 复用已接受的架构与工具链；设计发生变化时先更新事实来源，再继续实现。

## 验证与 Git

1. 验证范围与风险匹配，记录真实命令和结果；中断、跳过或缺少外部条件的验证不得报告为通过。
2. 修改前后检查 diff 和工作区，排除无关文件、secret、缓存、日志和构建产物。
3. 提交只包含当前任务文件，提交类型使用 `test:`、`docs:`、`impl:`、`feat:`、`refactor:` 或 `chore:`。
4. 未经用户明确要求，不创建或推送分支、PR、标签，也不执行合并或发布。
5. 最终答复前运行 `git status --short`，如有遗留修改必须逐项说明。

## 交付说明

每次完成任务时简要说明：

1. 结果与关键文件。
2. 验证命令和结果。
3. 未决问题或残余风险。
4. Git 提交、工作区和发布状态。

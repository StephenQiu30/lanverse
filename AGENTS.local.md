# AGENTS.local.md

本文件只记录 Lanverse 仓库当前有效、且不属于业务设计的局部约束。

## 当前状态

1. 仓库处于全量重新设计阶段，暂不存在可复用的应用实现。
2. 用户已确定后端语言为 Python、HTTP 框架为 FastAPI，前端组件体系为 shadcn/ui + Radix UI；其余产品、架构和技术决定以 `docs/design/` 中已接受的文档为准。
3. Design 未被接受前，不创建应用脚手架、数据库表、部署配置或占位模块。

## 仓库边界

1. `.codex/agents/` 与 `.codex/skills/` 只存放 Codex 协作配置，不承载业务代码或业务规范。
2. `AGENTS.md` 与本文件不得固定目录数量、数据表数量、供应商、队列、存储方式或前后端实现细节。
3. 本地 secret、媒体、模型、数据库和对象存储数据不得进入 Git；仓库只允许安全的配置示例。
4. 项目命令和目录在脚手架建立后，以仓库内实际配置为准，不在本文件提前发明。
5. 项目自有的业务与设计文档使用中文文件名；`AGENTS.md`、`README.md`、`LICENSE` 等工具链或平台约定名称保持标准命名。

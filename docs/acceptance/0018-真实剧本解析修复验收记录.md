# 真实剧本解析修复验收记录

2026-09-08。本轮已打通指定原稿的分集入库与搜索闭环，**场景/人物全链路尚未通过语义验收**。真实模型输出、已批准的分集和未批准的场景草案必须分开理解。

## 实际输入与运行

- 原稿：用户指定的 `He Left Our Kids to Drown—He Didn’t Know I Was the Empress.docx`，沿用此前通过电脑上传的文件和不可变版本，没有替换成合成稿。
- 项目：Empress · 60集原稿解析验收，`8877ed81-d0af-450b-b448-3027e4f74476`。
- 原稿版本：`2dd9c0c1-5125-4719-94fd-fd0b825fb95d`，139723 个字符，3981 个文本块，60 个显式集标题。
- 真实运行：`68c180a2-8548-4b89-b937-cab9d01fa399`。Chrome 页面启动、领取分集审阅、批准及正式采纳；没有通过 SQL 写入正式剧集或伪造模型结果。
- 原文紧凑编码的上下文为 186196 字节，上限仍为 240000；恢复每一块后逐字符等于原稿。
- 本次冻结的 text-storyboard 发布摘要为 `63310724e287f09d5577bbe44410a480aefe7ad8f72956b2d78615c3ac05ecb3`。运行中未更换 Skill，也未重新绑定旧任务。

## 已验证闭环

| 检查 | 实际结果 |
| --- | --- |
| AI 分集 | 60 集；60 个起止范围逐项对齐显式集标题，0 个边界差异 |
| 前置说明 | 14 个标题/修订说明/空白块明确排除，没有丢弃剧情正文 |
| 正式采纳 | 60 个 Episode、60 个已发布 ScriptVersion、真实 OwnerReceipt |
| 原稿一致性 | 所有正文逐字符等于原稿相应范围，全部内容摘要一致 |
| 同步事件 | 60 个 ScriptVersionPublished，均为 published |
| ES | 60 个 script 文档，另有 1 个快照标记；全部版本摘要与 DB 一致 |
| 搜索投影 | 全部 search_text 等于约定的“集标题 + 换行 + 正文”去首尾空白结果 |
| 实际检索 | 在该项目的 script 索引检索 Jace Iris，命中 42 集 |
| 恢复 | 接收合同修复期间原运行暂受阻；页面恢复后复用原分集草案，未再次推理分集 |
| 下一阶段 | 已实际进入 analyze_episode，第一集完成后继续第二集 |

这里的双写为 Go 同一数据库事务写正式正文与 Outbox，再经现有 Kafka/Event Worker 投影到 ES，提供可靠的最终一致性；没有增加一个不具备原子性的同步 ES 写接口。

## 本轮实修

- Harness 保留全稿的紧凑块编码及独立发布摘要，解决模型调用前超上下文限制的问题。
- 分集采纳连接既有发布事件工厂；事件插入失败时剧集、版本、事件及回执整体回滚，重放不重复创建。
- Go 执行快照补充输出定位合同；实测复现 `agent_proposal_invalid` / unknown field `step_key` 后修复，保留闭合 JSON 校验。
- 正式 TextMention 保留并校验 visual_details；测试证明证据实际进入场景 JSON，跨场引用仍被拒绝。
- 上传后的默认入口接入 Creation；原稿固定不再显示“剧本解析完成”。执行中的逐集阶段按真实步骤显示已生成草案数。
- 同步 OpenAPI 与 TypeScript 生成产物。生成器也补齐工作区内此前已有 OpenAPI 合同对应的客户端类型，未新增无关接口。
- 0017 仅调整一处旧协议的文字描述以符合仓库命名检查，保留原来的历史失败事实与结论。

## 真实语义审阅：尚不通过

第一集实际输出 3 个场景、22 段逐字对白、29 次人物/场所/道具提及、17 处可见细节证据，页面可查 87 处去重来源引用。它识别了医疗中心冷开场、主卧冲突和儿童舱/水井危险；保留了 Aurelia 怀孕八个月、Jace 六岁、Iris 四岁、七级风暴、水循环面板及外侧锁门等事实。

尚不能采纳的原因：第二场标记为 24 小时前闪回，第三场明确延续该时间线，却被标成 present，并给出另一个 time_branch。这会影响后续状态继承和身份披露。候选还声称 schema 没有 required 字段；实际 schema 存在该字段，该说法属于模型错误自检，不能作为修改工程合同的依据。本轮没有批准或入库这份场景草案。

未决项：全部 60 集语义审阅、全局人物身份/关系/状态归并、跨集时间线、采纳前草案修订、全稿 build_world 上下文容量、episode summary/conflict/turning_point/ending_hook 的正式投影，以及模型前明确拒绝与调用结果未知的细分。第一集已产出不代表以上能力通过。现有逐集流程串行执行，后台继续运行；后续草案必须经审阅才可进入正式数据。

截至本轮最后一次状态核查，前 3 集候选已完成，第 4 集正在分析，运行无 last_error；这只是执行进度，不是三集都已通过语义验收。

## 验证命令与结果

在各应用目录执行；测试数据库与真实原稿运行数据库完全分离。

- Agent 文本任务/HTTP/模块边界针对性测试：29 passed。
- `LANVERSE_TEST_CREATION_DATABASE_URL=postgresql://localhost/lanverse_creation_fix_tests_0908 LANVERSE_TEST_TEMPORAL_ADDRESS=127.0.0.1:7233 .venv/bin/python -m pytest -q tests/creation`：74 passed。此前未设置隔离库时存在 48 项跳过，已用独立库及真实 Temporal 重跑。
- `.venv/bin/ruff check`、`ruff format --check`、`pyright`：本轮 Python 文件通过。
- Go 创建/规划/原稿 GORM/Search/Eventing 的 `go test -race`：通过；新增发布回滚、重放及可见细节持久化测试均通过。
- `go vet`：Creation/Planning 与其测试通过；`goimports -l`：本轮 Go 文件无输出。
- `golangci-lint run`：Creation/Planning 与其测试 0 issues。
- `govulncheck`：本轮 Creation/Planning 调用路径未发现可达漏洞；另报告导入包 1 项及依赖模块 3 项非调用路径漏洞，不能解读为整个依赖树没有风险。
- Go `tests/architecture`、`tests/openapi`：通过。
- 前端 `project-detail-page`、`creation-workspace`：9 tests passed；typecheck 和涉及文件 ESLint 通过。
- `git diff --check`：通过。本轮使用 Next 开发服务验证真实页面，未执行整仓前端生产构建、远程 CI 或正式部署。

## 本地运行与工作区

当前本地前端 8123、Go 8686、Candidate Harness 8787、Creation API 8788 与独立 Creation Worker 已启动。沿用本机 PostgreSQL、Temporal、Kafka 和 MinIO；搜索使用本次创建的 loopback Elasticsearch 验证实例。原 Docker 前后端停用后改为本轮源代码对应的本机进程，没有重置原业务数据库。

运行凭据仅保存在仓库外权限受限的本地配置，未写入 Git。验收资料目录：`/Users/stephenqiu/Documents/Codex/2026-09-08/lanverse-real-fix/`；包括上下文核验、实际候选、分集边界、数据库、搜索证据、本轮增量审查与工作区逐项清单。该目录含私有原稿和运行配置，不应整体提交或分享。

保持 main；开始时已有 192 项 Git 状态记录，全部保留，包含用户确认的删除项。本轮未提交、未推送、未创建分支。完整遗留清单见同目录 `工作区遗留修改清单.md`。

## 2026-09-08 提交前补充检查

- Go `go test ./...` 全量通过；集成测试沿用隔离测试库，不连接真实业务库执行清理。
- Agent 全量 `pytest -q`：176 passed、6 skipped；跳过项为需显式启用的真实 Codex 集成。`ruff check app tests`、`pyright` 通过。
- 前端全量首次发现跨 feature 导入不符合公共 workspace 边界；将真实创作工作区入口统一为 `text-creation-workspace.tsx`，未放宽架构测试。修复后 25 个测试文件、69 项测试及 `typecheck`、`lint` 全部通过。
- 本节只记录提交前检查；上文验证实例及进度是历史现场。后续按用户要求接入已经启动的本机正式环境，不把旧验证实例作为交付环境。

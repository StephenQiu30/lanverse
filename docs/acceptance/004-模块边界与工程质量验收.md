---
layer: Acceptance
doc_no: ACCEPTANCE-004
audience: [Dev, QA, Ops]
feature_area: modular-architecture-quality
purpose: 验证 DESIGN-004、PRD-004 与 PLAN-004 定义的模块和工程约束
canonical_path: docs/acceptance/004-模块边界与工程质量验收.md
status: draft
execution_state: in_progress
version: 2.0.0
owner: StephenQiu
inputs:
  - docs/design/004-ai内容创作平台模块边界与解耦设计.md
  - docs/prd/004-模块边界与工程质量需求.md
  - docs/plans/004-模块边界与工程质量计划.md
outputs:
  - AC-M01 至 AC-M10 的状态、架构证据、风险和工程结论
triggers:
  - 模块、依赖、数据所有权、Port、事件、Workflow 或实现变化
downstream:
  - Human Review
---

# 模块边界与工程质量验收

## 1. 当前结论

DESIGN-004 v3.0 与 PRD-004 v2.0 已接受，但代码仍是旧的多 source root、八模块占位和
多 head migration 结构，因此结论为 `not_ready`。旧 AC-M01/M02 的 passed 结论已失效；
新 AC-M01 至 AC-M10 全部从 `pending` 重新验证，不用旧绿色测试证明新契约。

## 2. 证据规则

状态仅使用 `pending/passed/failed/blocked`。实施后记录环境戳、命令、退出码、提交和报告到 `artifacts/acceptance/004/`。所有架构与契约测试必须进入 `make verify`。

当前环境戳：`StephenQius-MacBook-Pro.local:/Users/stephenqiu/Desktop/StephenQiu/Thief@d566bc7`

重构前审计：后端使用 6 个 Python source root；8 个业务模块中 6 个没有实现；26 个
`__init__.py` 仅为占位；Alembic 有 8 个并行 head 和 8 个 migration 目录。现有
31 个 architecture tests 会要求这些目录继续存在，说明测试契约本身也需要先 Red
再替换。identity/catalog 行为与此前 27 unit、7 integration、会话浏览器证据作为
回归基线保留，但不直接判定新 AC 通过。

## 3. 验收矩阵

| AC | 状态 | 验收方法 | 必需证据 |
| --- | --- | --- | --- |
| AC-M01 | pending | 扫描 source root、Make、pyproject 和 import | 只有 `backend/src/thief` 的路径报告 |
| AC-M02 | pending | 扫描业务目录、schema 和 migration | 无未实现占位/空业务 schema 报告 |
| AC-M03 | pending | 向 model/use case 注入框架 import | 架构测试失败信号与恢复结果 |
| AC-M04 | pending | 扫描 repository SQL/约束并运行集成测试 | identity/catalog 独立写入与无跨 schema FK |
| AC-M05 | pending | 空库执行 migration 并查询 heads | 唯一 head、revision 与六张业务表 |
| AC-M06 | pending | downgrade base 后重新 upgrade | 两轮 schema/table/constraint checksum |
| AC-M07 | pending | 构建并启动 Web/API/Worker/Scheduler | 单 Python 包入口与四进程报告 |
| AC-M08 | pending | 重跑 identity/catalog/API 原有契约 | unit/integration/API 结果与基线一致 |
| AC-M09 | pending | 扫描空文件、业务文件行数和目录层级 | 零占位、零超限、允许目录清单 |
| AC-M10 | pending | 对未实现模块预建文件运行门禁 | architecture test 明确拒绝预建模板 |

## 4. 执行记录

| 工作包 | 状态 | 必需证据 |
| --- | --- | --- |
| M-R01 Red | pending | 新 architecture tests 在旧树失败且原因精确 |
| M-R02 单包 | pending | 移动清单、import 扫描、unit/API 结果 |
| M-R03 migration | pending | 数据计数、显式卷重建、heads、downgrade/restore |
| M-R04 命令/运行 | pending | verify、runtime、CI |
| M-R05 验收 | pending | agent-browser、Acceptance、Git 状态 |

## 5. 结果

| 项目 | 当前值 |
| --- | --- |
| 结论 | `not_ready` |
| 已通过 | 0/10 |
| 失败 | 0/10 |
| 阻塞 | 0/10 |
| 待执行 | 10/10 |
| 残余风险 | 旧 import 漏改、开发库基线重建、migration checksum 与四进程入口尚未验证 |

只有 10/10 AC 全部通过且架构测试在 CI 强制执行时，才能更新为 `accepted`。

## 6. 变更记录

- 2026-07-23：v2.0.0 按 DESIGN-004 v3.0/PRD-004 v2.0 重置 AC-M，记录旧结构审计基线和五个重构工作包；旧 passed 状态失效。
- 2026-07-22：v1.9.0 回填 catalog 独立领域/Port/Adapter、三张事实表和无跨 schema asset 外键证据；AC-M03 继续等待强制写入阻断。
- 2026-07-22：v1.8.0 回填 Web same-origin BFF、Server Component 会话恢复、最小客户端交互边界和 30 个架构测试证据。
- 2026-07-22：v1.7.0 回填 identity 会话用例、FastAPI 组合根、仓储 Adapter 与最新 CI 证据。
- 2026-07-22：v1.6.0 回填 identity Port/Adapter、事务仓储与最新 CI 证据；AC-M04 仍等待全套 Adapter 契约。
- 2026-07-22：v1.5.0 回填 identity 独立领域与事实表、26 个架构测试和最新 CI 证据；AC-M03 等待 Repository 阻断证据。
- 2026-07-22：v1.4.0 回填四进程入口、生产资源和干净运行目录证据；AC-M10 等待四队列独立 Worker。
- 2026-07-22：v1.3.0 回填八模块 schema/migration、UnitOfWork 和 PostgreSQL 18 CI 证据；AC-M03 继续 pending。
- 2026-07-22：v1.2.0 将架构测试接入 `make verify` 与真实 GitHub CI。
- 2026-07-22：v1.1.0 回填前后端分层和真实 core 扫描证据，AC-M01、AC-M02 通过。
- 2026-07-22：v1.0.0 基于 DESIGN-004/PRD-004/PLAN-004 创建模块边界与工程质量验收。

---
layer: Acceptance
doc_no: ACCEPTANCE-004
audience: [Dev, QA, Ops]
feature_area: modular-architecture-quality
purpose: 验证 DESIGN-004、PRD-004 与 PLAN-004 定义的模块和工程约束
canonical_path: docs/acceptance/004-模块边界与工程质量验收.md
status: accepted
execution_state: complete
version: 3.0.0
owner: StephenQiu
accepted_on: 2026-07-23
inputs:
  - docs/design/004-ai内容创作平台模块边界与解耦设计.md
  - docs/prd/004-模块边界与工程质量需求.md
  - docs/plans/004-模块边界与工程质量计划.md
outputs:
  - AC-M01 至 AC-M10 的状态、架构证据、风险和工程结论
triggers:
  - 模块、依赖、数据所有权、Port、事件、Workflow 或实现变化
downstream:
  - Human Review
---

# 模块边界与工程质量验收

## 1. 当前结论

DESIGN-004 v3.0、PRD-004 v2.0 和 PLAN-004 v2.0 已按 Red→Green 完成。
后端现在只有 `backend/src/thief` 一个包、identity/catalog 两个已实现业务模块和
`platform_0001` 一条 Alembic 历史。AC-M01 至 AC-M10 全部具有当前代码、数据库、
运行时、浏览器和 GitHub CI 证据，结论为 `accepted`。

## 2. 证据规则

状态仅使用 `pending/passed/failed/blocked`。命令、关键输出、提交和 CI 链接记录在本
文档；所有架构与契约测试进入 `make verify`。

当前环境戳：`StephenQius-MacBook-Pro.local:/Users/stephenqiu/Desktop/StephenQiu/Thief@763aec3`

重构前审计：后端使用 6 个 Python source root；8 个业务模块中 6 个没有实现；26 个
`__init__.py` 仅为占位；Alembic 有 8 个并行 head 和 8 个 migration 目录。现有
31 个旧 architecture tests 会要求这些目录继续存在。`8676673` 先以目标契约替换
旧门禁并产生预期 Red；identity/catalog 的 27 unit、7 integration 和会话浏览器
行为作为重构前回归基线。

## 3. 验收矩阵

| AC | 状态 | 验收方法 | 必需证据 |
| --- | --- | --- | --- |
| AC-M01 | passed | `pyproject.toml` 的 pytest/mypy 均只有 `src`；Make 只有一个 `BACKEND_PYTHONPATH`；旧 import 扫描为 0 |
| AC-M02 | passed | 源码只有 api/identity/catalog/infrastructure；数据库只有 identity/catalog；旧 apps/packages/八迁移目录均不存在 |
| AC-M03 | passed | architecture fixture 注入 FastAPI 或跨业务模块 import 时分别报告 `business-framework`/`cross-business-module` |
| AC-M04 | passed | 7 integration tests 验证 identity/catalog 独立 SQL、约束和 generation example 无 asset FK |
| AC-M05 | passed | 空卷 `make migrate` 成功；`alembic heads` 仅 `platform_0001`；六张业务表存在 |
| AC-M06 | passed | `migrate-down` 后业务 schema/revision 均为 0；恢复前后表 checksum `6417…c85fe`、约束 checksum `2cbf…e6a3` 一致 |
| AC-M07 | passed | `make test-runtime` 返回 `{"api":"ok","scheduler":"ok","web":"ok","worker":"ok"}`；Python 入口均来自 `thief` |
| AC-M08 | passed | 27 unit + 7 integration 全部通过，会话 API 与 catalog 事实契约未变化 |
| AC-M09 | passed | 结构门禁报告 0 占位、0 超 200 行业务文件；最大业务文件 184 行 |
| AC-M10 | passed | `8a3a988` Red 明确拒绝预建 `creation/`；`763aec3` Green 只允许四个当前目录 |

## 4. 执行记录

| 工作包 | 状态 | 必需证据 |
| --- | --- | --- |
| M-R01 Red | passed | `8676673`；5 tests 中 4 个因旧 source root/入口/迁移结构失败；CI `29937687252` 预期失败 |
| M-R02 单包 | passed | `4e527ca`；删除 26 个占位文件和旧 source roots；27 unit、目标 architecture、ruff、mypy 通过 |
| M-R03 migration | passed | `7b9d6ff`；六表均 0 行后只重建 `thief_postgres_data`；upgrade/downgrade/restore、7 integration 通过 |
| M-R04 命令/运行 | passed | `28b9728`；`make verify`、`make test-runtime`、CI `29966616067` 通过，README 与真实结构一致 |
| M-R05 验收 | passed | `8a3a988`/`763aec3` 完成 AC-M10；62-test 本地门禁与 CI `29976430520` 通过 |

## 5. 浏览器验收

使用 production standalone Web、production FastAPI 和真实 PostgreSQL，通过
`agent-browser --session thief-acceptance` 执行：

| 场景 | 结果 |
| --- | --- |
| Web 健康 | `/health` 返回 `{"service":"web","status":"ok"}` |
| 匿名访问 | `/create` → `/login?returnTo=%2Fcreate` |
| 错误凭据 | 保持登录页并显示“邮箱或密码不正确。”；无 page/console error |
| 有效登录 | 回到 `/create`，显示“开始创作”和“退出”；`/works` 显示“我的作品” |
| 安全退出 | `DELETE /v1/session` 为 204，Cookie 清除，回到首页；再次访问 `/works` 被保护 |
| 移动端 | 390×844 的 `scrollWidth=390`、无横向溢出，导航与创作卡片可见 |
| 桌面端 | 1440×900 无溢出、缺失 alt=0、重复 ID=0、无名按钮=0；CLS=0、FCP=56ms、TTFB=4.4ms |

## 6. 结果

| 项目 | 当前值 |
| --- | --- |
| 结论 | `accepted` |
| 已通过 | 10/10 |
| 失败 | 0/10 |
| 阻塞 | 0/10 |
| 待执行 | 0/10 |
| 残余风险 | `platform_0001` 只允许在当前无保留数据的 pre-release 阶段重建；以后必须追加 revision，不得改写历史 |

AC-M01 至 AC-M10 全部通过，架构测试、空库迁移、集成、构建和四进程运行已在
GitHub CI `29976430520` 强制执行并成功。本结构重构验收完成。

## 7. 变更记录

- 2026-07-23：v3.0.0 完成单包、单线迁移、模块门禁、四进程和 agent-browser 验收；AC-M01 至 AC-M10 全部通过，CI `29976430520` 成功。
- 2026-07-23：v2.0.0 按 DESIGN-004 v3.0/PRD-004 v2.0 重置 AC-M，记录旧结构审计基线和五个重构工作包；旧 passed 状态失效。
- 2026-07-22：v1.9.0 回填 catalog 独立领域/Port/Adapter、三张事实表和无跨 schema asset 外键证据；AC-M03 继续等待强制写入阻断。
- 2026-07-22：v1.8.0 回填 Web same-origin BFF、Server Component 会话恢复、最小客户端交互边界和 30 个架构测试证据。
- 2026-07-22：v1.7.0 回填 identity 会话用例、FastAPI 组合根、仓储 Adapter 与最新 CI 证据。
- 2026-07-22：v1.6.0 回填 identity Port/Adapter、事务仓储与最新 CI 证据；AC-M04 仍等待全套 Adapter 契约。
- 2026-07-22：v1.5.0 回填 identity 独立领域与事实表、26 个架构测试和最新 CI 证据；AC-M03 等待 Repository 阻断证据。
- 2026-07-22：v1.4.0 回填四进程入口、生产资源和干净运行目录证据；AC-M10 等待四队列独立 Worker。
- 2026-07-22：v1.3.0 回填八模块 schema/migration、UnitOfWork 和 PostgreSQL 18 CI 证据；AC-M03 继续 pending。
- 2026-07-22：v1.2.0 将架构测试接入 `make verify` 与真实 GitHub CI。
- 2026-07-22：v1.1.0 回填前后端分层和真实 core 扫描证据，AC-M01、AC-M02 通过。
- 2026-07-22：v1.0.0 基于 DESIGN-004/PRD-004/PLAN-004 创建模块边界与工程质量验收。

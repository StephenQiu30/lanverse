# 剧本资产联合工作台验收

- 状态：accepted（DEV-S2-06 与 S2 产品切片）
- 日期：2026-07-31
- 最近复核：2026-08-04
- 对应产品：[PRD-002 S2](../prd/002-MVP纵向交付切片.md)、[PRD-008](../prd/008-创作生产模块PRD任务.md)
- 对应设计：[模块详细设计总览](../design/模块设计/001-模块详细设计总览.md)、[对象存储与任务调度](../design/模块设计/014-缓存对象存储与任务调度详细设计.md)
- 对应计划：[PLAN-000 DEV-S2-06](../plan/000-MVP全栈实施总计划.md)、[PLAN-002 S2](../plan/002-MVP纵向切片执行计划.md)
- 工程结论：统一入口、真实媒体闭环、资产授权交接、角色/场景/声线 ready、后台恢复与 DeepSeek 联合调用成立；DEV-S2-06 completed
- 产品结论：S2 accepted；真实在线提取→人工修改/接受→结构确认→设为 current 已通过

## 1. 交付结果

1. 创作首页、项目库、项目概览、账户与工作空间页面已删除演示数据，全部通过生成的 OpenAPI client 读取或提交真实 FastAPI 事实。
2. 单集工作台以 ProductionSnapshot 组合真实阶段、current ScriptVersion、ready 资产、Ready 分镜、进行中任务、blockers 和 next actions，并提供剧本、资产、分镜、媒体和任务五个可恢复入口。
3. 浏览器已完成真实登录、创建单集、导入并发布剧本、启动提取、查看失败原因、签名直传媒体及等待探测结果；刷新后状态均从 PostgreSQL 恢复。
4. 独立 Scheduler、I/O Worker 和 Media Worker 在数据库操作前统一注册已实现模块的 SQLAlchemy Model；容器 `server` 同级监督 API 与三个后台角色，修复局部 Metadata 导致 Outbox 外键无法解析的问题。
5. 本轮通过 `make minio-up` 启动当前项目唯一发布 9000/9001 的 MinIO 服务；共享 Compose 使用 `minio/minio:latest`，不维护 tag、digest 或二进制发行版守卫。development/production 分层和启动语义以 [ACC-008](./008-D006全栈Compose部署验收.md) 为准。
6. 资产 readiness 的 `review_asset_consent` 动作把固定 AssetVersion 和证明 MediaVersion 带入治理登记界面；服务端重新校验 subject 和证明访问权限，登记完成后回到资产页重读事实，无需手工复制 UUID，也不修改旧版本。
7. 验收单集已建立可用角色、场景和声线各一项；三类固定媒体均经同一 MinIO 和 Media Worker 探测后登记授权，ProductionSnapshot 显示 `Ready 资产 3 / 3`，撤销声线授权后立即降为 `2 / 3`。声线证据只使用测试代码生成的普通 PCM WAV 音调验证上传、探测和权利链，不接入或声称任何 AI Speech/Audio 能力。
8. 2026-07-30 后续增量已固定 `langchain-deepseek==1.1.0` 及全部传递依赖，通过官方 DeepSeek `deepseek-v4-pro` 非思考 JSON 输出生成候选；模型调用位于数据库事务外，响应落库失败时转 `unknown` 且重投不再调用供应商。前端已增加新 Batch 重新提取、修改后接受和同类候选合并，复用 shadcn/ui 组件与现有视觉令牌。
9. Playwright 默认以 8001/3000 启动统一 API 与 Next.js standalone，使用 `lanverse_test` 和同一 RabbitMQ 实例的 `lanverse_contract` 专用 vhost；`LANVERSE_E2E_BACKEND_PORT`/`LANVERSE_E2E_FRONTEND_PORT` 可在不终止本机既有服务时切换独立端口，后端 CORS、浏览器 base URL、健康检查和前端 API 构建地址共同取同一配置，不再复用开发 API、开发队列或 Turbopack 冷编译状态。
10. 账户/工作空间改名、归档恢复、密码修改、账户停用、全局退出，以及项目/单集编辑、预算、排序、归档和安全删除入口已恢复到当前统一视觉系统；当前浏览器以真实 API 完成 7/7 无密钥验收。
11. 自动化联合验收在独立 `lanverse-e2e` bucket 中完成三份媒体的浏览器 SHA-256、签名 PUT 和探测，再分别建立角色、场景、声线 AssetVersion 并登记固定版本授权；三类达到 3/3 ready 后撤销声线授权，单集摘要和资产详情同步重新阻断。全程使用同一本机 MinIO 实例、真实 RabbitMQ/Worker/PostgreSQL。
12. Next.js 生产构建曾因 `next/font/google` 的构建时下载失败；已改为精确锁定的本地 `geist@1.7.2` 并保持原视觉字体变量，断开字体外网后仍可生产构建。
13. 剧本版本历史已补齐版本内容查看、版本差异、current 切换影响确认、未发布 draft 删除和来源归档/恢复；后端通过 storyboards 公开读取契约返回仍固定旧 ScriptVersion 的 active Shot ID，不跨模块读取 ORM。资产工作台同步补齐身份编辑、历史 current 切换、删除预检与空身份安全删除。
14. 资产联合浏览器用例继续覆盖身份编辑、追加第二个不可变版本、历史 current 切换、归档/恢复与删除预检阻断；删除 blocker 由服务端稳定 `version_count` 驱动中文提示，不解析英文详情。
15. 媒体工作台已按 PT-MED-003 补齐对象级版本管理：列表返回所属对象 kind/source/status/current/revision；用户可在同一对象追加不可变版本、查看完整历史、用 current+revision CAS 切换当前版本并归档/恢复。archived 状态隐藏追加/切换动作，服务端同时 fail-closed 并给出 `restore_media`；普通删除仍为 405。真实浏览器链路完成“追加 v2 → 探测 ready → current 切回 v1 → 归档 → 恢复”，刷新后均从 PostgreSQL 恢复。
16. PT-GOV-006 已进入 `in_progress`：`identity.registered`、`identity.login_succeeded`、`identity.logged_out`、`identity.password_changed`、`identity.profile_updated`、`identity.account_deactivated`、`workspace.created`、`workspace.updated`、`workspace.archived`、`workspace.restored`、`project.created`、`project.updated`、`project.budget_updated`、`project.archived`、`project.restored`、`project.deleted`、`episode.created`、`episode.updated`、`episode.reordered`、`episode.archived`、`episode.restored`、`episode.deleted`、`task.created`、`task.started`、`task.succeeded`、`task.failed`、`task.unknown`，以及授权、媒体、`script.version_created`、`script.version_published`、`script.current_changed`、`script.source_archived`、`script.source_restored`、`script.version_deleted`、`asset.created`、`asset.updated`、`asset.archived`、`asset.restored`、`asset.deleted`、`asset.version_created`、`asset.current_changed`、`shot.spec_version_created`、`shot.current_spec_changed` 均在对应业务事务内追加 AuditEvent；只有 owner 可按 actor/target/action/time 查询。动作级 metadata 白名单只允许稳定 ID、revision/token_version、版本号、位置/数量、枚举、前后状态和变更字段名，不写入邮箱、密码、姓名、头像 URL、Workspace/Project/Episode/资产名称、简介、预算金额、Task input_hash/错误摘要、剧本正文、规格、提示词、授权理由、证明、文件名、hash、URL 或原始响应。幂等导入/建镜/变换/Task 结果重放和并发/CAS loser 不重复追加事件，Project/Episode 的 stale/非法命令同样零事件；无法安全解析 actor 的失败/不存在账号登录继续归属脱敏安全运行日志，不创建伪业务 AuditEvent。审核和交付动作仍待对应 S5 真实实体接入，因此本 PT 不得提前 accepted。
17. PT-PRJ-003 的 S2 持续门禁已补齐：production 用公开批量契约按 Episode 返回全部状态 Task 数量；Episode 预检返回 `HAS_TASKS`，实际 DELETE 在 Episode 锁内重新查询，预检后新增 Task 仍会被阻断且不写 `episode.deleted`；Project 预检对 active/archived Episode 汇总并同时返回 `HAS_EPISODES`/`HAS_TASKS`。现有生命周期卡直接渲染服务端 blocker，不复制计数规则。
18. PT-PRJ-003 的版本门禁不再只看 current 指针：scripts 公开批量查询覆盖 active/archived 来源下的全部 draft/published ScriptVersion，Episode 预检返回 `HAS_SCRIPT_VERSIONS` 并在锁内 DELETE 重检，Project 汇总所属全部 Episode 的版本数量。PostgreSQL 测试固定 current 为空、来源已归档仍不可删；真实浏览器显示“单集已有 2 个剧本版本”和“项目关联 2 个剧本版本”，前端继续逐条渲染服务端 blocker。
19. S3 继续收口 PT-PRJ-003：storyboards 公开批量摘要同时计数 active/archived Shot 和全部 ShotSpecVersion，Episode 预检返回 `HAS_STORYBOARD_SHOTS` 并在锁内 DELETE 重检，Project 汇总所属全部 Episode。真实 S3 浏览器在 Project 页面显示“单集已有 6 个分镜镜头（7 个规格版本）”和“项目关联 6 个分镜镜头（7 个规格版本）”，前端沿用现有生命周期卡与服务端 blocker，没有建立第二套判定。
20. S2/S3 资产增量继续收口 PT-PRJ-003：assets 公开批量摘要覆盖 active/archived Asset 和全部 AssetVersion，Project 预检返回 `HAS_ASSETS` 并在行锁内 DELETE 重检。真实 S3 浏览器在同一 Project 页面显示“项目已有 5 个资产（5 个版本）”，与分镜/Episode/Task/剧本 blocker 共用同一服务端列表。
21. Asset 自身删除预检增加 scripts 候选决议引用计数：前端按 `asset_has_candidate_decisions` 与 `decision_count` 显示“只能归档”，服务端在 Asset 锁内复查。真实浏览器对受 1 条 Decision 关联且没有版本的 Asset 不显示确认删除动作；link/delete 并发没有悬空 downstream_id。
22. Asset 自身删除预检增加 Prop/Costume 历史版本 holder/wearer 引用计数：前端按 `asset_has_related_versions` 与 `related_version_count` 显示“只能归档”。全部 Asset 写命令改为 Project → Asset 锁顺序并刷新锁定实体；连续 3 轮引用 append/delete 无悬空 UUID，既有 append/current CAS 仍只有一个 winner。
23. PT-PRJ-005 的 S3 增量把 storyboards 批量 readiness 接入 Episode/Project ProductionSnapshot。单集与项目概览复用既有 Card 显示服务端 `Ready 分镜`；Project 级资产数不按 Episode 重复累加，分镜数则按 Episode 汇总。前端不复制 readiness 或阶段规则，partial failure 继续统一显示“部分摘要不可用”。
24. 2026-08-04 本机 `.env` 由后端 Settings、Compose 和显式 Provider E2E 按各自运行边界读取，变量名与 `.env.example` 完整对齐且文件继续被 Git 忽略；DeepSeek 密钥不进入前端、文档、数据库、消息或日志，`make contract-deepseek` 使用 Node `--env-file-if-exists=../.env`，无需手工 export。
25. 最终真实联合链通过 DeepSeek `deepseek-v4-pro` 非思考提取获得 2 个场次与 3 个对白，人工修改首个场次并接受全部必需候选，确认生成 v3 并设为 current；随后使用本机 MinIO、RabbitMQ/Media Worker 与三类 ready 资产，在真实 confirmed Scene/Dialogue 上保存 Ready 分镜 1/1。

## 2. 验收证据

| 范围 | 证据 |
| --- | --- |
| 全局真实入口 | commits `bdfc237`、`9f94833`；四个单集页面和项目/账户入口使用真实 API；旧 mock 文件已删除 |
| 后台运行时恢复 | commit `d6f9e66`；架构测试验证所有已注册外键可解析、四个独立入口显式注册模型、容器入口监督 Media Worker |
| 资产授权交接 | commits `9da6d74`、`b8f3169`；前端 Red 测试固定 AssetVersion/证明媒体预填，治理入口支持 `ASSET_VERSION` 并由服务端页面校验参数 |
| 剧本与任务 | 浏览器发布 ScriptVersion v2 后启动结构提取；Scheduler/Worker 消费真实 Outbox，未配置 AI 服务时 Task 进入 `失败 / blocked` 并显示 `AI extraction service is not configured · configure_ai_service`；返回项目后 Episode 显示“单集已有 2 个剧本版本”和“单集已有 1 个任务”，Project 同时显示单集、剧本版本与任务 blocker |
| 本地 MinIO 媒体闭环 | 浏览器通过签名 PUT 上传角色图、场景图和测试 PCM WAV 音调；Media Worker 从 RabbitMQ 消费任务，经本地唯一 MinIO 与 ffprobe/image probe 后回写 ready；WAV 为测试代码生成的 8 kHz、16-bit 单声道、250 ms 普通媒体夹具，不涉及 AI Speech/Audio Provider |
| 三类 ready 资产 | commit `9d670f5` 的 Playwright 用例在每次运行时新建角色、场景、声线及固定 AssetVersion，分别完成授权并断言单集 3/3 ready；撤销声线授权后资产详情返回 `consent_revoked`，单集降为 2/3 |
| 重启恢复 | 替换并停止旧 Scheduler、I/O Worker、Media Worker 后，新进程继续运行；认证读取 ProductionSnapshot 仍返回 `asset_status=ready`、`ready=3`、`blocked=0` 及 character/location/voice 三类事实 |
| 存储与契约 | 当前项目唯一 9000 端口服务健康；真实 PUT → 完成 → 签名 GET 契约 2/2 通过，镜像版本不作为当前门禁 |
| 视觉检查 | 实际页面检查无布局破损、错误占位或伪造媒体；导航、单集切换、模块入口、上传控件、治理表单和状态卡保持已接受的 Lanverse 设计语言 |
| DeepSeek 真实联合证据 | 结构化提取、输出二次校验、未知结果防重调、新 Batch 重试、修改与合并的 Red→Green 证据成立；2026-08-04 显式 Provider E2E 覆盖浏览器→FastAPI→RabbitMQ/I/O Worker→DeepSeek→候选人工决议→确认结构→设为 current，并继续到三类 ready 资产与 Ready 分镜 1/1 |
| 剧本与资产生命周期 | commits `206b004`、`4dd7db0`、`5c62a86`、`2b5cd92`、`234079e`、`e7f111f`；后端集成测试固定 current 切换的真实 Shot 影响集合与结构化资产版本数量，前端单测和真实浏览器固定版本差异/影响确认/draft 删除/来源归档，以及资产身份编辑/追加版本/历史 current/归档恢复/删除预检闭环 |
| 媒体版本生命周期 | 后端 PostgreSQL 集成测试固定追加版本、current+revision CAS winner/冲突、archived 写入阻断、恢复与历史可读；OpenAPI 生成 client、RTK Query 和媒体工作台接入追加/current/归档/恢复，组件与页面编排单测 10/10；真实 S2 Playwright 使用本机 MinIO/RabbitMQ/Media Worker/PostgreSQL 完成 v2 追加、ready 探测、切回 v1、归档和恢复 |
| 受控操作审计 | PostgreSQL 集成测试固定身份、Workspace、Project/Episode、授权、媒体、Task 创建及 started/succeeded/failed/unknown 转换、剧本、资产和分镜规格的只追加事件；转换沿原消息 trace、归属原请求用户，事务回滚、消息/结果重放、stale 和并发 loser 不重复写入，错误摘要、input_hash 与供应商原文不落 metadata。治理页展示并筛选全部已实现动作；真实 S1 覆盖身份/Workspace 与 Project/Episode，真实 S2/S3 覆盖 Task fail-closed、剧本、资产与 ShotSpec 版本动作 |
| ProductionSnapshot 分镜摘要 | 当前增量以 PostgreSQL 固定 storyboards 故障 fail-closed、12 集批量列表及 36/120 镜头 ready 摘要；生成 client 增加 `storyboard_summary`，项目/单集组件测试验证 Ready 分镜与 Project 级资产不重复累加 |
| 隔离浏览器运行栈 | `make e2e` 7/7 通过；独立数据库、专用 RabbitMQ vhost/对象存储 bucket、8001 API 与 Next.js standalone 组合；覆盖 S0、S1 两条完整生命周期、S2 三类媒体→资产生命周期→授权→撤销联合链、DeepSeek 未配置的失败恢复和 S3 本地分镜生命周期 |
| 可重复字体构建 | `geist@1.7.2` 作为精确锁定的运行依赖，`layout.tsx` 使用 `geist/font/sans`；生产构建不再请求 Google Fonts |
| 实例基础设施契约 | 2026-08-02 显式重跑：MinIO 2/2、专用 vhost RabbitMQ 2/2、ffprobe 1/1、RabbitMQ→Media Worker→MinIO→ffprobe→PostgreSQL 1/1 |
| 供应商边界审计 | 文本仅使用 LangChain DeepSeek，图像/视频仅保留火山方舟 Seedream/Seedance 候选，未越过 D-004 门禁安装 SDK |

## 3. 已执行验证

| 命令或检查 | 结果 |
| --- | --- |
| `make check` | 通过；Ruff、Pyright、ESLint、TypeScript、后端 118 项通过/6 项显式外部契约跳过、前端 12 个文件/24 项通过、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置通过 |
| `make check`（2026-07-30 DeepSeek 增量） | 通过；后端 123 项通过/7 项显式外部契约 skipped，前端 12 个文件/27 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 生产构建和两份 Compose 配置通过 |
| `make check`（2026-07-30 当前） | 通过；后端 128 项通过/6 项默认显式跳过，前端 12 个文件/27 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置通过；重复的 DeepSeek 适配器直连契约已删除 |
| `make check`（2026-07-31 无密钥功能增量） | 通过；后端 145 项通过/7 项显式外部或性能契约跳过，前端 15 个文件/41 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置通过 |
| `make check`（2026-07-31 资产浏览器增量） | 通过；后端 147 项通过/7 项显式外部或性能契约跳过，前端 15 个文件/42 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置通过 |
| `make check`（2026-08-02 追踪复核） | 通过；后端 150 项通过/7 项显式外部或性能契约跳过、前端 15 个文件/43 项通过；新增 D-006 验收事实守卫，Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置全部通过 |
| `make check`（2026-08-03 媒体生命周期与审计复核） | 通过；后端 152 项通过/7 项显式外部或性能契约跳过、前端 16 个文件/47 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和 development/production Compose 配置全部通过 |
| `make check`（2026-08-03 身份审计复核） | 通过；后端 153 项通过/7 项显式外部或性能契约跳过、前端 16 个文件/47 项通过；新增身份审计追踪守卫，静态检查、类型检查、pip check、Next.js 生产构建和 development/production Compose 配置全部通过 |
| `make check`（2026-08-03 剧本与资产审计复核） | 通过；后端 153 项通过/7 项显式外部或性能契约跳过、前端 16 个文件/47 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和 development/production Compose 配置全部通过 |
| `make check`（2026-08-03 Project/Episode 审计复核） | 通过；后端 153 项通过/7 项显式外部或性能契约跳过、前端 16 个文件/47 项通过；新增项目/单集动作与隐私白名单回归，Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 生产构建和两份 Compose 配置全部通过 |
| `make check`（2026-08-03 Task、剧本版本、分镜与资产事实删除门禁复核） | 通过；后端 164 项通过/7 项显式外部或性能契约跳过、前端 16 个文件/47 项通过；四类 Task 转换、消息重放、隐私、追踪守卫，以及 Project/Episode 的 Task/剧本版本/active 与 archived Shot/全部规格、Project 的 active 与 archived Asset/全部版本引用和真实并发删除门禁随 Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 生产构建及 development/production Compose 配置全部通过 |
| `make check`（2026-08-03 Asset 候选决议删除门禁复核） | 通过；后端 167 项通过/7 项显式外部或性能门禁跳过、前端 16 个文件/48 项通过；新增决议引用计数、锁内复查、并发 winner、中文 blocker 与文档追踪守卫，其他发布门禁全部通过 |
| `make check`（2026-08-03 Asset 关联版本删除门禁复核） | 通过；后端 170 项通过/7 项显式外部或性能门禁跳过、前端 16 个文件/49 项通过；新增 JSONB 引用计数、统一锁顺序、identity map 刷新、并发/CAS 回归和中文 blocker，其他发布门禁全部通过 |
| `make check`（2026-08-03 ProductionSnapshot 分镜摘要增量） | 通过；后端 174 项通过/7 项显式外部或性能门禁跳过、前端 16 个文件/49 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、当前 OpenAPI client、Next.js 生产构建及 development/production Compose 配置全部通过 |
| `make check`（2026-08-03 本机 MinIO 复用闭环） | 通过；后端 175 项通过/7 项显式外部或性能门禁跳过、前端 16 个文件/49 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建及 development/production Compose 配置全部通过 |
| `OPENAPI_SCHEMA_URL=http://127.0.0.1:8003/openapi.json make generate-api && git diff --exit-code -- frontend/src/api`（2026-08-03） | 通过；从当前 FastAPI schema 重新生成 client 后零漂移，Task/剧本版本删除门禁只扩展既有结构化 blocker 内容，没有改变公开 HTTP 类型；临时 API 已正常停止 |
| `.venv/bin/pytest tests/integration/test_scripts_api.py tests/integration/assets/test_assets_api.py tests/unit/governance/test_audit_policy.py -q`（2026-08-03） | 16/16 通过；固定剧本/资产版本与生命周期事件、CAS/并发 loser 零重复、metadata 动作白名单和敏感正文拒绝 |
| `.venv/bin/pytest tests/integration/test_tasks_api.py tests/integration/test_script_extractions_api.py tests/integration/media/test_media_api.py tests/integration/governance/test_consents_api.py tests/unit/governance/test_audit_policy.py tests/architecture/test_module_boundaries.py -q`（2026-08-03） | 21/21 通过；覆盖任务创建回归、授权/媒体审计、metadata 白名单与跨模块边界 |
| `make e2e`（2026-08-02 复核） | 7/7 通过；真实 API 与后台角色、真实 PostgreSQL/RabbitMQ/本机 MinIO，包含媒体→资产身份/版本/current/归档/删除预检→授权→ready→撤销阻断和 S3 本地分镜生命周期；DeepSeek 凭据强制为空，验证可恢复失败而非伪成功 |
| `.venv/bin/pytest tests/integration/media -q`（2026-08-03） | 6/6 通过；覆盖上传、探测、追加版本、current/revision 冲突、归档/恢复与历史访问 |
| `npm test -- --run tests/unit/media-workspace.test.tsx tests/unit/episode-studio-page.test.tsx`（2026-08-03） | 2 个文件 10/10 通过；组件动作和生成 client 请求体均被断言 |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/platform-health.spec.ts tests/e2e/media-asset-governance.spec.ts`（2026-08-03） | 2/2 通过；证明可配置端口与媒体版本真实链路 |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 make e2e`（2026-08-03） | 7/7 通过，35.5 秒；真实基础设施完整回归，DeepSeek 未配置路径继续 fail-closed |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/account-workspace-lifecycle.spec.ts`（2026-08-03） | 1/1 通过，11.1 秒；真实 PostgreSQL/RabbitMQ/本机 MinIO 与 standalone 前端完成身份/Workspace 生命周期，并从 owner 治理页筛选密码修改 AuditEvent；结束后停止本轮启动的 MinIO |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/project-episode-lifecycle.spec.ts`（2026-08-03） | 1/1 通过，11.4 秒；真实 PostgreSQL/RabbitMQ/本机 MinIO 与 standalone 前端完成 Project/Episode 生命周期，并从治理页筛选 2 条 Episode 删除和 1 条 Project 删除事件；结束后停止本轮启动的 MinIO |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/script-extraction-fail-closed.spec.ts tests/e2e/media-asset-governance.spec.ts`（2026-08-03） | 2/2 通过，24.5 秒；真实 PostgreSQL/RabbitMQ/本机 MinIO 与 standalone 前端完成剧本导入/发布、资产更新/版本/current/归档恢复，并从治理页查看和筛选对应 AuditEvent；结束后停止本轮启动的 MinIO |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/storyboard-lifecycle.spec.ts`（2026-08-03） | 1/1 通过，25.8 秒（测试体 15.4 秒）；真实 PostgreSQL/RabbitMQ/本机 MinIO 与 standalone 前端创建道具版本并选择持有角色，完成候选决议/关联版本双重空资产删除阻断及完整分镜生命周期，从治理页精确筛选 7 条 ShotSpec 创建和 2 条 current 切换事件，并在 Project 页同时断言 6 镜头/7 规格与 5 资产/5 版本 blocker；结束后停止本轮启动的 MinIO |
| `.venv/bin/pytest tests/integration/assets/test_assets_api.py tests/integration/storyboards/test_storyboards_api.py tests/integration/projects/episodes/test_episodes_api.py tests/integration/projects/projects/test_projects_api.py tests/integration/test_scripts_api.py tests/integration/test_tasks_api.py -q` | 40/40 通过；覆盖资产/分镜/剧本/Task 生命周期、Project/Episode 完整 blocker、失败删除保留事实、并发导入/建镜/建资产/删除的安全结果 |
| `.venv/bin/pytest tests/integration/test_tasks_api.py tests/integration/test_inbox_consumer.py tests/integration/test_io_worker.py tests/integration/test_script_extractions_api.py tests/integration/media/test_media_worker.py tests/integration/media/test_media_api.py tests/integration/scripts/structure/test_structure_api.py tests/unit/governance/test_audit_policy.py tests/architecture/test_module_boundaries.py -q`（2026-08-03 Task 转换审计） | 30/30 通过；覆盖剧本提取与媒体探测的四类转换、原 trace/actor、消息和结果重放去重、事务回滚及错误摘要/input_hash 隐私 |
| `.venv/bin/pytest tests/integration/projects/episodes/test_episodes_api.py tests/integration/projects/projects/test_projects_api.py tests/integration/test_scripts_api.py tests/integration/test_tasks_api.py tests/architecture/test_module_boundaries.py -q`（2026-08-03 剧本版本删除门禁） | 25/25 通过；覆盖 current 为空的 active/archived 来源历史版本、Project/Episode 批量 blocker、失败删除零孤立事实，以及真实 PostgreSQL 并发导入/删除的两种安全锁顺序 |
| `npm test -- --run tests/unit/governance-page.test.tsx && npm run typecheck && npm run lint`（2026-08-03 Task 转换审计） | 治理页 5/5、TypeScript 与 ESLint 通过；复用现有 AuditTrail/Badge/Button/select 展示和筛选四类转换 |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 npx playwright test tests/e2e/script-extraction-fail-closed.spec.ts`（2026-08-03 剧本版本、Task 转换与删除门禁） | 1/1 通过，测试体 7.0 秒；真实 PostgreSQL、RabbitMQ/I/O Worker、standalone 前端和本机 MinIO 完成未配置 DeepSeek 的 fail-closed 链，显示 Episode/Project 的 2 个剧本版本与 1 个任务两级 blocker，并从治理页精确筛选 1 条 `task.failed`；未调用 Provider，结束后停止本轮 MinIO；独立 `agent-browser` 同时确认首页有内容、无错误浮层、控制台错误为空且主导航可交互 |
| `make contract-minio contract-rabbitmq contract-ffprobe contract-media-stack`（2026-08-02 复核） | 通过；分别为 2/2、2/2、1/1、1/1，RabbitMQ 契约拒绝默认 vhost |
| `LANVERSE_E2E_FRONTEND_PORT=3001 LANVERSE_E2E_BACKEND_PORT=8002 make contract-deepseek`（2026-08-04 语义化命名复核） | 1/1 通过，测试体 17.9 秒、总计 30.2 秒；真实 DeepSeek 提取、人工决议、结构确认、current、本机 MinIO 媒体探测、三类 ready 资产与真实 confirmed 起点 Ready 分镜 1/1 全链成立 |
| `.env` 安全配置复核（2026-08-04） | `.env.example` 的全部变量名均已配置且无重复，文件未被 Git 跟踪；`docker compose --env-file .env config` 通过，API `/readyz` 返回 PostgreSQL、Redis、RabbitMQ、MinIO 全部 available；校验过程未输出任何配置值或密钥 |
| `RUN_STORYBOARD_PERFORMANCE=1 .venv/bin/python -m pytest tests/performance/test_storyboard_profile.py`（2026-08-02 复核） | 1/1 通过；固定 12/36/120 Shot 样本和连续重排性能门禁真实执行，不把默认 skip 作为证据 |
| 2026-08-03 最新显式复核：`make contract-minio contract-rabbitmq contract-ffprobe contract-media-stack`、固定性能 profile、`make e2e` | 真实 MinIO/RabbitMQ/ffprobe/媒体联合栈分别 2/2、2/2、1/1、1/1 通过；性能 profile 1/1；完整无密钥浏览器 7/7 通过（42.7 秒）。DeepSeek Provider 用例按用户无密钥边界未运行，未用失败或本地 fixture 冒充成功 |
| 2026-08-04 当前代码显式复核：基础设施、性能、语义化命名 E2E 与 DeepSeek 联合链 | MinIO/RabbitMQ/ffprobe/媒体联合栈 2/2、2/2、1/1、1/1；性能 1/1，36/120 镜头 P95 10.14/52.98 ms，120 重排 30/30；完整无密钥 E2E 7/7（46.3 秒）；真实 DeepSeek→S3 Ready 分镜联合链 1/1（测试体 17.9 秒、总计 30.2 秒） |
| 独立浏览器 smoke（2026-08-02） | `http://localhost:3000` 首页与 `/login` 均渲染真实内容，无框架错误遮罩、无页面错误；导航点击成功，检查结束后会话和 Next.js 开发服务已关闭 |
| 浏览器人工验收 | 真实项目与单集完成发布剧本、提取失败恢复、角色/场景/声线媒体直传和探测、固定版本授权登记；资产页最终显示 3/3 ready、0 blocked |

## 4. Red → Green 记录

1. 真实 Scheduler 刷新 Outbox 时首先暴露 `NoReferencedTableError`：后台入口只导入局部模型，`sys_outbox_events.workspace_id` 无法解析 identity 表。
2. 新增运行时模型注册架构测试，证明所有已实现外键目标都进入同一 Metadata，并约束数据库初始化、Scheduler、I/O Worker、Media Worker 四个入口执行注册。
3. 引入唯一 `app/model_registry.py` 组合根并把 Media Worker 加入 `server` 监督列表；目标测试与完整 `make check` 全绿后，再重新运行真实后台角色完成媒体探测。
4. 资产页虽给出“前往授权治理”，但最初没有传递被阻断 AssetVersion；治理表单也只支持媒体/剧本版本，用户无法从当前动作完成资产授权。
5. 新增前端 Red 测试固定 `ASSET_VERSION` subject、证明 MediaVersion 和表单预填，再以已有组件扩展治理工作台；创建授权后重新读取 readiness，角色/场景/声线均变为 ready。
6. 对三个后台角色执行逐个替换与停止旧进程后，通过认证 ProductionSnapshot 再次确认 3/3 ready 和现有失败 Task 均从服务端事实恢复，没有重复资产版本或授权副作用。
7. DeepSeek 增量先以缺失适配器、失败批次无重试入口、候选无修改/合并控件形成 Red，再分别以 LangChain 适配和 shadcn/ui 交互转 Green。
8. Worker 在首个事务中只固定输入并将 Task/Batch 转 running，事务外调用 DeepSeek，第二事务落候选和终态；故障注入证明响应后落库失败的重投不会第二次调用模型。
9. 首次真实 RabbitMQ 契约与开发 Worker 共用 `/` 后被实时消费，无法作为可重复契约；先增加“默认 vhost 必须拒绝”的 Red 测试，再统一本地与 CI 的 `lanverse_contract` 配置，结果 4/4 安全单测和 RabbitMQ 2/2 真实契约通过。
10. Playwright 复用 8000 服务且只启 API 时无法验证 Worker，冷启动 Turbopack 还出现真实 chunk 加载竞态；加入可配置 API 绑定的 Red 测试后，改用独立组合根和 standalone 生产产物，完整 E2E 稳定通过。
11. 媒体/资产/授权原只有单模块测试与人工记录，首个 Playwright 用例先串联角色链路，并暴露和消除了构建时字体外网依赖；commit `9d670f5` 再把场景与非 AI 的普通 PCM WAV 声线纳入同一真实后台组合根，当时自动断言 3/3 ready 与撤销后 2/3，`make e2e` 6/6 与 `make check` 通过。
12. 原 `make contract-deepseek` 只直接调用适配器，无法证明业务队列、Worker、浏览器决议和确认结构。新增真实 Provider E2E 后，默认模式当前收集 7 条无密钥本地用例并强制空密钥；显式模式缺密钥会在启动前失败，使用非密钥占位值执行 `--list` 时只收集 1 条联合验收，确保真实运行只产生一次模型提取调用。
13. PT-SCR-005 审计先证明切换 current 时 `affected_shot_ids` 恒为空、版本历史缺少影响与清理动作；Red 测试固定真实 active Shot 影响后，通过 storyboards 公开契约补齐查询，并以现有 Dialog/Button/Badge 完成版本差异、影响确认、draft 删除和来源归档。
14. 资产生命周期审计先以页面测试证明身份编辑、历史 current 切换和删除预检控件缺失，再复用生成 client、RTK Query 与现有组件转 Green；删除只有服务端 preflight 明确允许时才出现确认动作。
15. 浏览器扩展测试先暴露两版本资产的英文删除详情；后端补充必填 `version_count`，前端按稳定 blocker code 和结构化数量生成中文文案，再由集成测试、单测、定向 Playwright 与全量 E2E 转 Green。
16. PT-MED-003 追溯审计先证明后端和页面都缺少显式 current 切换、恢复命令及对象级状态；PostgreSQL Red 测试首先在缺失 endpoint 处得到 404。补齐服务、OpenAPI 和现有 shadcn/ui 工作台后，组件测试再暴露追加对话框交互未被覆盖，最终由单元/页面编排测试与真实 MinIO 浏览器链共同转 Green；独立端口复核同时暴露健康检查硬编码和 CORS 漂移，修复为一组环境契约后全量 7/7。
17. 审计 Red 测试先在缺失 `/api/v1/audit-events` 时得到 404，随后固定授权幂等重放/并发 loser 不重复写入、viewer 403 与敏感 metadata 不泄露；实现只追加模型、动作白名单、owner 查询和前端筛选后转 Green。媒体与任务在同一业务事务接入，后续仍须以新 Red 用例补齐登录、审核和交付。
18. 身份审计增量先以 5 个 Red 失败固定注册、成功登录、令牌撤销、账户停用和 Workspace 生命周期事件；实现 request trace 传播与事务内写入后 6/6 转 Green。完整门禁随后暴露 Consent 测试错误地统计整个 Workspace 事件总数，将查询收紧为 consent target 后恢复全绿。失败/不存在账号登录保持完全相同 401 信封且不产生带 actor 的 AuditEvent；资料更新只记录 `display_name/avatar_url` 字段名，不复制值。
19. 剧本/资产版本审计增量先以 8 个 Red 失败固定初始剧本版本幂等、发布并发 winner、显式 current、来源归档恢复、草稿删除，以及资产身份/版本/current 生命周期；事务内接入 request trace 和动作级 metadata 白名单后相关后端 16/16、治理页 5/5 转 Green。资产更新补测固定只记录 `aliases/name` 字段名；真实 S2 两条浏览器链随后从治理页看到并筛选对应事实，未配置 DeepSeek 仍按既有设计 fail-closed。
20. 分镜规格审计增量先以 3 个 Red 失败固定手工保存、资产升级、复制、拆分、合并和显式 current 的只追加事实，并验证规格正文、内容 hash 与输入 hash 不进入 metadata；事务内接入 request trace 后相关后端 4/4、治理页 5/5 转 Green。幂等变换重放和陈旧资产升级均在写事件前返回，不产生重复审计。
21. Project/Episode 审计增量先以 5 个 Red 失败固定创建、修改、预算、排序、归档恢复与删除事实；首轮 Green 暴露 Project 数据库默认 revision 必须在写审计前 flush，以及测试对父 revision 多算一次，修正后后端 8/8、治理页 5/5 转 Green。真实 S1 浏览器链随后完成全部生命周期并精确筛选 2 条 Episode 删除与 1 条 Project 删除；metadata 只保留稳定字段，不含名称、简介或预算金额。
22. Task 转换审计增量先以 6 个 Red 失败固定 `started/succeeded/failed/unknown`、事务回滚、消息/结果重放和隐私约束；状态实际变化时由 production 服务在原事务追加事件，Worker 传播 MessageEnvelope/InboxDelivery trace，并使用 Task `requested_by` 归属原用户。定向后端 30/30、治理页 5/5 转 Green；真实 S2 fail-closed 浏览器链从治理页精确筛选 1 条任务失败，未配置或调用 DeepSeek。S4 尚不存在的 Attempt/费用/cancel/retry/reconcile 没有被预建。
23. Task 删除门禁先以 PostgreSQL Red 证明“预检 allowed 后新增 Task，Episode 仍可误删”；production 增加公开批量引用计数后，projects 在预检组装 `HAS_TASKS`，并在 Episode 锁内 DELETE 重新查询。定向后端 19/19 转 Green；同一真实 S2 浏览器场景显示 Episode/Project 两级 blocker 后继续完成 Task 审计筛选。前端复用现有服务端 blocker 列表，没有新增本地删除判定。
24. 剧本版本删除门禁先以 PostgreSQL Red 证明 current 为空时，两个真实草稿版本（其中来源已归档）仍被旧预检误判为空单集；scripts 增加公开批量版本计数，API 组合层注入 projects 自有 Protocol，Episode 锁内 DELETE 重检。真实并发导入/删除再固定“导入成功/删除 409”或“删除成功/导入 404”两种安全结果，连续复核运行不出现 500 或孤立版本；项目/剧本/任务扩展回归 25/25 转 Green。真实 S2 浏览器在同一页面同时显示版本与 Task blocker，现有前端组件无需增加本地规则。
25. 分镜事实删除门禁先以 PostgreSQL Red 证明 active/archived Shot 与全部 ShotSpecVersion 未进入 Project/Episode 分项影响；storyboards 增加公开批量摘要，API 组合层注入 projects 自有 Protocol，Episode 锁内 DELETE 重检。真实并发首次建镜/删除固定为安全结果，真实 S3 浏览器在 Project 页面显示 6 个镜头和 7 个规格版本的两级 blocker；前端复用现有组件，无新增产品补丁。
26. Project 资产事实删除门禁先以 PostgreSQL Red 证明无 Episode 但有 active/archived Asset/AssetVersion 时预检仍误报 allowed；assets 增加公开 Project 批量摘要，API 组合层注入 projects 自有 Protocol，Project 锁内 DELETE 重检转 Green。首个资产/项目删除真实并发连续 3 次通过，无孤立 Asset；S3 Playwright 在现有页面显示 5 个资产/5 个版本，无前端计数逻辑。
27. Asset 候选决议删除门禁先以完整确认结构与 `link_existing` Red 证明无版本身份可误删，再实现 scripts 公共计数、assets 自有 Protocol、结构化 blocker 与锁内 DELETE 复查。后端集成、并发 winner、页面单测、OpenAPI 与真实 MinIO Playwright 全部转 Green。
28. Asset 关联版本门禁先以 holder Red 证明空 Character 可误删，再实现 JSONB 计数与第三类 blocker。锁顺序调整暴露 append/current 双 201，`populate_existing` 修复后全部资产集成、连续并发、页面单测、OpenAPI 与真实 MinIO Playwright 转 Green。
29. 首次真实 Provider 运行中断后，重投按设计把 processing Inbox/Task 转为 `unknown/ai_result_unknown`；一次无效结构响应转为 `failed/ai_output_invalid`，均未重复伪造成功。后续真实调用返回 5 个有效候选并完成联合闭环。
30. Provider E2E 先暴露候选 Card 只定位到 Header、版本切换错误等待 status，以及新进程 `.env` MinIO 凭据与运行容器不一致；分别改为完整 shadcn Card、验证影响 Dialog，并安全同步被 Git 忽略的本机 MinIO 字段，最终真实联合链转 Green。

## 5. 接受边界与下一步

1. 本文接受 DEV-S2-06、S2 对应 PT 与 S2 产品切片；真实 Provider、媒体、授权、资产和 current 用户闭环均已成立。
2. 未配置 DeepSeek 时的 fail-closed 回归仍保留；它只验证失败恢复，不替代本次真实 Provider 成功证据。
3. 默认 `make check` 不隐式调用付费 Provider；真实 DeepSeek 仍由显式 `make contract-deepseek` 控制，并安全读取被 Git 忽略的根 `.env`。
5. D-006 已明确接受 `latest` 的便利性取舍；仓库不再维护镜像 tag、digest 或二进制发行版守卫。每次部署必须重新通过健康启动、对象存储契约和业务 smoke，且不得因本机已有健康服务再启动冲突实例。
6. Next.js 开发服务曾因内存阈值自动重启并让已打开页面短暂出现 chunk 失配；HTTP 随后恢复且生产构建通过。该现象不改变本轮服务端验收事实，但开发态内存仍需在后续长时间联调中观察。
7. 资产生命周期对话框已通过真实 Playwright 浏览器交互、类型检查和 production 构建；该证据接受功能可用性和可见反馈，不替代逐像素截图复核。剧本版本及 S3 分镜新增对话框仍保留单独的视觉复查边界。
8. 2026-08-02 当前项目唯一 MinIO 已在 9000/9001 健康运行并通过 2/2 真实契约；本轮没有安装本机二进制或启动第二实例。该显式结果不改变默认 `make check` 不启动外部服务、相关用例仍 skip 的规则。
9. PT-MED-003 及其 S2 依赖已随联合链 accepted；S4 的生成媒体与费用范围不在本次接受范围。
10. PT-GOV-006 当前已覆盖 S1/S2 的身份、Workspace、Project/Episode、授权、媒体、任务、剧本与资产动作，以及 S3 的分镜规格版本与 current 切换；Review/Moderation/Label/Delivery 实体尚未进入 S5，必须随真实业务命令继续以 Red→Green 接入，不能用预建动作名或空事件提前补齐。
11. 只复用 `baozi-minio` 这一已有容器；2026-08-04 为使新进程与运行环境一致，仅更新被 Git 忽略的本机 `.env` MinIO access/secret 字段，未把任何凭据写入仓库文档、测试输出或日志。

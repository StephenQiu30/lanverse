---
layer: Acceptance
doc_no: ACCEPTANCE-003
audience: [PM, Dev, QA, Ops]
feature_area: technical-capabilities-runtime
purpose: 验证 DESIGN-003、PRD-003 与 PLAN-003 定义的技术功能和运行能力
canonical_path: docs/acceptance/003-技术功能与运行验收.md
status: draft
execution_state: in_progress
version: 2.3.0
owner: StephenQiu
inputs:
  - docs/design/003-ai内容创作平台技术选型与功能实现设计.md
  - docs/prd/003-技术功能与运行需求.md
  - docs/plans/003-技术功能与运行计划.md
outputs:
  - AC-T01 至 AC-T12 的状态、命令、证据、风险和运行结论
triggers:
  - 技术栈、页面/API、任务、模型、队列、存储、性能或实现变化
downstream:
  - docs/operations/001-ai内容创作平台运行手册.md
  - Human Review
---

# 技术功能与运行验收

## 1. 当前结论

S0、S1-T01 至 S1-T04 和 S2-T01 已实现，结论仍为 `not_ready`。除身份与 Web 会话闭环外，catalog 领域事实、三张事实表和最小 Repository 已通过单元、迁移、集成和运行验证；1K 幂等导入、目录 API/UI、可用 MinIO S3 和后续 contract/E2E 套件尚未完成。

## 2. 环境与计划命令

实施后记录 `<hostname>:<absolute-workdir>@<short-sha>`、命令、退出码、关键输出、提交和证据文件到 `artifacts/acceptance/003/`。

```text
make bootstrap / infra-up / migrate
make test-unit / test-integration / test-contract / test-e2e
make lint / build / verify
make test-provider-live   # 显式启用，最多 10 张
```

当前环境戳：`StephenQius-MacBook-Pro.local:/Users/stephenqiu/Desktop/StephenQiu/Thief@7b9d6ff`

| S0 证据 | 命令 | 结果 |
| --- | --- | --- |
| 前端锁文件 | `pnpm --dir frontend install --frozen-lockfile` | passed |
| 后端锁文件 | `uv sync --directory backend --frozen` | passed |
| Web 构建 | `pnpm --dir frontend build` | passed，`/` 静态生成、`/health` 动态路由 |
| 静态检查 | `make lint`、`make typecheck` | passed，TS/Python 均无错误 |
| UI 检查 | production standalone + agent-browser | passed，六路由、1440×900 与 390×844、键盘 Skip Link、404、无横向溢出/overlay/console/page error |
| 服务健康契约 | `make test-unit` | passed，API live/ready、Worker/Scheduler CLI、共享 RabbitMQ 与生产配置共 9 tests |
| Compose 基础设施 | `make infra-up`、`docker compose ps` | passed，PostgreSQL、RabbitMQ、MinIO 均 healthy |
| 运行版本 | 容器内版本命令 | passed，PostgreSQL 18.4、RabbitMQ 4.3.3、MinIO `RELEASE.2026-06-06T02-44-06Z` |
| 基础端点 | PostgreSQL 查询、RabbitMQ API、MinIO health/S3 | passed，DB 18.4、RabbitMQ 200、MinIO health 200；无 license 时 S3 为预期 403 |
| UnitOfWork | `make test-unit` | passed，显式 commit、未提交 rollback、异常 rollback 共 3 tests |
| 线性迁移 | `make migrate`、`alembic heads`、`make test-integration` | passed，唯一 `platform_0001`；identity/catalog 各含 3 张事实表 |
| 迁移回滚 | `make migrate-down`、计数、`make migrate` | passed，业务 schema/revision 降至 0 后完整恢复；表/约束 checksum 两轮一致 |
| S1-T01 身份事实源 | `make test-unit`、`make test-integration` | passed，固定角色、邀请/会话状态、admin 不绕过 owner，users/invitations/sessions 约束全部成立 |
| S1-T02 邀请接受 | 单元 + PostgreSQL integration | passed，过期/重复邮箱拒绝，用户与邀请在同一事务落库，仅保存 token SHA-256 与 Argon2id 密码 hash |
| S1-T03 会话 API | API 契约 + PostgreSQL integration | passed，`POST/GET/DELETE /v1/session`、统一错误结构、HttpOnly/Secure/SameSite Cookie、双提交 CSRF、创建/恢复/注销闭环 |
| S1-T04 Web 登录 | agent-browser + architecture | passed，same-origin BFF、Server Component 会话恢复、受保护路由、完整 query 回跳、安全注销和外部回跳阻断 |
| S2-T01 目录事实源 | catalog unit + migration/repository integration | passed，固定发布状态、完整来源快照、content hash 去重、模板/示例/分类表与无跨 schema FK 的 asset 引用 |
| S2-T01 回滚 | `make migrate-down && make migrate` | passed，单线基线完整回滚/恢复后通过 7 个集成测试 |
| 四进程运行时 | `make test-runtime`、RabbitMQ management API | passed，Web/API/Worker/Scheduler 均 ok，`generation` consumers=1，进程自动清理 |
| 当前门禁 | `make verify` | passed，61 tests + lint + typecheck + standalone build；另有 7 integration tests |
| 本地运行 | `make test-integration && make test-runtime` | passed，7 个 PostgreSQL tests；Web/API/Worker/Scheduler 四进程均 ok |
| GitHub CI | run `29966616067` | passed，单线 PostgreSQL migration、7 integration、61-test 门禁与四进程冒烟 |

## 3. 验收矩阵

| AC | 状态 | 验收方法 | 必需证据 |
| --- | --- | --- | --- |
| AC-T01 | pending | 全新环境执行 bootstrap/infra-up/migrate | Compose、健康状态和迁移版本 |
| AC-T02 | passed | 构建启动 Web/API/Worker/Scheduler | `c1be2de` Red；`3d1ca02`/`ea51304` Green；运行时、health、consumer 与浏览器证据 |
| AC-T03 | pending | DiffusionDB 1K fixture 连续导入两次 | 两轮计数、manifest、checksum |
| AC-T04 | pending | API/UI 执行关键词、筛选和 cursor | 契约报告与 E2E 截图 |
| AC-T05 | pending | 并发提交同一幂等键 | job count 与 provider spy count |
| AC-T06 | pending | 在供应商边界终止 Worker 后恢复 | attempt/job 终态与费用 ledger |
| AC-T07 | pending | 受控运行 Seedream 成功任务 | usage ID、媒体处理、MinIO receipt、作品 |
| AC-T08 | pending | 并发触发三类配额 | 无超发断言与拒绝响应 |
| AC-T09 | pending | 1K 数据运行搜索/提交负载测试 | P50/P95/P99 与错误率 |
| AC-T10 | pending | 注入队列、供应商、审核、删除异常 | 指标、告警和 trace 截图 |
| AC-T11 | pending | 在干净环境运行 `make verify` | 测试/lint/类型/构建/契约/E2E 报告 |
| AC-T12 | pending | 演练迁移、kill switch、切换和恢复 | 操作轨迹、数据校验与回滚时长 |

## 4. 故障场景

- 供应商提交边界崩溃后 reconcile 到唯一终态，不重复计费。
- crawl 积压时 generation 仍按独立并发运行。
- RabbitMQ 重投、MinIO 暂时不可用和审核超时均产生可观察状态。
- live provider 测试前记录最新价格、剩余额度和负责人批准。

## 5. 结果

| 项目 | 当前值 |
| --- | --- |
| 结论 | `not_ready` |
| 已通过 | 1/12 |
| 失败 | 0/12 |
| 阻塞 | 0/12 |
| 待执行 | 11/12 |
| 残余风险 | 1K 幂等导入、目录 API/UI、MinIO S3、队列语义、异步一致性、真实生成、性能及完整 contract/E2E 套件尚未验证 |

只有 12/12 AC 全部通过且 live/fake 范围与发布配置一致时，才能更新为 `accepted`。

## 6. 变更记录

- 2026-07-23：v2.3.0 回填 S0-R01 单包结构、`platform_0001` 线性迁移、downgrade/restore checksum、61-test 门禁与 CI `29966616067`。
- 2026-07-22：v2.2.0 回填 S2-T01 catalog 领域、三张事实表、Repository、独立 downgrade/restore 和 58-test CI 证据；AC-T03/T04 等待后续 S2 任务。
- 2026-07-22：v2.1.0 回填 S1-T04 Web 登录、会话恢复、受保护路由、回跳/注销和桌面/移动浏览器证据；各 AC 状态不变。
- 2026-07-22：v2.0.0 回填 S1-T03 会话 API、Cookie/CSRF、production 数据库配置和 52-test CI 证据；各 AC 状态不变。
- 2026-07-22：v1.9.0 回填 S1-T02 邀请接受、Argon2id、事务仓储与 45-test CI 证据；各 AC 状态不变。
- 2026-07-22：v1.8.0 回填 S1-T01 身份领域、三张事实表、迁移回滚/恢复与 40-test CI 证据；各 AC 状态不变。
- 2026-07-22：v1.7.0 回填 P-W01 六路由、页面状态与最新 34-test 门禁证据；AC-T02 保持 passed。
- 2026-07-22：v1.6.0 回填四进程、RabbitMQ consumer、生产 standalone 和桌面/移动端浏览器证据；AC-T02 通过。
- 2026-07-22：v1.5.0 回填八模块 migration、UnitOfWork、幂等 upgrade、downgrade/restore 和真实 CI 证据。
- 2026-07-22：v1.4.0 回填最新稳定 Compose 真实启动证据；迁移与 MinIO S3 未完成，AC-T01 继续 pending。
- 2026-07-22：v1.3.0 回填 T-T02 健康与生产 Worker 配置证据；完整 AC-T02 继续 pending。
- 2026-07-22：v1.2.0 回填 `make verify` 与 GitHub CI 证据；AC-T11 等待后续测试套件后再判定。
- 2026-07-22：v1.1.0 回填 S0 前后端依赖、Web 构建、静态检查和 UI 证据；完整运行 AC 继续 pending。
- 2026-07-22：v1.0.0 基于 DESIGN-003/PRD-003/PLAN-003 创建技术功能与运行验收。

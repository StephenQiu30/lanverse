# 真实全链持续集成验收记录

- 状态：本机完整语言门与镜像门通过；远端当前 HEAD 待获准推送后验证
- 首次记录：2026-08-26
- 最近复核：2026-09-09
- Design：[剧本到分镜 MVP 垂直切片设计](../design/0009-剧本到分镜MVP垂直切片设计.md)
- PRD：[剧本到分镜 MVP 产品需求](../prd/0009-剧本到分镜MVP产品需求.md)
- Requirement：[剧本到分镜 MVP 需求规格](../requirement/0009-剧本到分镜MVP需求规格.md)
- Plan：[剧本到分镜 MVP 实施计划](../plan/0009-剧本到分镜MVP实施计划.md)

## 验收范围

本记录验收当前 `.github/workflows/ci.yml` 是否对实际目标架构执行质量门禁。CI 只验证 Go Backend、私有 Python Agent Candidate Runtime、Next.js Frontend、Backend OpenAPI 契约、真实 PostgreSQL/Temporal、Compose 与三类部署镜像；不保留已删除的 Python 公共 API、`agent/openapi.json` 或“compatibility runtime”路径。

## 失败证据与修复

远端最近一次 `main` CI 运行 `34248332432` 对应旧提交 `05ae24df`，不是当前本地 HEAD。Backend Job 失败于原生 Go→Agent 交接超时，Deployment Job 失败于 Agent 镜像断言的 shell/Python 引号被提前截断。定向诊断进一步确认 Agent lifespan 把 Temporal lazy client 交给 Worker，SDK 因此在启动阶段明确拒绝；环回 HTTP 请求还错误继承了宿主机代理，而测试使用依赖 Codex 的 `/readyz` 判断未安装 Codex 的基础命令服务启动。

本轮以 `5f997fb1` 固定健康/就绪边界、禁用环回代理继承并保留失败进程输出，以 `e99a1860` 让 Temporal Worker 使用真实已连接客户端，以 `0bf9ddcf` 修复镜像断言的引号和合成配置注入。没有增加等待时长、fallback、兼容路径或第二个运行入口。

Deployment Job 实际构建 Frontend、Backend 与 Agent 三类镜像，并分别检查 Next.js standalone 产物、唯一 `/usr/local/bin/lanverse` Backend 入口、Codex CLI、非 root Agent Runtime 与固定 Skill Bundle。Backend 镜像不存在旧 API/Workflow/Event 独立二进制；API、Workflow Worker 和 Event Worker 由同一进程组合启动。

## 真实验证

1. Backend 在全新 PostgreSQL 16.15 与固定摘要 Temporal 上执行 `gofmt`、`go vet ./...`、`go test -count=1 -p 1 ./...`，外部依赖测试不跳过。
2. Agent 执行 Ruff check/format、Pyright 与全部 Pytest；只检查 `agent/app` 和 `agent/tests` 正式边界。
3. Frontend 重新生成 Backend OpenAPI Client，并执行 ESLint、TypeScript、Vitest 与 Next.js 生产构建；生成目录必须无 Diff。
4. 开发/生产 Compose 均在完整必填环境下渲染；Frontend、Backend、Agent 三类镜像从各自 Dockerfile 真实构建并核对运行入口。
5. Delivery Hygiene 拒绝 secret、数据、日志和浏览器报告进入 Git，并保持 Backend/Agent 语言边界。
6. `Required / CI` 必须等 Backend、Agent、Frontend、OpenAPI、Deployment 与 Delivery 全部成功；任一 Job 失败或跳过都不能报告绿色。

2026-09-09 在不启动或重启任何本机环境服务的前提下重新验证当前提交：Agent Ruff、format、Pyright 通过，独立 PostgreSQL 与既有 Temporal 下为 `194 passed, 6 skipped`，原生 Go→Agent→Temporal 交接为 `2 passed`；6 项跳过均为显式真实 Codex 长评测，不用于抵扣最终语义验收。Backend 连接既有 PostgreSQL、Temporal、MinIO、Homebrew Kafka、Elasticsearch、Kibana 与 Logstash 后，32 个包全部通过，skip gate 为 0 个非预期跳过。Frontend ESLint、TypeScript、69 个测试、OpenAPI 无漂移和 Next.js build 通过。三类镜像顺序构建并通过上述内容断言，Compose 服务清单仍严格为 `agent backend frontend`。这些证据证明当前本地代码和镜像门，不替代尚未触发的 GitHub Runner Deployment Journey。

## 残余风险

- 当前 `main` 比 `origin/main` 领先 3 个实现/CI 提交；未经用户明确授权不会推送。只有推送后当前 HEAD 产生的 `Required / CI = success` 才是远端恢复证据。
- `agent-browser` 仍按约定只在全部功能开发完成后执行，不属于 CI 修复任务。

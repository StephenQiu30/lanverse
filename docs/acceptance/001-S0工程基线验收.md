# S0 工程基线验收

- 状态：accepted
- 日期：2026-07-28
- 对应计划：[PLAN-000 S0](../plan/000-MVP全栈实施总计划.md)、[PLAN-002 S0](../plan/002-MVP纵向切片执行计划.md)
- 工程结论：DEV-S0-01～05 completed
- 产品结论：2026-07-29 用户授权全生命周期执行与自主配置协调；S0 和对应 PT 已接受

## 1. 交付结果

1. 使用本机 Python 3.11.15、uv 0.11.32、Node.js 22.23.1、npm 10.9.8、PostgreSQL 18.4 和 Redis 8.8.0；依赖只进入 `backend/.venv` 与 `frontend/node_modules`。
2. FastAPI 提供 `/healthz`、`/readyz`、`/metrics` 和 OpenAPI；PostgreSQL 是 API 硬依赖，Redis、RabbitMQ、MinIO 失败时返回结构化 degraded 原因。
3. Next.js App Router、shadcn/ui、Radix UI、Tailwind v4 和 axios 已启动；`@umijs/openapi` 从 URL 生成到 `frontend/src/api/`，重复生成无漂移。
4. RabbitMQ 与 MinIO 有显式 Compose profile；本机已有 MinIO 时优先复用，API/Web 使用独立可重复镜像。
5. 测试与生产代码分目录；Requirement 379 个叶子、69 个 PT、10 对 PRD/Plan 和本地链接均由架构测试守卫。

## 2. 提交与 Red/Green 证据

| 范围 | 提交 | 证据 |
| --- | --- | --- |
| 工程基线 | `758ba53` | `make setup`、基线 lint/build、生产依赖审计 |
| S0 后端 Red | `5b13d8d`、`b302102` | 隐藏实现后 Pytest 因 `app` 缺失失败；测试运行方式随后适配 ASGI transport |
| S0 后端 Green | `feb29be` | Ruff、Pyright、14 项非契约测试、空库初始化、health/ready/OpenAPI |
| S0 前端 Red | `969c223` | Vitest 因 request/client 缺失和首页仍为 Hello World 失败 |
| S0 前端 Green | `b3be204` | Vitest 2/2、Playwright 1/1、TypeScript、Next build、client 无漂移 |
| 基础设施 | `0cf1369` | 真实 MinIO contract 1/1、四依赖 ready、API/Web 镜像与容器 smoke test |
| CI 维护 | `aa2ee4c`、`99af078` | `setup-uv@v9` 别名不可解析后固定为 `v9.0.0`；最新远端 CI 全部通过 |

## 3. 已执行命令与结果

| 命令/检查 | 结果 |
| --- | --- |
| `make setup` | 通过；未下载 Python、未全局安装、未隐式启动容器 |
| `make check` | 通过；Ruff、Pyright、Pytest 14 passed/1 contract skipped、ESLint、TypeScript、Vitest 2/2、Next build、Compose config、卫生检查通过 |
| 显式 MinIO 契约 | 1 passed；put/stat/stream/presign/delete 使用真实本机 MinIO |
| `/readyz` 真实依赖矩阵 | PostgreSQL、Redis、RabbitMQ、MinIO 均为 `available`，总状态 `ready` |
| OpenAPI 二次生成 | 通过；`frontend/src/api/` 无 diff |
| `make e2e` | Chromium 1 passed；首页、API 健康状态与接口状态入口可见 |
| `agent-browser` 黑盒验收 | 桌面 1440×900、移动 390×844/320×568、正常/降级/恢复、就绪入口、键盘焦点、浏览器错误和网络请求均通过；axe 30 passes/0 violations；TTFB 20.5ms、FCP 96ms、CLS 0 |
| `make docker-build` | API/Web 两个镜像构建成功；`.dockerignore` 后上下文约 154 KiB/689 KiB |
| 容器 smoke test | API `/healthz` 与 Web 首页通过；临时 Lanverse 容器已移除，原有 MinIO 未停止 |
| GitHub Actions | [run 30376192417](https://github.com/StephenQiu30/lanverse/actions/runs/30376192417) success；包含 client 漂移、`make check` 与镜像构建 |
| `npm audit --omit=dev` | 0 vulnerabilities |

## 4. 未决项与残余风险

1. 本轮直接开始实施，DEV-S0-01～05 未在进入 `in_progress` 前记录估算；这是流程偏差，不补造工时数据。
2. 默认 `make check` 不隐式启动 MinIO，因此契约项显示 skipped；本次已用单独显式命令真实通过，不能把默认 skip 单独作为验收证据。
3. 本机复用的 MinIO 容器镜像为 `minio/minio:latest`；真实端口契约已通过，仓库的备用 Compose profile 仍固定 Design 指定 digest。上线环境必须使用受控镜像和 secret 注入。
4. `@umijs/openapi` 1.13.15 的开发依赖树仍有 npm 高风险报告；该版本是当前冻结选择，生产依赖审计为 0。升级前必须重新审查生成输出、仓库来源和完整性。
5. D-002 已由 Keycloak `lanverse-test` Realm、`lanverse-api` audience、Web PKCE、标准登出、账号恢复开关和真实令牌契约关闭；生产环境仍必须使用安全部署模式和 secret 注入。
6. `agent-browser` 的截图、视频和报告保存在 `/tmp/lanverse-agent-browser-s0/`，作为本机黑盒验收证据，不提交生成产物；本轮未发现 critical/high/medium/low 问题。

## 5. 产品接受清单

- [x] 接受 S0 首页、服务状态和工程命令满足当前切片用户结果。
- [x] 接受 PT-DAT-001 的 S0 空库基线、PT-OBS-001 的 S0 日志基线和 PT-STO-001 的 MinIO 端口基线范围。
- [x] 接受以上残余风险；后续切片按首次真实用例继续控制。
- [x] D-002 已关闭，S1 可以进入 `ready`。

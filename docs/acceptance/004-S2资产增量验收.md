# S2 资产增量验收

- 状态：accepted（DEV-S2-05 工程增量）
- 日期：2026-07-31
- 最近复核：2026-08-02
- 对应产品：[PRD-008 PT-AST-001～003/005](../prd/008-创作生产模块PRD任务.md)
- 对应设计：[资产模块详细设计](../design/模块设计/007-资产模块详细设计.md)
- 对应计划：[PLAN-000 DEV-S2-05](../plan/000-MVP全栈实施总计划.md)、[PLAN-002 S2](../plan/002-MVP纵向切片执行计划.md)
- 工程结论：DEV-S2-05 completed
- 产品结论：资产身份、不可变版本、实时准备度和剧本候选交接的工程结果成立；角色/场景/声音 3/3 ready 已由 [统一前端增量验收](./005-S2统一前端增量验收.md) 补证，PT-AST-001～003/005 与 S2 仍等待真实 AI 提取、决议和结构确认的联合闭环后接受

## 1. 交付结果

1. 资产身份支持角色、场景、道具、服装、视觉风格和声音六类受区分且类型化的规格；未知字段、跨类型字段和非法引用在进入 JSONB 前被拒绝。
2. AssetVersion 只追加不改写，current 指针使用 expected revision 比较交换；并发切换只有一个请求成功，历史版本及固定媒体引用保持不变。
3. Readiness 在读取时组合类型化字段、MediaVersion 探测状态和 RightsGate 结果；缺字段、媒体未就绪、授权撤回或治理依赖不可用均返回结构化 blocker 和 next action，不持久化会过期的派生布尔值。
4. 剧本确认后的资产候选可创建新身份或幂等关联既有资产；候选决定保留来源和固定关联，不把抽取结果直接升级为可生产资产。
5. Next.js 资产工作台通过生成的 OpenAPI client 读取真实项目、资产、版本、媒资与准备度，支持六类筛选、搜索、创建与编辑身份、追加版本、显式切换历史 current、归档/恢复、删除预检、安全删除与治理跳转；服务端状态由 RTK Query 缓存并在身份、版本或授权变化后使关联事实失效重取。
6. 本地验收使用当前项目唯一 MinIO 服务，不启动第二实例；共享 Compose 的镜像与环境分层已由 D-006 固定为 `latest` 和生产薄覆盖层，当前部署事实以 [ACC-008](./008-D006全栈Compose部署验收.md) 为准。

## 2. 验收证据

| 范围 | 证据 |
| --- | --- |
| 六类身份与版本 | 单元测试覆盖六类判别规格、未知/跨类字段拒绝和不完整草稿；PostgreSQL 集成测试覆盖身份生命周期、安全删除、不可变版本与 current 并发 |
| 媒体与权利准备度 | 集成测试覆盖固定 MediaVersion、媒体探测、授权允许/撤回、跨空间隐藏和 RightsGate fail-closed；浏览器撤回授权后资产从 ready 实时变为 blocked |
| 剧本候选交接 | 结构确认测试覆盖 required/unresolved/blocking 候选，以及 create/link 的重复请求幂等 |
| OpenAPI/前端 | 生成 `assets.ts` 与类型；commits `5c62a86`、`2b5cd92`、`234079e`、`e7f111f` 以 Red→Green 覆盖真实资产查询、创建、身份编辑、版本追加、历史 current 切换、归档/恢复、删除预检/安全删除和基于结构化 `version_count` 的中文 blocker；Next.js 生产构建包含 `/studio` |
| 本地 MinIO | 当前项目只运行一个发布 9000/9001 的服务；健康检查与真实 PUT → 完成 → 签名 GET 契约 2/2 通过，镜像版本不作为门禁 |
| 浏览器 | `make e2e` 在真实 FastAPI/PostgreSQL/RabbitMQ/Worker/本地 MinIO 与 Next.js standalone 中完成“上传三类媒体 → 创建角色/场景/声线 → 登记授权 → 3/3 ready → 编辑资产身份 → 追加 v2 → current 切回 v1 → 归档/恢复 → 两版本删除预检阻断 → 撤销授权 → 2/3 ready”；当前全栈 7/7 通过 |

## 3. 已执行验证

| 命令或检查 | 结果 |
| --- | --- |
| `make check` | 通过；Ruff、Pyright、ESLint、TypeScript、115 项默认 Pytest、11 个 Vitest 文件/19 项测试、依赖检查、OpenAPI 漂移、Next.js 生产构建和两份 Compose 配置均通过；6 项显式外部契约按默认规则 skipped |
| `MINIO_ACCESS_KEY=<本机实例配置> MINIO_SECRET_KEY=<本机实例配置> make contract-minio` | 2/2 通过；凭据只在进程内注入，使用本机现有 MinIO，不创建临时或第二实例 |
| `npm run test -- tests/unit/studio-page.test.tsx` | 2/2 通过；资产加载、错误恢复、创建、追加版本、归档/恢复及准备度 blocker 均由生成 client mock 驱动 |
| `make check`（2026-07-31 生命周期浏览器增量） | 通过；后端 147 项通过/7 项显式外部或性能契约跳过，前端 15 个文件/42 项通过；Ruff、Pyright、ESLint、TypeScript、pip check、Next.js 16.2.12 生产构建和两份 Compose 配置通过 |
| `npm test -- --run tests/unit/studio-page.test.tsx` | 6/6 通过；覆盖身份编辑、历史 current 切换、删除预检和结构化版本数量的中文 blocker 交互 |
| `npx playwright test tests/e2e/s2-media-assets-governance.spec.ts` | 1/1 通过；使用真实服务与对象存储覆盖身份、版本、current、归档恢复、删除阻断、授权和 readiness 生命周期 |
| `make e2e` | 7/7 通过；资产生命周期包含在 S2 联合用例中，新增 S3 本地分镜闭环；DeepSeek 凭据保持为空并只验证可恢复失败 |
| `make contract-minio`（2026-07-31） | 2/2 通过；该历史对象存储结果继续有效，原版本冻结条件已被后续 D-006 决策替代 |
| 浏览器人工验收 | 真实数据链路通过；新增场景无版本时不再短暂显示上一个资产的 readiness，授权撤回后角色资产立即显示中文阻断原因 |

## 4. 接受边界与残余风险

1. 本文只接受 DEV-S2-05 工程增量，不提前接受 PT-AST-001～003/005、WP-05 或 S2。角色、场景、声音各一项 ready 及撤销后的 2/3 阻断已有真实浏览器证据；S2 产品接受仍受真实 DeepSeek 联合链路约束。
2. 资产 UI 的身份编辑、追加版本、历史 current 切换、归档/恢复和删除预检已由真实浏览器交互证明，不再只依赖生成 client mock。该证据验证功能和可见状态，不等于逐像素截图对比或设计走查。
3. 浏览器验收使用实际签名 PUT 写入独立 `lanverse-e2e` bucket，并启动 RabbitMQ、Media Worker、MinIO、ffprobe 和 PostgreSQL 的真实链路；测试生成的普通 PCM WAV 只作为媒体夹具，不代表 AI Speech/Audio 能力。
4. current ScriptVersion、聚合 Snapshot、提取决议入口和资产工作台已由 DEV-S2-06 组合；当前缺口只是真实 DeepSeek Provider 的提取→决议→确认→设为 current 联合证据，不得用未配置服务的失败状态代替。
5. 默认 `make check` 不隐式启动外部服务；其中 6 项基础设施契约与 1 项固定性能 profile 的 skip 不能解释为通过。2026-08-02 已显式重跑 MinIO、RabbitMQ、ffprobe、媒体联合栈和分镜性能门禁并全部通过；DeepSeek 仍是唯一未运行的外部 Provider 门禁。

## 5. 工程接受清单

- [x] 六类资产规格、身份生命周期和租户隔离成立。
- [x] 不可变版本、current CAS、固定媒体引用和结构化 readiness 成立。
- [x] 剧本候选 create/link 交接幂等且不越过人工决议。
- [x] 生成 client、真实资产工作台与授权变化后的缓存失效完成。
- [x] 身份编辑、历史 current 切换、删除预检与安全删除交互完成。
- [x] 真实 PostgreSQL、当前项目唯一 MinIO、浏览器和全量质量门禁通过。
- [x] 明确保留 PT-AST、WP-05、M2 与 S2 的未接受边界。

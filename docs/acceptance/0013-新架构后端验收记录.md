# 新架构后端验收记录

目标与执行范围见 [0013 后端实施计划](../plan/0013-新架构后端实施计划.md)。本记录只描述实际完成的代码和验证；完整文本与分镜 MVP 尚未完成。

## 已完成的 Backend 能力

1. 原稿证据读取：固定已接受版本、Unicode code point 半开范围、最多 16,384 字符、源 hash 与片段 hash 校验；旧引用不随新原稿 Head 漂移，每次读取验证当前项目权限。
2. 分镜文本采纳接口：复用既有 Go Owner 的 FreezeIntentSet，通过已有 ReviewDecision 冻结导演意图，原子写入 Owner Receipt；补充严格 hash 格式验证。公开接口没有创建新的 Go 创作 Workflow。
3. 草案集合和正式文本恢复查询：按集合读取唯一回执并验证内容和视觉需求 hash；不再次调用模型，不要求已采纳的历史候选仍为最新 Head。文本采纳后仍可保持 needs_asset。
4. 严格 JSON 解码拒绝合法对象后附加的乱码、截断对象、多对象和超限后缀；修复 Script not-found 错误被映射成 500。
5. 同步公开 OpenAPI、生成的 TypeScript 类型和接受后的设计优先级。

当前分镜采纳仍读取 Go-owned 候选；新 Python 提案接入不能由这些测试抵扣。

## 本机执行证据（2026-09-07）

直接复用本机 PostgreSQL 与 Temporal。数据库测试创建同一 PostgreSQL 实例内的临时库，结束后删除该库；没有为最终验证启动或重启基础设施服务。测试使用合成数据。

| 命令 | 实际结果 |
| --- | --- |
| `go test -json -count=1 ./...`（初始无外部测试配置） | 376 通过、84 跳过、0 失败；不是全量集成通过 |
| `go test -json -count=1 -p 1 ./...`（本机 PostgreSQL 临时库及 Temporal） | 463 通过、18 跳过、0 失败 |
| `go test -json -count=1 -p 1 -race ./tests/production/script/... ./tests/production/storyboard/... ./tests/production/planning ./tests/platform`（本机 PostgreSQL 临时库） | 37 通过、1 跳过、0 失败；包含最后新增的非法 hash 回归测试 |
| `go test -race ./tests/production/script/... ./tests/production/storyboard/... ./tests/platform ./tests/bootstrap` | 通过；此轮未配置数据库，数据库部分由上一行真实数据库 Race 验证补充 |
| `go vet ./...` | 通过 |
| 修改文件的 `gofmt -l`、`goimports -l` | 通过，无待格式化文件 |
| `golangci-lint run --new-from-rev=HEAD ./...` | 0 issues；仅表示本次增量通过 |
| 全仓 `golangci-lint run` | 未通过；既有 ST1005 错误字符串大小写告警，未批量改动无关模块 |
| `govulncheck ./...` | 退出 0；未发现本代码调用到已知漏洞，另发现导入包 2 项和所需模块 3 项未触达漏洞记录，不能解释为依赖没有漏洞 |
| `npm --prefix frontend run openapi2ts`、`npm --prefix frontend run typecheck` | 通过 |
| `git diff --check` | 通过 |

全量中的 18 项跳过包括 MinIO/媒体、Kafka、ELK/Search 外部集成和进程 helper；本轮未用媒体链验证抵扣文本 MVP。所有原稿发布/证据读取与新增回执持久化测试实际访问了本机 PostgreSQL。

执行日志保留于本机 `/tmp/lanverse-mvp-full-tests.jsonl`、`/tmp/lanverse-mvp-isolated-tests.jsonl`、`/tmp/lanverse-mvp-govulncheck.log`；不提交日志。临时运行脚本为 `/tmp/lanverse-mvp-test.py`，只在内存中将本机 Python 风格 DSN 转换为 PostgreSQL DSN，再建立并清理临时测试库，不输出连接凭据。

测试过程曾先尝试对既有开发库执行项目原有 schema.Sync，因 med_upload_sessions.kind 旧行存在 NULL 而失败。随后停止在该库上验证并改用临时测试库，没有回填或清理旧数据。由于没有同步前的数据库结构快照，不能确认失败前是否已有部分 DDL 提交；开发库迁移问题仍需独立核对，不能声称既有库结构完全未变化。

## 尚未完成的完整 MVP

- [ ] M01：真实文件导入与新 Agent 消费的组合验收；正式源发布与证据读取已经单独验证。
- [ ] M02：Go/Python 持久接受、启动及原身份恢复已实现并完成本机跨语言服务验证；既有业务 API 正式切流尚未执行。
- [ ] M03：Python 全稿设定与身份提案接入现有正式 Owner，完成审阅与采纳。
- [ ] M04：新 Python 文本分镜提案进入已验证的正式采纳能力；真实模型语义验收。
- [ ] M05：完整主链和新 Python Workflow 的进程重启、迟到结果、版本冲突与恢复验收。

本轮未实施图片/视频生成、计费产品、完整画布或前端页面。没有把旧 Go Workflow 测试结果标为新 Python 编排已完成。

## 继续实现：Go 新创作运行可靠交接

新增 `production/creation` 完整 Go 模块及两张 GORM 表：运行接受记录与命令 Outbox。公共创建/列表/查询/重投接口、短时签名 HTTP 客户端和 API 进程内的 Dispatcher 已组合到现有服务中。新流程默认禁用，启用需要独立地址和授权密钥；模板及 Compose 已同步，但没有修改本机 `.env`、重启服务或对开发业务库同步新表。

验证覆盖：未正式接受原稿禁止创建、同键异输入冲突、8 个并发创建收敛为一个运行、Outbox 失败回滚运行、过期 fence 禁止落库、按当前权限显式重投、保留原路由和命令、响应丢失后从独立数据库连接池查询原回执、撤销权限后禁止新提交但允许已接受结果对账、严格回执身份/hash 校验、重定向及恶意后缀拒绝、数据库取消错误不误判为权限撤销。命令使用 Go/Python 共用的 production canonical fixture。

| 本次命令 | 结果 |
| --- | --- |
| `go test -json -count=1 -p 1 ./...`，本机 PostgreSQL 临时库及 Temporal | 482 通过、18 跳过、0 失败；临时库已删除 |
| `go test -json -count=1 -p 1 -race ./tests/production/creation/... ./tests/production/script/... ./tests/config ./tests/bootstrap ./tests/architecture`，本机 PostgreSQL 临时库 | 95 通过、0 跳过、0 失败；包含独立连接池恢复与原子失败测试 |
| 最后补充错误分类后 `go test -race ./tests/production/creation` | 通过；保留安全且可区分的上游授权拒绝、路由不存在、命令冲突和命令无效代码 |
| `go vet ./...` | 通过 |
| 新 Creation 模块及测试的全规则 `golangci-lint run` | 0 issues |
| `golangci-lint run --new-from-rev=HEAD ./...` | 0 issues；全仓既有告警仍然未批量清理 |
| `govulncheck ./...` | 退出 0，未触达已知漏洞；仍有导入包 2 项、所需模块 3 项未触达漏洞记录 |
| `pytest -q tests/contract tests/architecture` | 39 通过 |
| 新 Python fixture 测试的 `ruff check`、`pyright` | 通过，0 错误/告警 |
| `npm --prefix frontend run openapi2ts`、`npm --prefix frontend run typecheck` | 通过 |
| 修改 Go 文件 `gofmt`、`goimports` 与 `git diff --check` | 通过 |

本次日志：`/tmp/lanverse-creation-full-tests.jsonl`、`/tmp/lanverse-creation-race-tests.jsonl`、`/tmp/lanverse-creation-govulncheck.log`。这些是本机诊断文件，不进入 Git；较早使用的通用 `/tmp/lanverse-mvp-isolated-tests.jsonl` 会被后续测试覆盖。

**未完成范围没有改变**：M02 的 Go 侧交接已经实现，HTTP 对端仍是协议故障夹具。真实 Python 接受/启动 Outbox、Temporal 创作 Workflow、设定与分镜提案以及完整语义链尚未接入；不得将状态 accepted 解释为创作完成，也不得将本次证据作为完整 MVP 验收。

## 继续实现：Python 可信命令服务（2026-09-07 至 09-08）

`agent/app/creation` 现在提供独立 FastAPI 接受服务、严格命令与 HMAC 合同、PostgreSQL 接受/启动 Outbox 事务、确定性 Temporal 启动、租约 fencing 与恢复。Task Queue 随首次接受固定保存；进程取消传播到当前尝试，后续领取等待原租约过期。同键异输入拒绝，同输入重复投递返回原回执。

数据库迁移由显式 CLI 执行，初次要求独立空库，重复迁移检查 schema checksum；服务启动只读检查。可信依赖为可选 creation extra，沿用现有虚拟环境安装并锁定 psycopg 3.3.5、temporalio 1.32.0。两种镜像分别复制职责所需模块，受限 Harness 不含可信服务代码，可信镜像不含模型 CLI/Harness。CI 安装 creation extra 后覆盖新增代码的 Ruff/Pyright/单测；未启动容器验证镜像。

### 本机验证证据

| 命令与执行条件 | 结果 |
| --- | --- |
| `python3 /tmp/lanverse-agent-test.py tests`：原生 PostgreSQL 临时库、已有 Temporal、既有 agent/.venv | **93 通过、5 跳过、0 失败**；5 项均为需显式启用的真实 Codex 语义测试 |
| 测试内真实 Agent HTTP 进程及 `go test -count=1 -race ./tests/production/creation -run '^TestNativeAgentCommandAcceptance$'` | 通过；真实 Go HMAC/canonical 命令、Python 事务回执、重复投递与查询相符 |
| Agent 测试进程停止后再次启动 | 返回原 receipt_id/accepted_at，Temporal run_id 保持不变；关闭历史仍只对账，memo 冲突拒绝绑定 |
| 数据库故障、并发接受、Outbox 写失败、过期 fence、取消、未知结果、启动窗口/保留期及迁移漂移 | 回归通过；不会返回虚构接受或覆盖新领取结果 |
| `ruff check app tests`、`ruff format --check app tests`、`pyright app tests`、`uv lock --check` | 通过；Pyright 0 errors/warnings |
| `go test -race ./tests/production/creation` | 通过；单独运行时原生对端用例按缺少测试 URL 跳过，上述 Python 集成用例中已真实执行 |
| `go vet ./tests/production/creation`、新增 Go 文件 `goimports -l`、Creation 测试包 `golangci-lint run` | 通过，lint 0 issues |
| `go test -race ./tests/architecture` | **失败 2 项**：本轮执行期间工作区出现 backend/docker-entrypoint.sh 和 backend/observability/ 删除；runtime_entrypoint 与 semantic_naming 检查因路径缺失失败 |

测试没有重启 PostgreSQL、Temporal 或既有业务进程。所有 Agent 测试进程已退出，临时数据库已精确删除，合成 Workflow 已终止；其历史按已有 Temporal 保留策略清理。日志保留于 `/tmp/lanverse-agent-full-tests.log`，不提交日志或本地凭据。

通过 GitHub 插件复核 [Temporal Python SDK 官方说明](https://github.com/temporalio/sdk-python/blob/main/README.md)，并核对 [Workflow ID 保留期约束](https://github.com/temporalio/documentation/blob/main/docs/encyclopedia/workflow/workflow-execution/workflowid-runid.mdx)。启动前实际查询 Namespace 至少 24 小时保留期，首次接受超过 12 小时且原 ID 不存在时阻塞自动启动，避免把过期历史误判为从未执行。

### 当前边界

未注册占位创作 Worker，真实文本 Workflow/Activity、来源读取桥接、草案/事件事务及正式采纳仍待实现。Temporal 测试证明可靠 start 和身份恢复，不抵扣模型、草案或完整 MVP 验收。没有修改实际 .env、迁移开发业务库、启动常驻新环境、正式切流或部署；没有执行镜像构建和新模型调用。当前 Go 架构检查的并行删除项已保留，不能把历史全绿记录当成本轮全绿。

## 提交前快照校验（2026-09-08）

- 测试提交 `bc7155e5`、实现提交 `1f2ec552`，均位于 main；明确排除 7 项来源未确认的启动/观测文件删除。
- 独立校验工作树暴露语义命名检查对 Temporal Python 官方协议路径的误报。新增失败用例后修正规则，提交 `a8ff7aab`；同一行项目自有数字命名仍被拒绝。
- 待推送代码快照执行 `go test -race ./tests/architecture ./tests/production/creation ./tests/config ./tests/platform` 全部通过。独立工作树保留 Git 中的启动与观测文件，因此与原工作区的文件缺失失败明确区分。
- 本机再次执行 Agent 全量测试：93 通过、5 项真实模型测试跳过。真实 Go→Python→Temporal 交接与重启恢复仍通过；未启动新基础设施。
- 文件白名单与凭据模式检查通过；真实配置、临时日志、数据库及 7 项删除均不进入提交。原工作区仍会保留这 7 项删除，不宣称工作区干净。

## Harness 专业文本链验证（2026-09-08）

重新阅读了 Obsidian 原始规范中的全稿解析、Skill、导演分镜、运行与画布合同，并通过 GitHub 连接核验 Agent Skills、Anthropic skill-creator、LibTV 的固定提交与许可证位置。调研结论、四类任务、来源与恢复合同见 [3004 第 11 节](../design/3004-AgentHarness专业能力与创作流程设计.md#11-文本链实施合同2026-09-08)。参考的是组织和评测方法，没有导入外部脚本、动态安装 Skill 或向第三方视频服务上传原稿。

实际新增 `agent/app/modules/text_storyboard`、`agent/skills/text-storyboard` 和 `/internal/text-storyboard/invocations`。四类任务为 map_manuscript、analyze_episode、build_world、direct_scene，已在现有候选服务挂载；旧包内容与摘要不变。任务固定源、scope、输入和发布摘要，结果始终为 needs_review。专业逻辑保持无数据库/Temporal 依赖，没有新建常驻服务或占位 Workflow。

| 验证 | 命令与真实结果 |
| --- | --- |
| 全 Agent 回归与现有本机服务 | `python3 /tmp/lanverse-agent-test.py tests`：最终 122 passed、6 skipped；复用现有 PostgreSQL/Temporal，临时测试数据库已删除。跳过为 5 项旧真实模型测试及 1 项新真实模型评测；新评测已单独执行 |
| 真实候选主链 | `LANVERSE_TEST_REAL_CODEX=1 LANVERSE_TEXT_EVAL_OUTPUT=/tmp/lanverse-text-storyboard-eval-presence .venv/bin/python -m pytest -q -s tests/integration/test_text_storyboard_real_codex.py`：1 passed，418.46 秒 |
| 子进程取消与原 Harness 回归 | `.venv/bin/python -m pytest -q tests/unit/test_codex_process_lifecycle.py tests/unit/test_storygraph_harness.py`：7 passed；包括真实子进程/孙进程输出超限、进程组退出与回收 |
| Python 静态检查 | `.venv/bin/ruff check app tests`、`.venv/bin/ruff format --check app tests`、`.venv/bin/pyright` 均通过；67 个 Python 文件格式一致，类型检查 0 errors |
| 差异检查 | `git diff --check` 通过；本轮没有修改 Go 源码，因此未重跑 Go 全套质量门禁 |

真实输入为原设计的合成“钥匙、蒙面人与闪回”，并非用户业务原稿。最终结果：3 集、6 场、10 个候选实体、5 条状态事件；第一集生成 3 个文字镜头。模型提取的三条对白逐字等于黄金样本，第一集台词中提到的周野标为 mentioned；分镜未将其作为可见角色。输出通过来源、引用、范围和必拍节拍/对白/可见细节覆盖检查。WorldBook 保留 13 项待审问题，分镜保留 6 项问题（含平台披露映射未接线的 blocker），不能称为人工语义审阅通过。

- 原稿 SHA-256：`9e9d4cd791566c0f252f003a2c10545d9a554272845000c3bb166fe7b50b5a1e`。
- Skill Release：`fde472421e82aeb5679b942c3d0fd53d6533d9ed1cc34b3b351af0d2763e3683`，绑定专业规则、输入/严格输出 Schema 和执行上限。
- 本机完整原始候选、已校验结果及 `summary.json` 位于 `/tmp/lanverse-text-storyboard-eval-presence/`；人可读结果为该目录的 `评测审阅记录.md`。它们是临时评测产物，未纳入 Git，不是业务草案持久库。

本轮 Red/故障证据与修复：缺少新模块时合同测试先失败；原 Codex 取消路径未 kill/wait 的回归先失败；真实模型暴露同块重复姓名定位、严格 Schema 的可空字段以及全局身份证据与场内事件证据的区别，分别补充 occurrence、严格 Schema 编译和受限跨场引用规则。真实输出检查还发现“被提及”和“实际出场”需要独立字段，现已加入 presence 及错误入画拒绝测试。输出超限测试发现 kill 后满管道仍会使 wait 阻塞，现改为终止进程组、排空管道并等待。最后以失败测试固定 Pydantic frozen 模型内列表仍可被调用者修改的问题，Harness 现于推理前拥有独立、重验证的快照。先前失败/中断均不计为通过，最终发布摘要对应的真实链路已重新验证，后续生命周期与快照修复经针对性及完整回归通过。

仍未完成：Go 固定源读取桥、Agent 草案/持久预算/OutputBinding/Outbox 事务、Temporal 生产 Workflow/Activity、平台审阅与正式采纳回执、已审阅跨场连续性映射、超长稿分块归并和独立盲评。没有以测试缓存或草案串联代替这些能力；M03–M05 仍未验收。没有修改本机 `.env`、启动/重启基础设施或执行业务库迁移。

本轮改动逐项：`agent/README.md` 说明调用与限制；`candidate_runtime/api.py` 挂载新路由，`text_storyboard_api.py` 实现签名与受限执行；`text_storyboard/schemas.py`、`source.py`、`validation.py`、`harness.py` 分别承载合同、原文索引、确定性检查和上下文/推理循环，`__init__.py` 声明模块；`storygraph/harness.py` 补进程安全和严格输出支持；新 Skill 的 SKILL.md 及 manuscript/episode/world/directing 四份 reference 实现专业规则；新增 unit/integration 测试与合成文本夹具，更新技能目录架构断言；3004、实施计划及本记录同步实际状态。

本轮未提交或推送，分支仍为 main，HEAD 为 `0994fe9f`。此前来源未确认的七项删除均保留，未纳入本轮实现：

| 既有删除 | 本轮处理 |
| --- | --- |
| `backend/docker-entrypoint.sh` | 保留现场 |
| `backend/observability/elasticsearch/ilm-policy.json` | 保留现场 |
| `backend/observability/kafka/healthcheck.sh` | 保留现场 |
| `backend/observability/kafka/init.sh` | 保留现场 |
| `backend/observability/logstash/config/logstash.yml` | 保留现场 |
| `backend/observability/logstash/pipeline/lanverse.conf` | 保留现场 |
| `backend/observability/logstash/template/lanverse-logs-template.json` | 保留现场 |


### 提交前补充：可信执行存储（2026-09-08）

新增 `creation/execution.py`、`execution_schema.py`：运行预算冻结、步骤身份和输入绑定、先预留后执行、并发领取互斥、过期 unknown、迟到 fence 拒绝、结果重新校验，以及草案/OutputBinding/result_ready Outbox 原子提交。迁移追加到既有命令库，保留原迁移校验和。纯 Schema、来源、验证与任务合同移至 `app/text_contract`，两种镜像显式装载，可信镜像仍不包含 Harness/Skill/Codex。

提交前实际验证：`python3 /tmp/lanverse-agent-test.py tests` 为 **127 passed、6 skipped**（5.36 秒），复用现有本机 PostgreSQL/Temporal，测试临时库已删除；6 项均为显式选择执行的真实模型测试，本次提交前未重新调用。`ruff check app tests`、`ruff format --check app tests`（73 个文件）、`pyright -p agent` 全部通过。新增 5 项数据库测试覆盖并发/响应恢复、过期结果、不可变输入与策略、未知用量不退预算、Outbox 写失败整体回滚。

这修正上节的存储缺口状态：**持久存储组件已经实现，生产编排接线仍未完成**。执行 HTTP 入口、Go 固定源桥、Temporal 生产 Workflow/Activity、结果事件投递、四道审阅及正式 Owner Effect 回执仍未实现；不会把组件测试称作完整生产闭环。没有执行本机业务库迁移或重启服务。

## Git 与逐文件修改

2026-09-08 用户授权直接提交并推送 main。本轮按测试、实现、架构检查与文档拆分提交；下表保留完整变更清单，标记删除的 7 项只保留在原工作区，不纳入提交。没有手工部署或修改本机服务配置。

| 文件 | 修改用途 |
| --- | --- |
| `.env.example` | 新增显式可选 Creation 地址和独立授权密钥配置 |
| `.env.production.example` | 新增显式可选 Creation 地址和独立授权密钥配置 |
| `.github/workflows/ci.yml` | Agent CI 安装可信服务 extra，覆盖新增模块的质量检查 |
| `agent/Dockerfile` | 受限镜像只复制候选 API/Harness 所需模块 |
| `agent/Dockerfile.creation` | 独立可信命令服务镜像，不包含 Codex CLI/Harness |
| `agent/README.md` | 独立进程配置、显式迁移、本机测试和能力边界 |
| `agent/app/creation/__init__.py` | 可信服务模块入口，不导入受限执行代码 |
| `agent/app/creation/api.py` | 独立签名接受/查询入口、readiness 与 Dispatcher 生命周期 |
| `agent/app/creation/authorization.py` | 绑定 method/path/body/有效期的独立 HMAC 授权 |
| `agent/app/creation/config.py` | 显式专库配置、独立密钥与 Temporal TLS 校验 |
| `agent/app/creation/contract.py` | 严格命令/回执身份及 production canonical hash |
| `agent/app/creation/dispatcher.py` | 启动尝试的超时、取消、冲突阻塞和持久重试 |
| `agent/app/creation/migrate.py` | 显式 Agent 专库迁移 CLI |
| `agent/app/creation/repository.py` | 接受/启动 Outbox 事务、原回执、租约 fencing 与迁移 checksum |
| `agent/app/creation/temporal.py` | 确定性 Workflow ID、原身份对账、去重及历史保留保护 |
| `agent/pyproject.toml` | 以可选 creation extra 固定 PostgreSQL/Temporal 依赖 |
| `agent/requirements-creation.txt` | 从 uv.lock 导出可信镜像依赖 |
| `agent/tests/architecture/test_runtime_language_boundaries.py` | 验证 Harness 导入与镜像能力隔离，允许可信服务的存储/Temporal 依赖 |
| `agent/tests/contract/test_creation_command_canonical.py` | 验证 Go/Python 共用的命令 canonical hash 样例 |
| `agent/tests/creation/conftest.py` | 仅连接显式独立临时数据库的测试 fixture |
| `agent/tests/creation/test_api.py` | HTTP 签名、重放、冲突、输入上限和存储失败 |
| `agent/tests/creation/test_config.py` | 独立凭据、TLS、必要配置和不回退平台数据库 |
| `agent/tests/creation/test_contract.py` | 跨语言 hash、严格 JSON/身份和 HMAC 边界 |
| `agent/tests/creation/test_dispatcher.py` | 未知结果、冲突、保留期、取消和原命令恢复 |
| `agent/tests/creation/test_native_runtime.py` | 真实 Go 客户端→Python→Temporal 和测试进程重启验证 |
| `agent/tests/creation/test_repository.py` | 真实 PostgreSQL 并发/原子性/fence/回执/迁移回归 |
| `agent/uv.lock` | 锁定可信服务依赖，保留原有依赖版本 |
| `backend/api/openapi/lanverse-public-api.json` | 原稿证据、分镜文本与新创作运行的公开接口合同 |
| `backend/docker-entrypoint.sh` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/internal/bootstrap/api_process.go` | 组合公开路由和 Dispatcher，取消并等待后台退出 |
| `backend/internal/config/config.go` | 验证新服务地址、HTTPS 与独立密钥，默认关闭新流程 |
| `backend/internal/platform/database/model/creation.go` | 运行/Outbox GORM 模型，或真实 PostgreSQL 与 HTTP 交接验收 |
| `backend/internal/platform/database/schema/catalog.go` | 将两张 Creation 表注册到现有唯一 GORM Catalog |
| `backend/internal/platform/httpapi/response.go` | 拒绝非 EOF 的 JSON 后缀 |
| `backend/internal/production/creation/adapter/agenthttp/client.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/creation/adapter/gormdb/repository.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/creation/adapter/httpapi/handler.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/creation/application/dispatcher.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/creation/application/service.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/creation/domain/run.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/internal/production/script/adapter/gormdb/repository.go` | 固定源证据与权限；保留数据库故障错误链 |
| `backend/internal/production/script/adapter/httpapi/handler.go` | 固定源证据与权限；保留数据库故障错误链 |
| `backend/internal/production/script/application/source_span.go` | 固定源证据与权限；保留数据库故障错误链 |
| `backend/internal/production/script/domain/source.go` | 固定源证据与权限；保留数据库故障错误链 |
| `backend/internal/production/storyboard/adapter/gormdb/repository.go` | 分镜文本采纳、精确回执查询与恢复 |
| `backend/internal/production/storyboard/adapter/httpapi/handler.go` | 分镜文本采纳、精确回执查询与恢复 |
| `backend/internal/production/storyboard/adapter/httpapi/intents.go` | 分镜文本采纳、精确回执查询与恢复 |
| `backend/internal/production/storyboard/application/intent_query.go` | 分镜文本采纳、精确回执查询与恢复 |
| `backend/internal/production/storyboard/application/service.go` | 分镜文本采纳、精确回执查询与恢复 |
| `backend/observability/elasticsearch/ilm-policy.json` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/observability/kafka/healthcheck.sh` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/observability/kafka/init.sh` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/observability/logstash/config/logstash.yml` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/observability/logstash/pipeline/lanverse.conf` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/observability/logstash/template/lanverse-logs-template.json` | 执行期间出现的删除，来源未确认；保留现场，未纳入 Agent 实现 |
| `backend/tests/config/creation_test.go` | 验证新服务地址、HTTPS 与独立密钥，默认关闭新流程 |
| `backend/tests/platform/http_decode_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `backend/tests/production/creation/canonical_test.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/tests/production/creation/dispatcher_test.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/tests/production/creation/http_client_test.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/tests/production/creation/http_handler_test.go` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/tests/production/creation/native_agent_test.go` | 真实 Go HTTP 客户端接受与回执查回的本机集成验证 |
| `backend/tests/production/creation/testdata/command.json` | 新创作运行接受、查询、持久投递与恢复，或对应合同和故障测试 |
| `backend/tests/production/script/adapter/gormdb/creation_handoff_test.go` | 运行/Outbox GORM 模型，或真实 PostgreSQL 与 HTTP 交接验收 |
| `backend/tests/production/script/adapter/gormdb/creation_http_journey_test.go` | 运行/Outbox GORM 模型，或真实 PostgreSQL 与 HTTP 交接验收 |
| `backend/tests/production/script/adapter/gormdb/source_span_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `backend/tests/production/script/source_http_handler_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `backend/tests/production/storyboard/adapter/gormdb/intent_receipt_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `backend/tests/production/storyboard/intent_application_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `backend/tests/production/storyboard/intent_http_test.go` | 来源/采纳/权限/恢复及严格 JSON 失败回归 |
| `docker-compose-prod.yml` | 新增显式可选 Creation 地址和独立授权密钥配置 |
| `docker-compose.yml` | 新增显式可选 Creation 地址和独立授权密钥配置 |
| `docs/README.md` | 设计索引与当前实施入口 |
| `docs/acceptance/0013-新架构后端验收记录.md` | 真实验证、限制及逐文件修改记录 |
| `docs/design/0001-AI短剧制作平台完整设计基线.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/0003-系统总体架构.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/0006-领域语言与模块命名规范.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/0010-StoryGraph内容图与DAG创作画布设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/0011-剧本视觉生产工作台与世界观预设设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/0013-创作编排与多媒体画布架构调整设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/1001-前端应用架构.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/1002-前端功能模块设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/1003-多媒体创作画布与运行可视化设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/2001-后端服务架构.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/2002-后端领域模块功能设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/2003-后端语言与运行边界策略.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/2051-通用媒体Provider与Generation执行器设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/2055-Workflow公共HumanGate命令与恢复设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/3001-项目制作圣经生成执行框架设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/3002-本地-Codex-分镜智能体执行框架设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/3003-StoryGraph剧本解析Harness与内置Skill设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/design/3004-AgentHarness专业能力与创作流程设计.md` | 同步已接受架构及文本 MVP 职责；0013 明确可靠交接合同 |
| `docs/plan/0013-新架构后端实施计划.md` | 文本 MVP 分阶段进度、接口与未完成项 |
| `frontend/src/api/schema.d.ts` | 从公开 OpenAPI 生成类型；无页面改动 |
| `frontend/src/api/typings.d.ts` | 从公开 OpenAPI 生成类型；无页面改动 |
| `backend/tests/architecture/semantic_naming_test.go` | 提交前修正 Temporal 官方协议路径误报，并保留项目数字命名拒绝回归 |

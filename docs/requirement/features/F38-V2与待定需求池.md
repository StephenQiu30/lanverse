# F38 V2 与待定需求池

| 项 | 内容 |
| --- | --- |
| 需求 | PRJ-04、PRJ-05、SCR-07、BIB-08、SB-07、AUD-05、AUD-06、DLV-05、CST-05、API-01 |
| 优先级 | V2 / 待定 |
| 状态 | 需求池：只记录目标、关键规则与 MVP 已预留的设计；进入规划时各自拆为独立功能文件并补全验收标准 |

本文件的作用：确保 MVP 的数据与架构不会堵死这些需求（0102 §6 顶层架构要求）。

| 需求 | 目标（用户故事） | 关键规则 | MVP 已预留 | 进入规划前需要 |
| --- | --- | --- | --- | --- |
| PRJ-04 多成员协作与角色权限 | 制片邀请编剧、美术、导演、剪辑加入项目并按角色授权 | 项目级角色；按阶段授权写操作；审计记录角色 | 所有表带 `org_id` / `project_id`；命令层统一鉴权入口；0308 选定 Casbin | 角色与权限矩阵、邀请流程、并发编辑提示 |
| PRJ-05 项目快照与模板 | 把项目某一时刻保存为模板，基于模板创建新项目 | 模板包含设定集、风格、画布布局，不含生成结果（可选） | 版本不可变；`canvas.document_snapshot` | 模板内容边界、复制媒体的成本 |
| SCR-07 AI 编剧与小说改编 | 从创意或小说生成分集剧本 | 生成结果作为剧本版本导入，进入 F07 流程 | 剧本版本模型与导入流程 | 编剧 Skill、长文本改编评测 |
| BIB-08 角色一致性训练 | 为主要角色训练 LoRA 提升一致性 | 训练需授权与费用确认；训练产物作为参考类型 | 参考组合 `ref_type` 可扩展；注册表能力可扩展 | 供应商训练接口调研、费用模型 |
| SB-07 逐帧拉片 | 拆解参考视频的镜头、运镜与音频，生成参考镜头表 | 结果为参考镜头表，可导入为分镜候选 | Skill 机制；媒体库 | 拉片 Skill 与视频理解模型 |
| AUD-05 声音克隆 | 用授权的真人声音克隆音色 | 必须有声音授权（F12 `scope = voice`）；克隆音色仅限本项目 | `consent_record.scope` 已含 voice | 供应商接口、授权材料要求 |
| AUD-06 口型同步 | 对白镜头口型与音频同步 | 暂不考虑（P11）；作为新能力 `video.lipsync` 接入 | 能力与输入用途可扩展（EXT-03） | 产品负责人决定是否启动 |
| DLV-05 导出剪辑工程 | 导出剪映 / Premiere 工程继续精修 | 导出剪辑计划版本 + 素材包 | 剪辑计划为结构化数据 | 剪映草稿格式调研 |
| CST-05 充值、支付与套餐 | 对外阶段按量充值、套餐计费 | 平台余额 + 项目预算两级 | 账本与预留机制；金额以微元存储 | 支付渠道、发票、定价 |
| API-01 开放接口 | 外部工具通过接口创建会话、上传素材、查询任务与结果 | 与界面共用命令层；接口令牌 + 项目授权；付费仍需在界面确认或预授权额度（待定） | 命令层；Agent 会话按 `after_seq` 增量拉取 | 令牌管理、配额、付费确认方式（与 GEN-11 的关系） |

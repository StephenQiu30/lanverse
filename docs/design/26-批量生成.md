# DES-26 批量生成

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-23 批量生成](../requirement/23-批量生成.md)：GEN-03 批量生成 |
| 优先级 | MVP |
| 里程碑 | M3（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 镜头表、故事板、资产定稿、配音、影响清单的批量操作 |
| 基础设计 | DES-04 §9 |
| 依赖功能设计 | [DES-24](24-报价与二次确认.md)、[DES-27](27-任务中心与对账.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-23](../requirement/23-批量生成.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-23 §2 的规则）与验收标准以 REQ-23 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `operation.batch` |
| 接口 | `POST /api/projects/{pid}/quotes`<br>`POST /api/batches/{id}:confirm`<br>`GET /api/batches/{id}`<br>`POST /api/batches/{id}:cancel`<br>`POST /api/batches/{id}:resume`<br>`POST /api/batches/{id}:retry-failed` |
| 工作流与任务 | `BatchWorkflow`、`OperationWorkflow` |
| 领域事件 | `batch.finished.v1` |
| 测试用例 | TC-23-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 处理流程

```text
选择范围 → POST /api/projects/{pid}/quotes（多项）→ 批次报价（逐项费用与错误）→ 剔除错误项 / 不需要的项 → 确认
→ BatchWorkflow：按项目并发上限与供应商配额启动子 OperationWorkflow
→ 汇总：成功 / 失败 / 结果未知 / 已取消 → batch.finished → 一条汇总通知
→ 失败项“重试” = 对失败项重新报价（新批次）
```

## 3. 数据

`operation.batch`（`scope`、`status`、`total/succeeded/failed/unknown_count`、`quote_total_micros`、`workflow_id`）；`operation.operation.batch_id`。批次新增 `paused_reason text`（DES-02 同步）。

## 4. 接口

```http
POST /api/projects/{pid}/quotes         （多项，见 [DES-24](24-报价与二次确认.md)）
POST /api/batches/{id}:confirm          { "exclude_operation_ids": ["…"] }
GET  /api/batches/{id}
200 { "id": "…", "status": "running", "total": 20, "succeeded": 12, "failed": 1, "unknown": 0, "running": 5, "queued": 2,
      "paused_reason": null, "items": [ { "operation_id": "…", "target_label": "3-12", "status": "completed" } ] }
POST /api/batches/{id}:cancel
POST /api/batches/{id}:resume           （暂停后继续）
POST /api/batches/{id}:retry-failed     → 为失败项生成新的批次报价
```

## 5. 异步与工作流

`BatchWorkflow`（`batch/{id}`）：子工作流 `operation/{op_id}`，`ParentClosePolicy = ABANDON`（父工作流异常不影响已提交的子项）；使用信号 `cancel`、`resume`；熔断计数在工作流状态中（DES-04）。

## 6. 事件与实时

`batch.finished.v1` → notify；SSE `batch.updated`。

## 7. 界面

批量工具栏；报价对话框（逐项列表可剔除）；批次进度条（在任务中心与发起页面顶部）；完成后结果摘要与“重试失败项”。

## 8. 权限与审计

制作者；审计 `batch.confirmed`、`batch.cancelled`。

## 9. 非功能要求

CAP-02（≥ 200 并发任务平台不成为瓶颈）；PERF-10（150 项报价 ≤ 5 秒）。

## 10. 测试设计

父子工作流回放测试；熔断暂停；取消与预留释放。

## 11. 待确认

无。

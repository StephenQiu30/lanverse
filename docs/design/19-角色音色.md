# DES-19 角色音色

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-16 角色音色](../requirement/16-角色音色.md)：BIB-04 角色音色 |
| 优先级 | MVP |
| 里程碑 | M3（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 设定条目详情 · 音色 |
| 基础设计 | DES-02 §5.5 |
| 依赖功能设计 | [DES-11](11-模型注册表与价格.md)、[DES-17](17-设定集抽取与造型.md)、[DES-24](24-报价与二次确认.md)、[DES-32](32-台词配音与真人音轨.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-16](../requirement/16-角色音色.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-16 §2 的规则）与验收标准以 REQ-16 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `bible.voice_profile`、`lineage.dependency`、`operation.operation` |
| 接口 | `GET /api/models`<br>`GET /api/characters/{id}/voice`<br>`PUT /api/characters/{id}/voice`<br>`POST /api/projects/{pid}/quotes` |
| 工作流与任务 | `OperationWorkflow` |
| 领域事件 | `bible.voice_changed.v1` |
| 测试用例 | TC-16-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `bible.voice_profile` | `character_id`（唯一）、`model_key`、`voice_id`、`params`、`revision` | 角色音色 |
| `operation.operation` | `target_type = voice_preview` | 试听 |
| `lineage.dependency` | `dialogue_audio` → `voice_profile`（`upstream_version = revision`） | 过期传播 |

## 3. 接口

```http
GET /api/models?capability=audio.tts&project_id=…   → 含 voice 参数枚举：[{ "voice_id": "…", "name": "青年男声·沉稳", "tags": ["male","young"], "sample_url": "…" }]
GET /api/characters/{id}/voice
PUT /api/characters/{id}/voice  { "model_key": "minimax.speech-02", "voice_id": "…", "params": { "speed": 1.0, "emotion": "neutral" }, "expected_revision": 2 }
200 { "revision": 3, "stale_dialogue_audios": 37 }
POST /api/projects/{pid}/quotes { "items": [ { "target_type": "voice_preview", "target_id": "<character_id>", "capability": "audio.tts", "params": { "text": "…" } } ] }
```

## 4. 异步与工作流

试听走 OperationWorkflow（同步型 TTS，秒级完成）。

## 5. 事件与实时

`bible.voice_changed.v1` → lineage、audit。

## 6. 界面

音色选择器（筛选标签、播放官方示例）；“用本角色台词试听”按钮（显示预估费用）；试听结果播放器。

## 7. 权限与审计

制作者；审计 `voice.changed`。

## 8. 非功能要求

PERF-09（试听结果 ≤ 10 秒可播放，受供应商约束）。

## 9. 测试设计

音色变更的过期范围；试听复用。

## 10. 待确认

无。

# DES-22 生成模式与参考组合

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-19 生成模式与参考组合](../requirement/19-生成模式与参考组合.md)：SB-04 生成模式与参考组合 |
| 优先级 | MVP |
| 里程碑 | M3（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 镜头详情 · 参考组合编辑 |
| 基础设计 | DES-02 §5.6；DES-04 §5.2 |
| 依赖功能设计 | [DES-11](11-模型注册表与价格.md)、[DES-18](18-参考定稿与跨集复用.md)、[DES-21](21-分镜生成与镜头编辑.md)、[DES-32](32-台词配音.md)、[DES-33](33-媒体库.md)、[DES-20](20-真人授权.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-19](../requirement/19-生成模式与参考组合.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-19 §2 的规则）与验收标准以 REQ-19 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `lineage.dependency`、`operation.operation_input`、`storyboard.reference_item`、`storyboard.shot_version` |
| 接口 | `POST /api/shots/{id}/versions`<br>`POST /api/shots/{id}:validate-references`<br>`GET /api/projects/{pid}/characters`<br>`GET /api/projects/{pid}/media`<br>`GET /api/episodes/{eid}/dialogue` |
| 工作流与任务 | 无 |
| 领域事件 | 无 |
| 测试用例 | TC-19-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `storyboard.shot_version` | `generation_mode`、`video_model_key`、`video_params` | 模式与模型 |
| `storyboard.reference_item` | `seq_no`、`role`、`ref_type`、`ref_id`、`owner_hint` | 参考组合（属于版本，不可变） |
| `operation.operation_input` | 报价时展开：`role`、`ref_type`、`ref_id`、`ref_version`、`media_asset_id` | 冻结输入 |
| `lineage.dependency` | 镜头版本 → 每个参考项的上游 | 过期 |

## 3. 接口

```http
POST /api/shots/{id}/versions
{ "expected_revision": 5, "patch": { "generation_mode": "omni_reference", "video_model_key": "ark.seedance-2-pro",
  "references": [ { "role": "subject", "ref_type": "reference_version", "ref_id": "…" },
                  { "role": "subject", "ref_type": "reference_version", "ref_id": "…" },
                  { "role": "scene",   "ref_type": "reference_version", "ref_id": "…" },
                  { "role": "camera",  "ref_type": "media_asset",       "ref_id": "…" },
                  { "role": "audio",   "ref_type": "dialogue_audio",    "ref_id": "…" } ] } }
201 { "version_no": 6, "revision": 6, "validation": { "ok": true, "usage": { "image": [3, 9], "video": [1, 3], "audio": [1, 3] } } }

POST /api/shots/{id}:validate-references   { "video_model_key": "…" }    → 不保存，仅校验
200 { "ok": false, "errors": [ { "seq_no": 4, "code": "input_limit_exceeded", "message": "音频参考最多 3 段" },
                               { "seq_no": 2, "code": "input_not_ready", "message": "素材尚未就绪" } ] }
```

参考选择器数据源：`GET /api/projects/{pid}/characters?episode_id=`（锁定版本）、`GET /api/projects/{pid}/media?kind=video&status=ready`、`GET /api/episodes/{eid}/dialogue?shot_id=`。

## 4. 异步与工作流

无；报价时由命令层展开（DES-04 §报价）。

## 5. 事件与实时

随镜头版本事件发布。

## 6. 界面

参考组合面板（REQ-05 §4.4）：顶部上限条；卡片列表（缩略图 / 波形、名称与版本、用途下拉、“有新版本”提示、移除）；添加菜单（设定集 / 媒体库 / 台词音频 / 上传）；拖拽排序。

## 7. 权限与审计

制作者；随镜头版本留痕。

## 8. 非功能要求

校验 P95 ≤ 200 ms（纯内存计算 + 一次批量查询）。

## 9. 测试设计

校验器的所有规则组合（表驱动单元测试）；前后端校验结果一致性（共用测试向量）。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-22-Q1 | 参考项有新的锁定版本时，自动升级还是手动升级？ | 手动：显示“有新版本”，提供“全部升级到最新锁定版本”，升级即生成新的镜头版本。 | M3 开始前 |
| DES-22-Q2 | 前后端共用的校验测试向量放在哪里？ | `backend/internal/storyboard/testdata/reference-limits/*.json`，前端测试按相对路径读取同一批用例。 | M3 开始前 |

需求层面的待确认问题见 [REQ-19](../requirement/19-生成模式与参考组合.md) 的“待确认”。

# DES-MOD-007 资产模块详细设计

- 状态：proposed
- 覆盖需求：SYS-FR-003、SYS-ENT-003；AST-ENT-001 至 006、AST-FR-001 至 008、AST-IF-001 至 007、AST-NFR-001 至 007
- 输入：[资产模块需求](../../requirement/007-资产模块需求.md)

## 1. 聚合与类型边界

assets 提供项目级稳定资产身份与不可变版本。共享流程仅覆盖创建身份、追加版本、当前指针、归档和查询；不同 kind 的业务字段由显式类型负责，不建立接受任意 JSON 的万能 Asset。

| 实体/组成 | 关键字段 | 不变量 |
| --- | --- | --- |
| Asset | id、workspace_id、project_id、kind、name、aliases、tags、status、current_version_id、revision | kind 创建后不可变；当前版本只由命令切换 |
| AssetVersion | id、asset_id、version_no、schema_version、spec、prompt_description、source_type/id、created_by、content_hash | 只追加；spec 先经对应判别模型校验 |
| MediaReference | asset_version_id、media_version_id、purpose、position | 引用固定媒体版本；purpose 受 kind 限制 |
| ConsentReference | asset_version_id、consent_id、usage | 仅作来源引用；有效性由 governance 实时判断 |
| AssetReadiness | status、blocking_reasons、warnings、next_actions、dependency_snapshot | 派生 DTO，不持久化可编辑布尔值 |

支持 kind 固定为 character、location、prop、costume、visual_style、voice。数据库中的 spec 使用 JSONB 保存整体不可变快照，但写入只能来自以下 Pydantic 判别联合，extra=forbid：CharacterSpec、LocationSpec、PropSpec、CostumeSpec、StyleSpec、VoiceSpec。schema_version 只用于显式解析已存在版本，不允许静默接受未知字段。

## 2. 类型化规格

| 类型 | MVP 结构核心 | 专属校验 |
| --- | --- | --- |
| CharacterSpec | identity、appearance、age_impression、temperament、default_costume_version_id、voice_version_id、reference_views | 服装/声音类型匹配；参考视角用途唯一 |
| LocationSpec | spatial_description、time_weather、visual_elements、lighting、environment_refs | 场景参考至少满足 PRD 确认的最小集合 |
| PropSpec | appearance、material、usage_context、holder_character_id、references | holder 可空，存在时属于同 Project |
| CostumeSpec | appearance、material、wearer_character_id、usage_context、references | 不覆盖 Character 身份；穿戴者范围合法 |
| StyleSpec | visual_language、palette、lighting_language、negative_constraints、references | 不作为直接媒体 slot，只影响规格和请求快照 |
| VoiceSpec | source_kind、language、performance_traits、allowed_usage、references | 真人/克隆声音必须有相应 Consent |

类型最小字段和参考数量仍在 PRD 量化。未达到 ready 不阻止保存 draft 版本，但阻止新的 ShotSpec 引用或生产用途。

## 3. 用例与接口

| 用例 | HTTP 形态 | 关键规则 | 需求 |
| --- | --- | --- | --- |
| create/list/get/update_asset | /api/v1/projects/{id}/assets、/assets/{id} | editor/owner 写；PATCH 仅名称、别名、标签 | AST-FR-001、IF-001 |
| append_version | POST /assets/{id}/versions | 完整类型化 spec、固定媒体/授权、expected_current_version | AST-FR-003、IF-002 |
| list/get/set_current | GET /assets/{id}/versions、POST /assets/{id}/current-version | 历史只读；比较更新指针 | AST-FR-004、IF-003 |
| get_readiness | GET /asset-versions/{id}/readiness | 用途化 ready/blockers/next_actions | AST-FR-005、IF-004 |
| list_usage | GET /asset-versions/{id}/usages | storyboards 返回的分页引用摘要 | AST-FR-006、IF-005 |
| preflight_upgrade | POST /asset-versions/{id}/reference-upgrade-preflight | 只读影响，不修改 ShotSpec | AST-FR-006、IF-006 |
| create_or_link_candidate | scripts 内部公开命令 | candidate + decision_key 幂等 | AST-FR-002、IF-007 |
| archive/restore/delete | 明确生命周期命令 | 引用版本仍可读；空白身份才可删 | AST-FR-007/008 |

## 4. 追加版本事务

1. 读取并锁定 Asset，校验 active、Project active、expected_current_version_id 和 kind。
2. 用对应 Spec 模型校验并规范化，拒绝未知字段和跨类型引用。
3. 通过 media 批量解析 MediaVersionRef，通过 governance 检查 Consent 归属和用途；所有引用必须同 Workspace/Project 范围。
4. 计算规范化 content_hash，写 AssetVersion 及关系化引用行。
5. 若请求 set_as_current，比较更新 Asset.current_version_id 与 revision；写审计并提交。

版本号由锁定聚合后分配，asset_id + content_hash 可用于提示内容重复，但不自动合并用户有意创建的版本。外部媒体或授权校验在短事务前完成，事务内再次核对稳定 ID 和关键状态，避免检查后失效。

## 5. 准备度和引用升级

readiness 顺序为：Asset/Version 可用 → kind 必填字段 → 引用媒体 ready → Consent 对目标 usage 有效 → 类型专属规则。结果状态为 draft、ready 或 blocked，blocker 包含 code、field_path、dependency_type/id、summary、next_action；相同版本与依赖状态得到相同结果。

反向使用由 storyboards 的公开查询按 asset_version_id 返回，不建立双方都写的 AssetUsage 表。升级预检确认新旧版本同 Asset、目标 Shot 当前规格、类型兼容、授权和影响字段；实际升级由 storyboards 创建新的 ShotSpecVersion。Asset.current_version_id 切换永不触发批量升级。

## 6. 生命周期、持久化与失败

Asset 为 active ↔ archived。归档后禁止新 ShotSpec 引用和新版本，但已存在 AssetVersion 仍可用于历史重现与交付追踪。只有没有任何 AssetVersion、媒体/授权和候选决议关联的空 Asset 可硬删；AssetVersion 不提供 DELETE。

唯一约束覆盖 asset_versions(asset_id, version_no) 和 media_refs(asset_version_id, purpose, position)。assets(project_id, kind, normalized_name) 只建立查询索引并用于重复提示；名称允许业务确认的同名，故不以数据库唯一约束阻止创建。

跨类型/跨空间引用返回 VALIDATION_FAILED 或 NOT_FOUND；依赖失效返回 readiness blocked，不修改版本；current 指针冲突返回 VERSION_CONFLICT 与最新版本；批量升级存在一个冲突时默认不执行。

## 7. 测试设计

- 单元：六类 Spec 的必填/未知字段、kind 不可变、readiness 证据、版本不可变。
- PostgreSQL 集成：并发追加版本与切换、候选决议幂等、归档/删除阻塞。
- 模块契约：媒体/授权类型与租户校验、反向使用只读、升级预检不产生写入。
- 端到端：剧本候选 → 资产版本 → ShotSpec 固定引用 → 新版本切换后旧镜头不漂移。

公共素材市场、LoRA 训练、跨 Workspace 共享、无限资产树和自动身份合并不进入 MVP。

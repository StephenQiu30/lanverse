# DES-23 故事板视图

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-20 故事板视图](../requirement/20-故事板视图.md)：SB-05 故事板视图 |
| 优先级 | MVP |
| 里程碑 | M3（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 分镜 · 故事板 |
| 基础设计 | DES-02 §5.6 |
| 依赖功能设计 | [DES-21](21-分镜生成与镜头编辑.md)、[DES-25](25-多候选与选定.md)、[DES-26](26-批量生成.md)、[DES-34](34-依赖传播与影响清单.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-20](../requirement/20-故事板视图.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-20 §2 的规则）与验收标准以 REQ-20 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `lineage.stale_mark`、`media.rendition`、`storyboard.shot` |
| 接口 | `GET /api/episodes/{eid}/shots` |
| 工作流与任务 | 无 |
| 领域事件 | `operation.status_changed.v1` |
| 测试用例 | TC-20-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

复用 `storyboard.shot`（`progress`）、`shot_version`、`operation_output` 与 `media.rendition`（封面）、`lineage.stale_mark`。`shot.progress` 由 `storyboard-progress` 消费者根据 `operation.status_changed.v1` 更新（只写进度字段，符合 GEN-10）。

## 3. 接口

复用 `GET /api/episodes/{eid}/shots`，增加 `?view=board` 返回精简字段（封面 URL、进度、状态），单次响应 ≤ 150 条、≤ 200 KB：

```json
{ "items": [ { "id": "…", "shot_no": "3-12", "mode": "omni_reference", "cover_url": "…", "take_preview_url": "…",
               "progress": "awaiting_selection", "candidates": 3, "stale": true, "missing_refs": 0, "revision": 5 } ] }
```

## 4. 异步与工作流

无。

## 5. 事件与实时

SSE `shot.updated`、`operation.updated`、`stale.changed` → 局部刷新对应格子。

## 6. 界面

网格（TanStack Virtual 行虚拟化）；按场分组标题；筛选栏；多选（Shift 连选）后底部操作栏：批量生成关键帧 / 视频、批量改参数（跳镜头表）。

## 7. 权限与审计

制作者；只读视图无审计。

## 8. 非功能要求

PERF-04（150 格首次渲染 ≤ 1 秒、滚动 ≥ 50 fps）；PERF-03（状态更新 ≤ 2 秒）。

## 9. 测试设计

进度状态机单元测试；前端性能录制（150 格）。

## 10. 待确认

无。

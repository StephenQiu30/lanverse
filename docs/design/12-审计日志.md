# DES-12 审计日志

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-09 审计日志](../requirement/09-审计日志.md)：ADM-02 审计日志查询；REQ-01 G6 审计约定 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 管理 · 审计日志 |
| 基础设计 | DES-02 §5.11；DES-07 §10 |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-09](../requirement/09-审计日志.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-09 §2 的规则）与验收标准以 REQ-09 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `audit.audit_log`、`infra.processed_event` |
| 接口 | `GET /api/admin/audit-logs`<br>`GET /api/admin/audit-logs:export` |
| 工作流与任务 | 无 |
| 领域事件 | `audit.recorded.v1` |
| 测试用例 | TC-09-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 处理流程

```text
业务命令事务内写 Outbox 事件 audit.recorded.v1 → Kafka → audit 消费者生成审计记录（按事件 ID 幂等）→ audit.audit_log
管理员 → 审计页面筛选 → 查看前后值摘要 → 跳转对象
```

## 3. 数据

`audit.audit_log`（按月分区）：`org_id`、`project_id`、`actor_id`、`actor_kind`、`action`、`object_type`、`object_id`、`before`、`after`、`request_id`、`trace_id`、`ip`。消费者幂等依赖 `infra.processed_event`。

## 4. 接口

```http
GET /api/admin/audit-logs?from=2026-09-01T00:00:00Z&to=…&actor_id=…&project_id=…&object_type=shot&action=shot.take_selected&limit=50&cursor=…
200 { "items": [ { "id": "…", "create_time": "…", "actor": { "id": "…", "display_name": "Alice" }, "action": "shot.take_selected",
                   "object_type": "shot", "object_id": "…", "object_label": "第 1 集 3-12", "before": { "selected_take_output_id": "…" },
                   "after": { "selected_take_output_id": "…" }, "request_id": "…" } ], "next_cursor": "…" }
GET /api/admin/audit-logs:export?…   → CSV（最多 10 万行）
```

## 5. 异步与工作流

audit 消费者（`backend-relay`）只消费 `audit.recorded.v1`：命令层对 REQ-09 R1 列出的每个动作（登录与账号变更、凭据、注册表与价格、预算、报价确认、取消、人工核对处理、所有选定 / 锁定 / 确认、删除与恢复）在业务事务内写入该事件（通用信封：`action`、`actor`、`object`、`before`、`after`）；登录失败等不产生业务事务的动作在独立小事务中写入。审计覆盖不依赖各领域事件是否标注 audit。分区维护定时任务 `partition-maintain` 每月创建未来 3 个月分区、删除 3 年前分区。

异常与边界：Kafka 暂不可用时事件积压在 `infra.outbox`，relay 恢复后补发，审计延迟写入但不丢失（REL-07）。前置：`infra.outbox` 与 relay 就绪。

## 6. 事件与实时

消费 `audit.recorded.v1`（DES-03 §6.2）。

## 7. 界面

筛选栏（时间范围、操作人、项目、对象类型、动作）+ 虚拟滚动表格 + 详情抽屉（前后值差异）。

## 8. 权限与审计

仅管理员可查询；查询与导出本身也写审计（`audit.exported`）。

## 9. 非功能要求

SEC-10（100% 覆盖 R1 动作，以自动化测试遍历命令验证）；DAT-03；查询 30 天范围 P95 ≤ 1 秒。

## 10. 测试设计

- 命令覆盖测试：遍历 R1 中每个命令，断言产生对应审计记录。
- 消费者幂等：重复投递同一事件只产生一条记录。

## 11. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-12-Q1 | 审计记录保留多久？按月分区后何时删除？ | 保留 3 年（DAT-03），分区维护任务删除 3 年前的分区；删除前导出归档到对象存储冷存储。 | M1 开始前 |
| DES-12-Q2 | 导出 10 万行：同步流式返回还是异步生成文件？ | 同步流式输出 CSV；超过 10 万行返回 `422`，要求缩小查询范围。 | M1 实施中 |
| DES-12-Q3 | `before` / `after` 存全量对象还是只存变更字段？敏感字段如何脱敏？ | 只存变更字段；各模块声明字段脱敏清单（凭据、密码、令牌永不记录，提示词截断到 500 字）。 | M1 开始前 |
| DES-12-Q4 | 审计经 Outbox → Kafka 异步写入，是否接受审计是最终一致的？ | 接受。Outbox 保证不丢；审计页注明“最近 5 秒内的操作可能尚未出现”。 | M1 开始前 |

需求层面的待确认问题见 [REQ-09](../requirement/09-审计日志.md) 的“待确认”。

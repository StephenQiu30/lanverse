# DES-29 关键帧

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-26 关键帧](../requirement/26-关键帧.md)：IMG-01 生成关键帧；IMG-02 上传关键帧 |
| 优先级 | MVP |
| 里程碑 | M3（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 镜头详情、故事板 |
| 基础设计 | DES-04 §5.2 |
| 依赖功能设计 | [DES-18](18-参考定稿与跨集复用.md)、[DES-21](21-分镜生成与镜头编辑.md)、[DES-24](24-报价与二次确认.md)、[DES-25](25-多候选与选定.md)、[DES-33](33-媒体库.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-26](../requirement/26-关键帧.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-26 §2 的规则）与验收标准以 REQ-26 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `operation.operation` |
| 接口 | `POST /api/projects/{pid}/quotes`<br>`POST /api/shots/{id}/frames:upload`<br>`POST /api/shots/{id}:select-frame` |
| 工作流与任务 | `OperationWorkflow` |
| 领域事件 | 无 |
| 测试用例 | TC-26-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

`operation.operation`（`target_type = shot_frame`、`capability = image.generate`）；`operation_output`；`storyboard.shot.selected_frame_output_id`；上传关键帧：创建 `origin = upload` 的 Operation（`capability = image.upload`、`mode = upload`、`status = completed`、费用 0，模型版本与区域为空，受 `ck_operation_upload` 约束）及其 `operation_output`，与生成结果统一为候选。

## 3. 接口

```http
POST /api/projects/{pid}/quotes { "items": [ { "target_type": "shot_frame", "target_id": "<shot_id>", "capability": "image.generate", "output_count": 4 } ] }
POST /api/shots/{id}/frames:upload   { "media_asset_id": "…", "crop": { "x": 0, "y": 120, "w": 1080, "h": 1920 } }
201 { "output_id": "…", "operation_id": "…" }
POST /api/shots/{id}:select-frame    { "output_id": "…", "expected_revision": 7 }
```

## 4. 异步与工作流

生成：OperationWorkflow（图片同步或异步视供应商）；上传裁剪：`media` 队列 `CropImage` Activity。

## 5. 事件与实时

同 REQ-22。

## 6. 界面

镜头详情“关键帧”标签页：生成按钮（数量、模型、参数）、上传区（裁剪框）、候选对比、选定。

## 7. 权限与审计

制作者；审计 `shot.frame_selected`。

## 8. 非功能要求

PERF-09。

## 9. 测试设计

输入展开正确性；上传裁剪；审核拒绝路径。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-29-Q1 | 上传关键帧的裁剪在服务端还是前端执行？ | 服务端 `media.CropImage` 执行，保证画幅与分辨率精确；前端只提交裁剪框。 | M3 开始前 |
| DES-29-Q2 | 部分图片模型输出分辨率不足 1080p 短边，怎么办？ | 注册表 `limits` 排除不满足的模型或分辨率；MVP 不做超分。 | P0 结束时 |

需求层面的待确认问题见 [REQ-26](../requirement/26-关键帧.md) 的“待确认”。

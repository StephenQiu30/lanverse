# DES-24 报价与二次确认

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-21 报价与二次确认](../requirement/21-报价与二次确认.md)：GEN-01 生成前报价；GEN-11 付费生成二次确认；CST-03 预留与结算 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 全局“报价确认”组件（所有生成入口） |
| 基础设计 | DES-04 §5–7 |
| 依赖功能设计 | [DES-11](11-模型注册表与价格.md)、[DES-13](13-项目管理.md)、[DES-14](14-项目预算.md)、[DES-28](28-结果复用与写入边界.md)、[DES-35](35-成本账本与报表.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-21](../requirement/21-报价与二次确认.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-21 §2 的规则）与验收标准以 REQ-21 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `billing.budget`、`billing.ledger_entry`、`billing.reservation`、`operation.batch`、`operation.operation`、`operation.operation_input` |
| 接口 | `POST /api/projects/{pid}/quotes`<br>`POST /api/batches/{id}:confirm`<br>`POST /api/operations/{id}:confirm` |
| 工作流与任务 | `OperationWorkflow`、`BatchWorkflow`、`flow.SettleOperation`、`QuoteExpirySchedule` |
| 领域事件 | `operation.confirmed.v1` |
| 测试用例 | TC-21-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 处理流程

```text
入口（按钮 / 批量 / 画布 / Agent 草稿）→ POST /api/projects/{pid}/quotes（1..n 项）
  对每一项：
    1. 解析目标对象的当前版本 → 构造输入（镜头：展开参考组合；台词：文本 + 音色；槽位：描述 + 风格）
    2. 选择模型：显式指定 > 镜头 / 项目默认；校验模型启用、凭据可用、项目境外设置
    3. 校验：模式、输入数量 / 类型 / 时长、参数（param_schema）、就绪、授权、前置条件（如图生视频需关键帧）
    4. 计算 input_hash；命中已完成的相同输入且未要求强制重做 → reused，报价 0
    5. 计价：按生效价格规则 × 数量 × 参数系数 → quote_micros
    6. 创建 Operation(status = quoted, quote_expires_at = now + 15 min)
  汇总：合计、可用余额、是否可确认
→ 用户在确认对话框中点击“确认”（可剔除个别项）
→ POST /api/operations/{id}:confirm 或 /batches/{id}:confirm
  一个事务：检查未过期 → 锁预算行 → 余额足够 → 创建 reservation(held) → budget.reserved += 报价 → Operation = confirmed
           → 生成 provider_request_key → 写 ledger(reserve) → 写 Outbox(operation.confirmed) → 审计
  事务后：启动工作流 operation/{id}（ID 重复则忽略）
→ 终态（DES-04）：settle(实际费用) + release(预留 − 实际)；失败 / 取消按供应商实际计费
```

## 3. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `operation.operation` | `status`、`quote_micros`、`quote_detail`、`quote_expires_at`、`input_hash`、`reused_from_id`、`force_regenerate`、`model_profile_version_id`、`price_rule_version_id`、`target_version_no`、`provider_request_key`、`confirmed_at/by`、`reservation_id`、`settled_micros` | 报价、确认、结算全链路 |
| `operation.operation_input` | 冻结输入 | 执行与复现 |
| `operation.batch` | `quote_total_micros`、`status` | 批量报价与确认 |
| `billing.budget`、`billing.reservation`、`billing.ledger_entry` | 见 [DES-14](14-项目预算.md)、[DES-35](35-成本账本与报表.md) | 预留与结算 |

`quote_detail` 示例：`{ "unit": "per_second", "quantity": 10, "unit_price_micros": 1000000, "outputs": 2, "multipliers": { "resolution_1080p": 1.0 }, "region": "domestic" }`。

## 4. 接口

```http
POST /api/projects/{pid}/quotes
{ "items": [
    { "target_type": "shot_take", "target_id": "<shot_id>", "capability": "video.generate", "output_count": 2 },
    { "target_type": "shot_take", "target_id": "<shot_id>", "capability": "video.generate", "output_count": 2, "force_regenerate": false } ] }
200 { "batch_id": "…", "expires_at": "…",
      "items": [ { "operation_id": "…", "target_label": "3-12", "model_key": "ark.seedance-2-pro", "mode": "omni_reference",
                   "quote_micros": 20000000, "quote_display": "¥20.00", "reused": false, "region": "domestic", "errors": [] },
                 { "operation_id": null, "target_label": "3-13", "errors": [ { "code": "input_not_ready", "message": "素材尚未就绪" } ] } ],
      "total_micros": 20000000, "available_micros": 308000000, "confirmable": true }

POST /api/batches/{id}:confirm      { "exclude_operation_ids": [] }
POST /api/operations/{id}:confirm   {}
200 { "confirmed": ["…"], "reserved_micros": 20000000, "available_micros": 288000000 }
422 quote_expired { "meta": { "reasons": ["target_version_changed"] } } | budget_insufficient | model_unavailable
403 forbidden（非用户会话）
```

## 5. 异步与工作流

确认事务提交后启动 `OperationWorkflow`（单项）或 `BatchWorkflow`（批量）；结算由工作流最后一步 `flow.SettleOperation` Activity 执行（DES-04）。定时任务 `QuoteExpirySchedule` 每 5 分钟把过期报价置为 `expired`。

确认后工作流启动失败（Temporal 暂不可用）：Operation 保持 `confirmed`，由 relay 的“待启动扫描”在恢复后启动（以 Outbox 事件驱动）；工作流 ID `operation/{id}` 保证不重复启动。

## 6. 事件与实时

`operation.confirmed.v1`（audit、billing-report）；SSE `budget.updated`、`operation.updated`。

## 7. 界面

`QuoteConfirmDialog` 组件（REQ-05 §4.1）：逐项费用（可展开）、合计、剩余预算、倒计时、区域提示、错误项列表与“剔除”操作；确认按钮不响应回车；按钮文案“生成（约 ¥X）”由 `useQuote` 钩子统一计算。

## 8. 权限与审计

制作者与管理员。审计：`operation.confirmed`（金额、模型、区域、确认人、来源入口）、`batch.confirmed`。

## 9. 非功能要求

PERF-10（单项报价 P95 ≤ 1 秒，150 项 ≤ 5 秒）；REL-02（重复计费 0）；REL-04（幂等）；REL-08；COST-02（报价偏差 ≤ 5%）。

## 10. 测试设计

- 单元：计价（各计价单位、系数）、`input_hash` 规范化、报价失效判断。
- 集成：确认事务（预算约束、并发）；工作流启动失败恢复；复用路径。
- 故障注入：确认后立即重启 API 与 Worker，任务仍被执行一次。

## 11. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-24-Q1 | 大量未确认、已过期的报价记录如何清理？ | `expired` 报价保留 30 天后物理删除（不涉及账本）；已确认的记录永久保留。 | M1 实施中 |
| DES-24-Q2 | 批量确认时部分项失效（目标版本变化），整体失败还是部分确认？ | 部分确认：失效项置为 `expired` 并返回原因，其余照常确认；前端提示哪些项需要重新报价。 | M1 开始前 |
| DES-24-Q3 | 按 token 计费的 LLM 报价，输出上限从哪里来？ | 每个 Skill 在注册表声明 `max_output_tokens`，报价与预留按上限计算，结算按实际用量。 | M1 开始前 |
| DES-24-Q4 | 报价有效期 15 分钟是否需要可配置？ | 做成环境配置，默认 15 分钟；界面按响应中的 `expires_at` 倒计时。 | M1 实施中 |

需求层面的待确认问题见 [REQ-21](../requirement/21-报价与二次确认.md) 的“待确认”。

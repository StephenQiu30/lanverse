# DES-34 依赖传播与影响清单

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-31 依赖传播与影响清单](../requirement/31-依赖传播与影响清单.md)：LIN-01 依赖传播；LIN-02 影响范围与批量重做 |
| 优先级 | MVP |
| 里程碑 | M2（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 影响清单；所有对象上的“过期”标记 |
| 基础设计 | DES-02 §4.4 |
| 依赖功能设计 | [DES-16](16-逐集结构解析.md)、[DES-18](18-参考定稿与跨集复用.md)、[DES-19](19-角色音色.md)、[DES-21](21-分镜生成与镜头编辑.md)、[DES-22](22-生成模式与参考组合.md)、[DES-25](25-多候选与选定.md)、[DES-26](26-批量生成.md)、[DES-32](32-台词配音.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-31](../requirement/31-依赖传播与影响清单.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-31 §2 的规则）与验收标准以 REQ-31 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `lineage.dependency`、`lineage.stale_mark` |
| 接口 | `GET /api/projects/{pid}/impact`<br>`POST /api/projects/{pid}/impact:quote`<br>`POST /api/projects/{pid}/impact:dismiss` |
| 工作流与任务 | 无 |
| 领域事件 | `lineage.stale_marked.v1` |
| 测试用例 | TC-31-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

`lineage.dependency`（下游 → 上游键与版本）、`lineage.stale_mark`（对象、原因、开放 / 清除）；见 [DES-02 §4.4](../design/02-领域与数据模型.md#44-依赖与过期)。

## 3. 接口

```http
GET /api/projects/{pid}/impact?episode_id=&cause_type=
200 { "groups": [ { "episode_seq": 1, "scenes": [ { "scene_heading": "1-3 医院走廊", "items": [
        { "object_type": "shot_take", "object_id": "…", "label": "3-12 片段", "progress": "selected",
          "causes": [ { "type": "reference_version", "label": "李明 / 日常装 v1 → v2", "since": "…" } ],
          "redo": { "capability": "video.generate", "estimate_micros": 20000000 } } ] } ] } ],
      "total_objects": 20, "total_estimate_micros": 400000000 }

POST /api/projects/{pid}/impact:quote   { "objects": [ { "object_type": "shot_take", "object_id": "…" } ] }  → 批量报价（REQ-21、REQ-23）
POST /api/projects/{pid}/impact:dismiss { "stale_mark_ids": ["…"], "note": "保留旧版本" }   → 人工确认“不需要重做”，清除标记并写审计
```

`impact:dismiss` 为新增能力：用户确认某些过期对象保持现状（如只是改了造型描述里的错字），避免永久显示过期。

## 4. 异步与工作流

`lineage` 消费者（`backend-relay`）：接收上游事件 → 递归 CTE 查询下游 → 批量写 `stale_mark` → 发 `lineage.stale_marked.v1`；清除逻辑在下游对象的命令事务中同步执行。

## 5. 事件与实时

`lineage.stale_marked.v1` → realtime；SSE `stale.changed`。

## 6. 界面

各列表中的“过期”徽标（悬停显示原因）；影响清单页（分组、勾选、合计预估、“为选中项报价”“标记为无需重做”）；改锁 / 改选前的影响确认框。

## 7. 权限与审计

制作者；审计 `stale.dismissed`。

## 8. 非功能要求

REL-07（传播 P95 ≤ 5 秒）；影响清单 2,000 条 ≤ 2 秒。

## 9. 测试设计

依赖表（§2.1）逐行的集成测试；递归深度；大规模传播性能；清除条件。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-34-Q1 | 递归查询下游的深度上限与环检测？ | 深度上限 8；写入依赖时检查不产生环，出现环时拒绝写入并告警。 | M2 开始前 |
| DES-34-Q2 | 过期传播是异步的，改锁后立即打开影响清单可能不完整，如何提示？ | 改锁响应返回同步预估数；影响清单在传播完成前显示“正在计算”，完成后通过 SSE 刷新。 | M2 实施中 |
| DES-34-Q3 | 标记“无需重做”后，上游再次变化怎么办？ | 新的上游变化重新产生过期标记，不受之前的忽略影响。 | M2 开始前 |

需求层面的待确认问题见 [REQ-31](../requirement/31-依赖传播与影响清单.md) 的“待确认”。

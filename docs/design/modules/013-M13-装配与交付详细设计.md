# M13 装配与交付详细设计

> Design ID：DES-M13
> Requirement：[REQ-DLV](../../requirement/013-M13-装配与交付需求.md)
> 状态：proposed

## 1. 设计结论与范围

M13 提供平台闭环所需的基础音视频/字幕装配和可复现交付，不替代专业 NLE。`AssemblyRevision` 是版本化编辑/批准事实，`DeliveryBuild/RenderRun` 是可失败、取消、恢复和重跑的耐久过程，`DeliverySnapshot/Manifest` 是全部必需输出验证成功后在 PostgreSQL 原子发布的不可变结果；三类事实不能合并。所有媒体字节由 M09 以不可变 Artifact 和精确 MinIO Location 管理，M13 不保存 bucket/key、不调用 MinIO SDK，也不把多对象 copy 误称为原子事务。

M05 `AnimaticRevision` 是按镜头顺序、帧和音频意图形成的低成本规划预演，不是 M13 Assembly、审阅批准或正式交付。M13 可以从明确的 M05 ShotOrder/Animatic 引用**初始化新的 AssemblyRevision**，但不接管、不修改、不升级 M05 Animatic。切片 A 的 Animatic/Storyboard Export 也不宣称是 M13 正式 DeliverySnapshot；正式装配与交付在切片 E 建立。

### 1.1 范围内

- 从显式 ShotOrder 与 Selection 创建基础时间线版本；
- 镜头顺序、入出点、空隙、必要黑场、简单转场和画幅适配；
- 对白、旁白、环境声、音效、音乐的轨道放置与基础音量/淡入淡出/静音；
- 基础字幕的手工创建、导入、校对、样式和导出；
- 精确时间基、交付预检、冻结 Build、耐久渲染、原子 Snapshot、授权下载和有序镜头素材包。
- 对正式交付和外部下载使用逐请求鉴权、可即时撤销的下载网关；不向终端返回可绕过 Grant 的 MinIO 预签名 GET。

### 1.2 非目标

- 高级调色、复杂合成/跟踪/VFX、多机位、专业混音、任意插件或多人 NLE 工程合并；
- 由 M13 直接调用模型或 Provider 生成语音、音乐、音效、字幕或媒体；
- 把 M05 Animatic、M12 ReviewPackage、M10 QualityRiskAcceptance 或 M14 PolicyException 互相替代；
- 在 Build 成功前创建“preparing Snapshot”，或在 Snapshot 发布后补写文件。

## 2. 事实所有权与数据模型

| 对象 | 关键字段 | 不变量 |
| --- | --- | --- |
| `Assembly` / `AssemblyRevision` | project/unit、source ShotOrder/Animatic refs、timebase、raster/color/audio spec、revision/status/hash | 每次编辑形成新 revision；批准版本不可原地改 |
| `VideoTrackItem` | shot/selection/artifact version、source in/out ticks、timeline start/duration、fit、transition | 引用明确 M09 artifact version；时间范围合法且不使用浮点秒 |
| `AudioTrackItem` | purpose、selection/artifact version、source in/out、timeline ticks、gain/fade/mute | 只绑定可播放且已探测的 M09 音频版本；AudioCue 仅作意图来源 |
| `CaptionRevision` / `CaptionCue` | source kind/ref、language、integer start/end ticks、text、style preset、status | cue 不重叠规则由格式/策略定义；导入后版本冻结 |
| `AudioMixRevision` | tracks、item refs、loudness target、status/hash | 仅基础轨道参数；不保存外部 DAW 工程 |
| `ExportPresetRevision` | container/codec/raster/frame rate/color/audio/subtitle/required outputs | 发布后冻结；有序镜头包是一个显式预设 |
| `DeliveryBuild` / `DeliveryInput` | assembly/preset、review/quality/rights refs、input manifest/hash、status | start 前冻结所有输入；运行中项目变化不改 Build |
| `RenderRun` | build、run number、render engine version、plan hash、stage、diagnostics、status | 每次重跑新记录；不能覆盖历史成功/失败 Run |
| `DeliveryOutput` | run、purpose、M09 immutable artifact id、content hash、probe/spec、required、validation | 输出先进入 Build/Run staging；M09 冻结精确 MinIO object version、校验 SHA-256 并发布 Artifact 前不可 valid/进入 Snapshot |
| `DeliverySnapshot` / `DeliveryManifest` | build unique、input/output immutable Artifact refs/hash、spec、approval/rights evidence、created_by | 单 Build 至多一个；与完整 Manifest/规范化 Artifact 引用同事务发布；不保存 MinIO latest key |
| `DeliveryAccessGrant` | snapshot、principal、actions/purpose、expiry、M14 evaluation、status/revision | 撤销访问不修改/删除 Snapshot；新请求必须读取 current grant revision |
| `DeliveryDownloadTicket` | grant、opaque token hash、expiry、max uses/bytes、status | 只供下载网关；URL 不含 bucket/key/MinIO 签名或长期凭据 |
| `DeliveryDownloadReceipt` | ticket/grant/snapshot、artifact/hash、actor、range/bytes、started/ended、result、trace | 只追加；记录逻辑引用和安全摘要，不记录 token、MinIO URL 或对象键 |

项目时间采用 rational `timebase_num/timebase_den` 与 integer ticks；音频另保存 sample rate/timebase。UI 可展示浮点或 SMPTE，但命令、校验和 Manifest 只使用整数 tick 与显式时间基。

## 3. 音频、字幕来源与 Provider Gate

### 3.1 允许的来源

| 内容 | 来源 | 进入 M13 的方式 | M13 所有权 |
| --- | --- | --- | --- |
| 对白/旁白/环境声/音效/音乐 | 用户上传、已有素材、Gate 后由生成流程产出的 M09 MediaArtifact/Selection | M09 完成上传/摄取、探测、权利检查和版本化；M13 引用 Selection/Artifact version | 仅 AudioTrackItem/AudioMixRevision，不拥有源媒体 |
| 字幕 | M13 手工创建 | `CreateCaptionRevision`，保存 cues/style | 拥有 CaptionRevision |
| 字幕文件 | 用户导入的 SRT/VTT 等受支持格式 | M09 保存源文件；M13 `ImportCaptionRevision` 解析为冻结 cues 并保留 source hash | 拥有解析后的 CaptionRevision，不改源文件 |
| 转写/翻译/生成字幕 | Provider Gate 后的 M07 Plan → M08 执行 → M09 Artifact | 用户显式选择结果后，由 M13 导入/校对为新 CaptionRevision | 不拥有模型调用或 Candidate；只拥有采用后的字幕版本 |
| M05 AudioCueRevision | 镜头/叙事中的声音意图、文案和时序建议 | 初始化 Assembly 时作为 track intent/placeholder 来源 | 不视为可播放音频，不可直接交付 |

切片 E 的必达能力是**导入/已有音频 + 手工/导入字幕**。没有通过 Provider Gate 时，界面不得声称支持真实语音、音乐、音效或字幕生成/转写；可以显示占位和“需要导入/生成能力尚未准入”。

### 3.2 Provider Gate

每一种真实音频或字幕外部能力都必须逐 `media kind + purpose + provider` 通过下列 Gate，不能用已有图像/视频 Provider 的通过结果自动继承：

1. **M07 Capability Gate**：存在已批准且可路由的 `CapabilityRevision`，输入/输出 schema、reference limits、估计单位和 purpose 明确；
2. **M08 Execution Gate**：ProviderConnection/credential/adapter 已就绪，提交幂等、回调验签、unknown 对账、取消语义、下载与健康检查通过契约测试；
3. **M14 Policy Gate**：地域、保留、训练使用、内容/权利和实际外发字段与当前策略兼容；
4. **M11 Usage Gate**：估计和 Provider 实际计量能映射到明确 unit，硬上限可在提交前原子门禁；
5. **M09/M10 Media Gate**：输出可安全摄取/探测/版本化，且适用技术质量检查可执行。

任一 Gate 未通过时，M13 只消费已授权导入/已有素材和手工字幕；M13 不提供绕过 M07/M08 的 Provider SDK 调用。

## 4. 功能分解

| 能力 | 命令或查询 | 输入 | 输出 | 业务规则 | 失败恢复 | 切片 |
| --- | --- | --- | --- | --- | --- | --- |
| 从镜头选择建立装配 | `CreateAssemblyFromSelections`；`GetAssemblyRevision` | explicit ShotOrder revision、每 shot Selection/artifact version、可选 Animatic ref、project spec | draft AssemblyRevision、source manifest/hash | 每个引用冻结；Animatic 仅初始化提示；缺主选可建 draft 但不得批准/Build | 引用冲突返回缺失/变化列表；刷新选择后新建 revision | E |
| 编辑基础画面时间线 | `CreateAssemblyRevision`；`CompareAssemblyRevisions` | base revision、track item edits、expected head revision | 新 revision、规范化时间线、差异 | 支持顺序/入出点/空隙/简单转场/画幅适配；每次编辑新 revision | 并发 head 冲突返回三方差异；用户基于新 head 重放编辑 | E |
| 配置精确项目规格 | `SetAssemblyProjectSpec`；`GetAssemblyTiming` | rational frame rate、raster、color、audio sample/timebase | 新 AssemblyRevision、换算/阻塞报告 | 创建后固定；禁止浮点秒作为权威；改规格必须新 revision 并重检 | 不可精确映射的 cue/item 阻止批准，用户修正后新 revision | E |
| 放置和混合基础音频 | `CreateAudioMixRevision`；`GetAudioMixRevision` | M09 audio Selection/Artifact versions、purpose、ticks、gain/fade/mute、loudness target | AudioMixRevision、缺素材/越界诊断 | 只消费可播放已探测素材；AudioCue 不是音频；功能限基础混音 | 素材不可用保留 draft/placeholder；替换引用后新 mix revision | E |
| 创建/导入/校对字幕 | `CreateCaptionRevision`、`ImportCaptionRevision`、`ReviseCaptionCues`；`GetCaptionRevision` | manual cues 或 M09 subtitle artifact、language、timebase、style、expected revision | 解析报告、CaptionRevision | 保存 integer ticks；导入保留源 hash；Provider 结果必须先过 Gate/M09 | 非法格式/时间阻止发布并逐 cue 报错；修复后新 revision | E |
| 批准装配 | `ApproveAssemblyRevision` | assembly、audio mix、caption refs、expected revision、actor | approved revision 或 blockers | 引用必须冻结且技术上可播放；批准不替代 M12/M14/M10 门禁 | 项目已有新版本时允许批准旧 revision 但明确提示；修改需新 revision | E |
| 交付预检 | `RunDeliveryPreflight`；`GetDeliveryPreflight` | assembly、preset、intended use/region、candidate review/quality/policy refs | 按对象/规则分类的 pass/warn/block、input preview hash | 主选、媒体、时间/规格、M10、M14、M12、输出位置/保留逐项检查；例外不互替 | 依赖不可用时高风险项 block；修复/重评后以新 input hash 重跑 | E |
| 冻结并启动 Build | `CreateDeliveryBuild`、`StartDeliveryBuild` | passed preflight/input hash、assembly/preset、idempotency key | frozen manifest、queued Build、首个 RenderRun | 预检与输入 hash 必须仍一致；冻结后当前项目变化不进入运行 | 版本冲突不建 Build；重新预检后新建；重复 start 回读原 Run | E |
| 耐久渲染与重跑 | `CancelDeliveryBuild`、`RetryDeliveryBuild`；`GetRenderRun` | build/run、failed stages/outputs、idempotency key | 新 RenderRun、stage/diagnostics/outputs | 每次重跑独立；只复用内容寻址且 engine/plan hash 相同的中间物 | 中断从确定 stage 恢复或新 Run；unknown 不重复提交非幂等步骤 | E |
| 验证多种输出 | `ValidateDeliveryOutputs` | run、required output set、artifacts/probe/hash | per-output valid/invalid/missing，Build partial/failed/succeeded candidate | 主文件、字幕、音频、缩略图、交换清单按 preset 判断 required | 缺附属输出为 partial 且无 Snapshot；只重做缺失项的新 Run | E |
| 原子发布 Snapshot | `FinalizeDeliverySnapshot`；`GetDeliverySnapshot` | succeeded Build、validated outputs、manifest hash、idempotency key | immutable Snapshot/Manifest | 所有 required 输出存在且 hash/spec 通过；同 Build/manifest 唯一 | 重复 finalize 回读同一 Snapshot；事务失败不暴露半成品 | E |
| 下载、撤销与历史 | `GrantDeliveryAccess`、`RevokeDeliveryAccess`；`ListDeliverySnapshots`、`CreateDeliveryDownloadTicket`、`StreamDeliveryDownload` | snapshot、principal、purpose、expiry、M14 evaluation、可选 range | opaque gateway ticket/stream、grant/status、历史列表 | 网关每次请求/Range 重查主体、Grant revision、M14 与精确 Artifact hash；不 302 到 MinIO；撤销提交后新请求立即拒绝且不改 Snapshot/历史 | 失败可重新授权；M14 阻止时显示确切依据；活动流按撤销信号在配置的短 abort interval 内中止 | E；外部下载按 F 策略 |
| 有序镜头素材包 | `CreateOrderedShotPackageBuild`；`GetShotPackageManifest` | ShotOrder、Selections、preset、命名规则 | ordered files、manifest、hash、Snapshot | 仍走 preflight/build/validate/finalize；文件集必须与 Manifest 一致 | 单文件失败为 partial；新 Run 补齐后再原子发布 | E |

## 5. 模块边界与跨模块契约

### 5.1 M05 Animatic 边界

- M05 拥有 Shot、ShotOrderRevision、AnimaticRevision、AudioCueRevision；M13 只读稳定引用。
- M05 Animatic 是规划预览，可以使用 fixture/低成本媒体；M13 Assembly 是正式编辑与交付输入，必须引用 M09 可验证媒体版本。
- `CreateAssemblyFromSelections` 可记录 `source_animatic_revision_id` 以解释初始化来源，但所有 Video/Audio TrackItem 都重新解析为显式 Selection/Artifact；之后互不联动。
- M13 的修改不反写 Animatic；ShotOrder/Animatic 更新只提示“有新基线”，用户显式创建新的 AssemblyRevision。

### 5.2 跨模块端口

| 模块 | M13 消费/调用 | M13 提供 | 边界约束 |
| --- | --- | --- | --- |
| M03/M05 | 项目时间规格、ShotOrder/Animatic/AudioCue 稳定版本 | Assembly source refs | 不写镜头与 Animatic；AudioCue 只作 intent |
| M07/M08 | Gate 后音频/字幕生成计划与执行状态（经 M09 成果进入） | 无直接 Provider 调用 | M13 不拥有 Capability、ProviderConnection、Job/Attempt |
| M09 | Selection、不可变 MediaArtifact、精确版本可用性、register rendered media、受限读取端口 | Rendered output lineage、Snapshot refs | 所有可播放输入/输出先进入 M09；M13 不保存或选择 MinIO Location，不直接写对象存储事实 |
| M10 | QualityReport/QualityRiskAcceptance 有效性、交付技术检查 | build/output refs 供检查 | 质量接受不能替代权利/审阅；M13 不修改质量事实 |
| M11 | Build/Run 估计与实际用量；每交付分钟查询 | Snapshot duration/build lineage | M11 只做资源治理，不决定交付成功 |
| M12 | ReviewPackage/Decision | Assembly/display proxy refs、Snapshot refs | 预检读取明确批准包；新 Assembly 不继承旧批准 |
| M14 | PolicyEvaluation/Exception、retention/download/share decisions、Grant recheck | intended action、全部采用输入与输出摘要、下载 receipt | M14 拥有策略；M13 网关执行 block/allow，不自判权利；撤销不依赖异步事件才能生效 |
| 任务/媒体 Worker | 耐久任务、隔离工作目录、Build/Run-scoped ObjectStoragePort capability | Render plan、stage checkpoint | M13 拥有 Build/RenderRun 业务状态；Worker 无 MinIO root/通用 ListBucket，外部 Provider execution 仍属 M08 |

### 5.3 关键契约载荷

- `AssemblySourceManifest`：ShotOrder/Selection/Artifact/Animatic refs + versions/hashes，项目 timebase/spec。
- `DeliveryPreflightRequest`：assembly/preset refs、intended use/region、M10/M12/M14 candidate evidence refs、input preview hash。
- `RenderStageCheckpoint`：build/run、engine version、plan hash、stage、input/output content hashes；不包含 Provider 凭据。
- `DeliverySnapshotCreated`：snapshot/build/manifest refs、duration ticks/timebase、required output IDs/hash、approval/rights summary refs。
- `DeliveryDownloadTicket`：opaque ticket、grant/snapshot logical refs、actions、expiry 和限制摘要；终端契约不包含 MinIO bucket/key/version/signature。

渲染 Worker 使用限权临时目录和 M09 分配的 Build/Run 专属 `delivery-staging` capability。每个输出经 `prepare → write → freeze exact object version → M09 stream SHA-256/probe → register Artifact` 后才成为 valid；数据库事务只发布已由 M09 登记且验证成功的 output refs。跨模块事件通过 Outbox/Inbox 至少一次投递并幂等消费。

下载网关由 Go SVC-01 的鉴权控制面与受控反向代理数据面组成，不新增第七类业务进程角色或业务微服务。Go Application Service 校验 opaque ticket、M01 scope、Grant current revision、M14 evaluation 和 Artifact/hash，只生成绑定精确 version、Range、短时效与 trace 的一次性内部交接结果；反向代理使用只读最小权限读取 MinIO 并承担正文传输、Range、限速和 Content-Disposition，Go API 不缓冲媒体正文。网关不得向终端返回 3xx MinIO URL；撤销事务直接使 Grant revision 失效，事件只用于终止既有活动流和清理 ticket，不承担新请求授权事实。

## 6. 关键状态、并发与幂等

### 6.1 状态机

| 对象 | 合法状态与转换 | 不变量 |
| --- | --- | --- |
| AssemblyRevision | `draft → approved \| superseded`；approved 可因新 revision 关系显示 `superseded` | approved 内容冻结；不能回 draft |
| DeliveryBuild | `draft → queued → rendering → validating → succeeded`；运行中可 `partial \| failed \| cancelled`；partial/failed 可通过新 Run 回到 rendering | succeeded 仅表示 Build 具备完整验证结果；Snapshot 另建 |
| RenderRun | `queued → preparing → rendering → validating → succeeded \| partial \| failed \| cancelled \| unknown` | 每次 retry 新 Run；旧状态不覆盖 |
| DeliveryOutput | `staged → validating → valid \| invalid \| quarantined` | 只有 valid 且 required set 完整可发布 |
| DeliverySnapshot | 创建即 `published`；访问另由 Grant 控制 | 无 preparing/partial；不可修改、补写或覆盖 |
| DeliveryAccessGrant | `active → revoked \| expired` | revocation/expiry 不恢复；重新授权创建新 Grant/revision |
| DeliveryDownloadTicket | `active → consumed \| revoked \| expired` | 每次使用绑定 current Grant；Grant 失效时即使 ticket 状态投影滞后也拒绝 |

### 6.2 并发与一致性

1. Assembly 更新用 `(assembly_id, expected_head_revision)` compare-and-swap；冲突产生差异，不覆盖另一作者版本。
2. `StartDeliveryBuild` 锁 Build，比较 preflight input hash 与当前解析 hash；不一致则拒绝，绝不偷偷换输入。
3. 同一 Build 同时至多一个 active RenderRun；唯一键 `(build_id, active_marker)`；retry 为新 run number。
4. M09 分配的 staging key 包含 Build/Run/purpose/output identity 且禁止覆盖；旧 Run 无法写新 Run 的 capability。发布后 Snapshot 只引用不可变 Artifact/hash，不依赖 staging key 的“最新版本”。
5. `FinalizeDeliverySnapshot` 在事务内锁 Build，验证 required outputs，并以 `UNIQUE(build_id)`、`UNIQUE(manifest_hash, build_id)` 保证重复 finalize 回读同一结果。
6. Snapshot Manifest 与 required output refs 同事务写入；MinIO 多对象写入/复制在事务前逐项完成并由 M09 验证，Snapshot 原子性仅指数据库可见性，不宣称对象存储多对象原子提交。
7. `RevokeDeliveryAccess` 以 expected Grant revision CAS 提交；下载网关每次请求使用权威读取或可证明不越过 revocation revision 的强一致缓存。撤销完成后的新请求/Range 返回 403，不能依赖已签发 MinIO URL 的到期时间。

### 6.3 幂等键

| 写入 | 幂等键 | 重复结果 |
| --- | --- | --- |
| CreateAssemblyFromSelections | project + ShotOrder revision + source manifest hash + request key | 回读原 draft；hash 不同冲突 |
| StartDeliveryBuild | Build + frozen input hash | 回读现有 active/first RenderRun |
| RetryDeliveryBuild | Build + failed Run + retry request key | 回读已创建的新 RenderRun |
| RegisterDeliveryOutput | Run + purpose + content hash | 回读 M09 artifact/output ref |
| FinalizeDeliverySnapshot | Build + manifest hash | 回读唯一 Snapshot |
| Grant/RevokeDeliveryAccess | Snapshot + principal + action + request key | 回读原 grant/revocation |
| CreateDeliveryDownloadTicket | active Grant revision + Snapshot manifest hash + request key | 回读同 opaque ticket；request hash 不同冲突 |
| StreamDeliveryDownload | ticket + request/range id + grant revision | 追加/回读同 DownloadReceipt，不重复计数或扩大 range |

## 7. 页面与可观察状态

| 页面/视图 | 主要动作 | 必须展示的状态 |
| --- | --- | --- |
| 基础装配工作台 | 从选择初始化、排列/裁剪、简单转场、播放 | source ready/missing/newer baseline、draft/approved/conflict、proxy unavailable、precision warning；明确“Animatic 来源”而非同一对象 |
| 音频与字幕面板 | 放置音频、基础混音、手工/导入/校对字幕 | imported/selected/placeholder、artifact unavailable、cue invalid、timebase mismatch；Provider Gate 未通过时生成按钮禁用并展示原因 |
| 交付预检 | 选择 preset、用途/地域，查看 blockers | checking/pass/warn/block/stale；按 material/selection/quality/rights/review/spec/output 分类并链接来源 |
| Build/RenderRun 详情 | 启动、取消、重跑、查看各 stage/output | queued/rendering/validating/partial/failed/cancelled/unknown/succeeded；每次 Run 独立，展示 checkpoint/diagnostics |
| Snapshot/Manifest | 查看不可变输入输出谱系、校验和、授权和网关下载 | publishing transaction、published、access denied/revoked/expired、artifact location unavailable；不会出现 partial Snapshot，也不展示 MinIO key/URL |
| 交付历史 | 比较 Assembly/Build/Snapshot、重新授权下载 | empty/ready、newer project version、retention blocked、M14 recheck required |

指标：预检一次通过率及 blocker 类型、Build/Run 成功/partial/failed/unknown、阶段耗时与恢复时间、输出隔离/校验失败、Snapshot 原子性错误、可复现率、下载网关授权拒绝/撤销传播/中断流/字节数、音频/字幕来源分布、Provider Gate 阻塞原因、每交付分钟分单位资源。日志不记录完整字幕/Prompt/媒体、ticket、bucket/key 或 MinIO URL，只记录逻辑 refs/hash/spec/stage/trace。

## 8. 失败路径与恢复约定

| 场景 | 用户结果 | 恢复 |
| --- | --- | --- |
| 缺主选/媒体 | 预检阻塞到具体 Shot/purpose | 返回 M09 选择/媒体修复；新输入 hash 后重跑预检 |
| 权利/质量/审阅缺失 | 正式 Build 不启动，分别显示依据 | 在对应 M10/M12/M14 完成事实；不同例外不能互替 |
| 音频/字幕 Provider 未过 Gate | 显示“需导入/手工或等待能力准入” | 使用导入/已有素材；Gate 通过后走 M07→M08→M09，不改 M13 边界 |
| 渲染中断 | 当前 Run 失败/unknown，已确定中间物按 TTL 保留 | 先按 staging exact version/hash reconcile unknown；从 checkpoint 恢复或新 RenderRun，未被 Artifact/Snapshot 引用的对象由具名 cleanup 清理 |
| 主文件成功、字幕失败 | Build partial，无 Snapshot | 新 Run 只重做缺失/失败附属输出，再整体验证 |
| 输出校验失败 | 文件隔离并显示 hash/spec 差异 | 修复 engine/preset 后新 Build/Run，不发布坏文件 |
| finalize 事务失败 | 用户仍只看到 succeeded Build，无半个 Snapshot | 同 manifest 重试并回读/原子创建唯一 Snapshot |
| 下载访问撤销 | opaque gateway ticket 的新请求/Range 立即 403，活动流在短 abort interval 中止；Snapshot/历史不变 | 经 M14/权限重新授权并创建新 Grant/ticket；绝不复活旧 ticket |
| Snapshot Artifact active Location 丢失 | Snapshot/hash 保持不变但下载显示存储暂不可用 | M09 从同 SHA-256 备份恢复为新 verified Location；不得改 Manifest 或用同 key latest 冒充 |
| 外部 NLE 回传 | 不覆盖 Assembly/Snapshot | 经 M09 作为新来源摄取，再创建新 Assembly/Build 链 |

## 9. 逐 ID 验收映射

### 9.1 功能需求

| ID | 设计落点 | 可执行验证 | 切片 |
| --- | --- | --- | --- |
| DLV-FR-001 | `CreateAssemblyFromSelections` | 用明确 ShotOrder + Selection 建 revision，修改 current selection 后旧 revision 不变 | E |
| DLV-FR-002 | VideoTrackItem/基础画面命令 | 顺序、入出点、空隙、简单转场与必要黑场的时间线测试 | E |
| DLV-FR-003 | AudioTrackItem/AudioMixRevision | 对白/旁白/环境声/音效/音乐放置、gain/fade/mute 重读测试 | E |
| DLV-FR-004 | CaptionRevision create/import/revise | 手工与受支持字幕导入、校对、样式、导出 round-trip 测试 | E |
| DLV-FR-005 | rational timebase/project spec | 分数帧率、raster/color/audio sample spec 校验和整数 tick 属性测试 | E |
| DLV-FR-006 | `RunDeliveryPreflight` | 缺素材/选择/M10/M12/M14/spec 分项阻止测试 | E |
| DLV-FR-007 | frozen DeliveryInput/manifest hash | start 前后修改项目，运行中的 Build 输入 hash 不变 | E |
| DLV-FR-008 | ExportPreset required outputs/DeliveryOutput | 主文件、字幕、音频、缩略图、交换清单组合测试 | E |
| DLV-FR-009 | RenderRun 独立记录/retry | 同 Build 失败后重跑产生新 Run 且历史完整 | E |
| DLV-FR-010 | `FinalizeDeliverySnapshot` 原子事务 | 缺任一 required output/校验失败均无 Snapshot；全部成功一次创建 | E |
| DLV-FR-011 | DeliveryManifest | schema 测试覆盖输入版本、选择、批准、权利、规格、精确 Artifact refs 和全部 hash；不含可变 MinIO latest key | E |
| DLV-FR-012 | AccessGrant/Ticket/Gateway/历史查询 | 授权后通过 opaque 网关下载；撤销提交后的新请求/Range 即时 403，旧 Snapshot 内容不变且终端未获得 MinIO URL | E |
| DLV-FR-013 | ordered shot package preset | Manifest 顺序/命名/hash 与实际文件集合逐项一致 | E |

### 9.2 非功能需求

| ID | 设计落点 | 可执行验证 |
| --- | --- | --- |
| DLV-NFR-001 | frozen input、engine/plan hash、content-addressed outputs | 相同 fixture 重跑，解释输入/engine 差异并校验输出 hash/spec |
| DLV-NFR-002 | Snapshot + Manifest 数据库原子发布 | MinIO 写入/验证与事务故障注入、对象缺失、copy 响应丢失和重复 finalize 测试，永不返回半成品 |
| DLV-NFR-003 | RenderRun/checkpoint/独立 staging capability | Worker 中断恢复与越权 key/version 测试；历史成功 Snapshot/Artifact 不被覆盖 |
| DLV-NFR-004 | integer ticks/rational timebase | 23.976/29.97、长音频、大 tick 和字幕 round-trip 属性测试 |
| DLV-NFR-005 | M14 download gateway/retention/delete port | 许可到期、grant 撤销、活动流中止、LegalHold/retention/exact-version 删除跨模块测试 |

### 9.3 验收条件

| ID | 验收场景到测试 | 通过标准 |
| --- | --- | --- |
| AC-DLV-001 | 至少 10 个主选镜头装配 E2E | 可播放、顺序稳定、保存重开一致；对象是 Assembly 而非 Animatic |
| AC-DLV-002 | 对白/旁白/字幕在分数帧率项目保存重开 | 所有 integer ticks 不变且视听定位无漂移 |
| AC-DLV-003 | 移除一个 required Selection | preflight 指向具体 shot 并禁止 Build |
| AC-DLV-004 | 渲染失败或缺附属输出 | Build failed/partial，Snapshot 查询为空 |
| AC-DLV-005 | 成功 Snapshot 追溯 | 能从 Manifest 解析全部输入/选择/批准/权利/不可变 Artifact/output hash，且 Location 迁移不改 Manifest |
| AC-DLV-006 | Snapshot 后修改项目/Assembly head 或 MinIO 同 key 出现新 version | 旧 Manifest、网关下载内容与 hash 完全不变；绝不读取 latest 冒充 |
| AC-DLV-007 | 同 Build 安全重跑并重复 finalize | RenderRun 增加，Snapshot 仍唯一且不重复发布 |
| AC-DLV-008 | 有序镜头包输出 | Manifest ordinal/filename/hash 与目录逐项双向一致 |
| AC-DLV-009 | 首期功能/命令/验收清单审计 | 无高级调色、复杂 VFX、专业混音、插件或多人 NLE 操作 |

高级 NLE 功能是明确非目标，不属于“暂缓后默认实施”的范围。

## 10. 实施顺序与门禁

1. 建立 Assembly/TrackItem 精确时间模型和从 M05/M09 初始化的只读边界。
2. 完成导入/已有音频、基础 AudioMix、手工/导入 Caption；Provider 生成入口保持 Gate 关闭。
3. 实现确定性 preflight、冻结 DeliveryBuild、RenderRun/checkpoint 和 M09 output 登记。
4. 实现 required output 精确 Artifact 验证、Snapshot 数据库原子事务、Manifest/历史和有序镜头包。
5. 实现 AccessGrant、opaque Ticket 与“鉴权控制面 + 反向代理数据面”下载网关，完成 Range、限速、撤销后新请求拒绝、活动流中止、M14 重检、Go API 不缓冲正文和不泄漏 MinIO URL 的安全测试。
6. 仅当某音频/字幕能力逐项通过 M07/M08/M14/M11/M09-M10 Gate 后，接入该能力产物；M13 本身始终不调用 Provider。

# DES-005 S2 供应商与合规准入设计

- 状态：blocked
- 调研日期：2026-07-29
- 适用切片：S2 剧本结构与资产
- 上游：[PRD-003 MVP 执行计划与追踪矩阵](../prd/003-MVP执行计划与追踪矩阵.md)、[架构与技术选型](./002-人工智能短剧平台架构与技术选型.md)、[审核治理模块详细设计](./模块设计/005-审核治理模块详细设计.md)
- 下游：[PLAN-000 MVP 全栈实施总计划](../plan/000-MVP全栈实施总计划.md)、[PLAN-002 MVP 纵向切片执行计划](../plan/002-MVP纵向切片执行计划.md)

## 1. 结论

S2 暂不具备实施准入条件。D-003 已形成可评审的 Text Provider 候选基线，但当前开发环境没有可用的生产调用凭据，不能验证账号实际额度、地域可用性和真实响应契约；D-005 已形成面向中国大陆首发的工程合规基线，但首发地区、合规责任人和留存规则尚未获得产品负责人确认。

在两项门禁关闭前，只允许维护脱敏 fixture、Provider 端口契约和合规测试清单，不创建 S2 业务表、Worker、空模块或模拟“真实供应商已验收”的实现。本文件是工程决策输入，不构成法律意见。

## 2. 事实、推断与待确认事项

| 类型 | 内容 | 处理方式 |
| --- | --- | --- |
| 公开事实 | 阿里云百炼在北京地域提供 OpenAI 兼容接口；指定 Qwen 模型支持结构化输出；模型价格、限流和下线策略由官方页面维护 | 记录带日期的能力快照；运行时保存实际模型、地域和用量，不把价格写死进领域逻辑 |
| 公开事实 | 中国大陆已对生成式人工智能服务、深度合成、人工智能生成内容标识和个人信息处理设定义务 | 在公开发布前完成适用性评审；显式标识、隐式元数据、授权证据和审计记录必须进入交付链 |
| 合理推断 | 产品中文化、参考即梦/可灵且对象存储计划使用阿里云 OSS，因此首发地区倾向中国大陆，Text Provider 倾向阿里云百炼北京地域 | 仅作为候选方案，不代替产品负责人确认 |
| 待确认 | 首发地区、合规责任人、供应商账号与额度、正式留存期限、未成年人策略 | 未确认前 D-003/D-005 保持 `blocked` |

## 3. D-003 Text Provider 候选基线

### 3.1 固定候选

| 项目 | 候选决策 |
| --- | --- |
| Provider | 阿里云百炼 Model Studio，中国大陆北京地域 |
| 接口 | `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` |
| 模型 | 固定快照 `qwen3.7-plus-2026-05-26`，不使用会自动升级的浮动别名 |
| 调用方式 | 非流式、`enable_thinking=false`、`response_format={"type":"json_object"}`，提示词明确要求 JSON |
| 建议参数 | `temperature=0.1`、`top_p=0.8`、输出上限 16K tokens；真实样例通过后再调整 |
| 输出验证 | LangChain 获取结构化文本输出，Provider JSON 再经 Pydantic 领域模型校验；LangGraph 只编排提取、修复/阻塞和人工决议，缺字段、未知枚举或引用不存在均不得写入确认事实 |
| 凭据 | 只读取本地 secret 中的 `DASHSCOPE_API_KEY`；不写入文档、日志、消息或 Git |
| 禁止项 | 不使用仅面向个人编程场景的 Coding/Token Plan key 承担平台生产自动化 |

公开能力依据：[模型列表与上下文规格](https://help.aliyun.com/en/model-studio/text-generation-model/)、[结构化输出](https://help.aliyun.com/en/model-studio/qwen-structured-output)、[北京地域调用方式](https://help.aliyun.com/zh/model-studio/model-calling-in-sub-workspace)。

### 3.2 能力与费用快照

以下数值只用于 2026-07-29 的准入预算，不是长期价格契约：固定快照在不超过 256K 输入时，公开价为输入 2 元/百万 tokens、输出 8 元/百万 tokens；超过 256K 至 1M 时为输入 6 元/百万 tokens、输出 24 元/百万 tokens。官方公开限流为 600 RPM、1,000,000 TPM。账号实际免费额度、并发和账单必须通过目标账号验证，不能依据公开页面推定。

来源：[模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)、[限流说明](https://help.aliyun.com/en/model-studio/rate-limit)、[模型下线策略](https://help.aliyun.com/zh/model-studio/model-depreciation)。每次调用保存 `provider`、`region`、实际 `model`、请求标识、输入/输出 token 和计价快照版本；价格查询失败不得阻止保存提取结果，但不得将未确认费用展示为最终费用。

官方隐私说明称请求数据不用于模型训练，但调用数据仍会按适用法律和服务条款处理；精确保存期限必须以目标账号签署的服务协议为准。依据：[百炼隐私政策](https://help.aliyun.com/zh/model-studio/privacy-notice)、[数据安全常见问题](https://help.aliyun.com/en/model-studio/faq-about-alibaba-cloud-model-studio)。在该边界获得负责人接受前，真实契约样例必须脱敏，不向 Provider 发送可识别自然人的正文、声音、肖像或授权证明。

### 3.3 失败分类

| 类别 | 典型结果 | 系统动作 |
| --- | --- | --- |
| 输入不可接受 | HTTP 400、结构校验失败 | 不重试；任务失败并显示可修改输入的下一动作 |
| 配置或权限错误 | HTTP 401/403/404 | 不自动重试；标记服务不可用并告警，禁止泄露凭据或原始正文 |
| 限流 | HTTP 429 | 有界指数退避并加入抖动；保留同一业务幂等键 |
| 供应商暂时故障 | HTTP 500/503、连接超时 | 有界重试；无法判断副作用时进入 `unknown`，使用原请求标识对账 |
| 输出不符合契约 | 非 JSON、字段非法、引用不存在 | 保存脱敏诊断，不写入业务事实；允许人工重新提取 |

依据：[官方错误码](https://help.aliyun.com/zh/model-studio/error-code/)、[限流处理建议](https://help.aliyun.com/zh/model-studio/rate-limiting-best-practices)。正文、提示词、授权证明和供应商原始响应不得进入 RabbitMQ 消息或普通日志；消息只携带任务标识和事实版本标识。

### 3.4 D-003 关闭条件

1. 产品/AI 负责人接受上述 Provider、地域、固定模型和参数，确认可调整参数只进入能力快照。
2. 在本地 secret 配置目标账号的 `DASHSCOPE_API_KEY`，确认不是个人 Coding/Token Plan key。
3. 使用仓库内脱敏固定剧本执行 `make contract-text-provider` 发起一次真实调用，记录日期、地域、实际模型、请求标识、token 用量、HTTP 结果和 Pydantic 校验结果；命令只输出脱敏证据，不保存密钥和原始供应商正文。
4. 验证目标账号的实际额度、限流和账单查询路径，并给出 S2 测试预算上限。
5. 对 400、401/403、429、超时、5xx 和非法 JSON 建立可执行契约测试后，才把 D-003 改为 `closed`。

当前阻塞证据：截至 2026-07-29，真实契约探针已实现且本地已配置 `DASHSCOPE_API_KEY`；真实请求到北京按量付费 Base URL 返回 HTTP 401（仅保留 request_id 脱敏证据），说明密钥与当前地域/计费方案不匹配或无效，因此仍没有成功响应、目标账号额度或账单样例。400、401/403/404、429、超时、5xx 与非法 JSON 的脱敏失败契约已由 commit `ee2cfdc` 实现并在 [CI 30447411225](https://github.com/StephenQiu30/lanverse/actions/runs/30447411225) 通过，关闭条件 5 已满足；关闭条件 1～4 未全部满足。修正匹配的账号密钥/Base URL 并取得成功契约前，D-003 保持 blocked。安装了 Ollama 但没有本地模型，它不能替代目标 Provider 的生产契约验收。

## 4. D-005 工程合规基线

### 4.1 候选适用范围

本基线按“面向中国大陆公众提供人工智能短剧制作与下载服务”准备，但只有产品负责人明确首发地区后才能接受。若首发范围不同，必须重新完成当地适用性评审，不能直接复用本结论。

主要公开规则包括：[生成式人工智能服务管理暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)、[互联网信息服务深度合成管理规定](https://www.cac.gov.cn/2022-12/11/c_1672221949570926.htm)、[人工智能生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm)及[个人信息保护法](https://www.miit.gov.cn/jgsj/zfs/fl/art/2022/art_515a4b20c12f430eab54bb4f56d89f56.html)。是否需要算法备案或其他上线手续必须由指定合规责任人根据真实服务方式确认。

### 4.2 最小权利与授权模型

每份 ConsentRevision 至少记录：授权主体、权利类型（著作权/肖像/声音/其他）、被授权用途、生成与发布渠道、地域、有效期、撤回规则、授权证明的 MediaVersion、提交人和审查人。涉及人脸、声纹或可识别自然人的材料，必须单独确认处理的必要性、目的、方式、范围和同意；未成年人材料在 MVP 默认阻断，直到监护人授权流程另行接受。

声音人格权益依据还包括：[民法典关于声音保护的规则](https://gdca.miit.gov.cn/zwgk/zcwj/flfg/art/2020/art_573d6ef5018b46b6a4e1f31ca085a710.html)和[最高人民法院人工智能生成声音人格权案例](https://www.court.gov.cn/zixun/xiangqing/467631.html)。系统只验证证据是否完整、有效且覆盖目标用途，不自动判断授权文件的法律效力；有争议时必须转人工审查。

### 4.3 审核、标识与留存

| 能力 | MVP 基线 |
| --- | --- |
| 责任分工 | 内容提交人声明来源和授权；平台运营审查证据；产品负责人指定最终合规责任人；系统只执行已接受规则 |
| 生成门禁 | 权利状态为未知、过期、撤回或覆盖不足时可以保存草稿，但禁止 ready、提交生成和交付 |
| 内容检查 | 保存 Provider 内容安全结果和人工决议；检查服务不可用时对生成与交付 fail-closed |
| 显式标识 | 预览和下载成片必须展示人工智能生成/合成标识；MVP 不提供“无标识导出” |
| 隐式标识 | 交付媒体元数据记录生成属性、服务提供者标识和可追踪内容编号；DeliveryManifest 同步记录 |
| 留存 | 不臆造统一年限；Consent、审核、生成和交付审计不可随业务软删除立即消失，具体期限在公开测试前由合规责任人形成表格并接受 |
| 撤回 | 撤回只阻止后续使用和生成，不篡改已发生的审计事实；历史交付的处置由人工决议记录 |

标识办法自 2025-09-01 起施行，并规定特定未显式标识场景的用户义务和日志留存要求。Lanverse MVP 直接禁止无标识导出，因此不把该例外设计成产品能力；未来若增加，必须先修订 Requirement、Design、PRD 和留存策略。

### 4.4 脱敏授权验收样例

授权验收 fixture 只使用虚构主体和仓库生成的占位资产，不得包含真实姓名、证件、声音、肖像、签名或供应商原始响应。每个样例固定包含 `fixture_id`、主体类型、权利人角色、权利类型、资产及版本、授权用途、发布渠道、地域、有效期、撤回状态、证明文件版本、提交人、审查人、期望决议和原因码。

| fixture_id | 场景与输入 | 期望决议 | 原因码 |
| --- | --- | --- | --- |
| `consent-valid-adult-001` | 虚构成年主体；图像与声音权利；覆盖 AI 短剧生成及中国大陆公开发布；授权在有效期内；证明文件版本可访问 | 可进入生成与交付门禁 | `consent_valid` |
| `consent-revoked-001` | 字段完整，但权利人在生成前已撤回 | 允许保留草稿；阻断后续生成与交付 | `consent_revoked` |
| `consent-insufficient-001` | 授权只覆盖内部演示，本次请求公开发布 | 阻断生成与交付，并提示补充用途范围 | `purpose_not_covered` |
| `consent-expired-001` | 授权有效期早于本次生成时间 | 阻断生成与交付 | `consent_expired` |
| `consent-minor-001` | 主体为未成年人，MVP 没有已接受的监护人授权流程 | 默认阻断，不进入自动复核 | `minor_not_supported` |

同一 `ConsentRevision` 的判定必须是可复现的纯规则：输入事实版本和规则版本相同，输出决议及原因码相同。修改授权不覆盖历史版本；撤回生成新版本并只影响撤回后的新动作。人工审查只能产生带审查人、时间和理由的新决议，不得直接改写旧审计事实。

可执行输入位于 [授权门禁 fixture](../../backend/tests/fixtures/governance/consent_gate_cases.json)，契约校验位于 [fixture 契约测试](../../backend/tests/contract/test_consent_fixture_contract.py)。fixture 只使用 `synthetic-*` 引用，不引用外部 URL，也不携带姓名、证件、电话、邮箱、地址或签名字段；它们是后续 GOV-001/002 Red 测试的固定输入，不代表产品负责人已经接受规则。

### 4.5 留存与删除候选矩阵

下表是提交合规责任人签认的工程候选，不把尚无依据的统一年限写入代码。个人信息遵循目的限定和最短必要期限；删除项目或账号时，业务资产按已接受策略删除或匿名化，受到投诉、争议、法定义务或安全事件保全的事实进入访问受限的 legal hold，不随软删除消失。

| 事实类型 | 留存目的与起算点 | MVP 候选处理 | 负责人待确认项 |
| --- | --- | --- | --- |
| 授权证明及 `ConsentRevision` | 证明目标资产在生成/发布时具有相应授权；从最后一次使用或撤回起算 | 对业务用户不可见后转受限存储；争议时保全；到期删除证明文件并保留不可还原的最小审计摘要 | 合同、争议处理和适用法律对应的最终期限 |
| 审核决议与内容安全结果 | 支撑发布门禁、申诉和监管审计；从决议完成起算 | 原始敏感正文不进入普通日志；保留决议、规则版本、原因码和证据引用；到期删除或匿名化 | 申诉窗口及最终期限 |
| 生成任务与 Provider 请求标识 | 重试、幂等、故障对账和成本核算；从任务终态或账单结清起算 | 保存任务状态、模型、地域、请求标识和 token 用量；不把提示词或供应商原文复制进审计表 | 供应商合同、账单周期对应的最终期限 |
| 交付物、隐式标识与 `DeliveryManifest` | 用户下载、内容追踪和交付证明；从最后交付或项目删除起算 | 用户可按产品策略删除业务副本；发生争议时冻结相应版本；到期删除对象并保留最小不可还原摘要 | 项目/账号删除后的宽限期及最终期限 |
| 操作 `AuditEvent` | 安全调查和责任追踪；从事件发生起算 | 只记录主体、动作、对象标识、结果和关联标识；禁止记录凭据、完整输入、预签名 URL | 安全事件响应和适用监管对应的最终期限 |
| 无标识导出的接收者日志 | 仅在未来允许用户要求无显式标识导出时适用 | MVP 禁止该能力，因此不采集；未来若启用，按标识办法记录用户信息和日志且保存不少于六个月 | 当前为“不适用”；启用前重新评审 |
| 临时上传/下载地址 | 完成受控传输 | 只在请求时短期签发，不作为业务事实或日志字段保存；过期即失效 | 预签名 URL 的短期有效时长 |

最终期限必须配置在统一的留存策略中，由清理任务按事实类型执行；领域代码不得散落硬编码天数。软删除只控制业务可见性，不等同于完成个人信息删除，也不能成为无限期保留的理由。

### 4.6 产品负责人签认清单

关闭 D-005 只需要对以下四项给出明确结论，不能用“默认同意”代替：

1. 首发是否面向中国大陆公众；如果不是，提供首发地区并重新评审适用规则。
2. 指定一名可被追责的产品/运营合规责任人（姓名或组织角色均可，但必须能够实际处理审核、申诉和上线材料）。
3. 接受“无标识导出不进入 MVP”“未成年人材料默认阻断”以及本节五个脱敏授权样例的期望决议。
4. 在 4.5 的每一行填写最终期限或明确的删除触发条件；在签认前实现只允许使用更严格的临时策略，不能声称长期策略已验收。

### 4.7 D-005 关闭条件

1. 产品负责人明确首发服务地区；若为中国大陆，书面接受本节作为工程基线。
2. 指定可被追责的产品/运营合规责任人，并由其确认算法备案、生成式人工智能服务适用性及上线材料清单。
3. 接受 ConsentRevision 字段、未成年人默认阻断、人工审查责任和“无标识导出不进入 MVP”。
4. 签认 4.5 的留存/删除矩阵并补齐最终期限或删除触发条件。
5. 接受 4.4 的脱敏授权模板、撤回样例、权利不足样例和未成年人阻断样例；标识验收样片在 S4 合成交付实现前补齐，作为 GOV-005/006 测试输入。

当前阻塞证据：截至 2026-07-29，脱敏授权验收 fixture、对应契约测试与按事实类型划分的留存/删除候选矩阵已经形成；仓库仍没有首发地区确认记录、明确的合规责任人、对模板与策略的签认，以及各事实类型的最终期限或删除触发条件，因此 D-005 不能关闭。

## 5. S2 解锁顺序

1. 先分别完成 D-003 与 D-005 的关闭清单，在 [PRD-003](../prd/003-MVP执行计划与追踪矩阵.md) 记录日期、责任人和可访问证据。
2. 接受 S2 对应 Requirement、Design 和 PRD 范围，不扩大到 S3/S4 Provider 能力。
3. 将 `DEV-S2-01` 指派给具体负责人并补充估算，状态从 `blocked` 改为 `ready`。
4. 严格按 `DEV-S2-01 → DEV-S2-06` 实现，并在 S2 Acceptance 中记录真实 RabbitMQ、MinIO、Provider 和浏览器闭环证据。

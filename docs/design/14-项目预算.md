# DES-14 项目预算

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-11 项目预算](../requirement/11-项目预算.md)：PRJ-03 项目预算 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 项目设置 · 预算；成本页；报价确认对话框 |
| 基础设计 | DES-02 §5.10；DES-04 §6–7 |
| 依赖功能设计 | [DES-13](13-项目管理.md)、[DES-24](24-报价与二次确认.md)、[DES-35](35-成本账本与报表.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-11](../requirement/11-项目预算.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-11 §2 的规则）与验收标准以 REQ-11 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `billing.budget`、`billing.ledger_entry` |
| 接口 | `GET /api/projects/{pid}/budget`<br>`PUT /api/projects/{pid}/budget` |
| 工作流与任务 | 无 |
| 领域事件 | `billing.budget_changed.v1`、`billing.budget_low.v1` |
| 测试用例 | TC-11-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `billing.budget` | `limit_micros`、`reserved_micros`、`settled_micros`、`revision`；`ck_budget_balance` | 余额计算与并发保护 |
| `billing.ledger_entry` | `entry_type = budget_change` | 预算调整留痕 |

说明：由于上述“结算超支”边界，`ck_budget_balance` 调整为 `reserved_micros + settled_micros <= limit_micros OR is_overrun`，并增加 `is_overrun boolean NOT NULL DEFAULT false` 列（DES-02 同步修改）。

## 3. 接口

```http
GET /api/projects/{pid}/budget
200 { "limit_micros": 500000000, "reserved_micros": 12000000, "settled_micros": 180000000,
      "available_micros": 308000000, "available_display": "¥308.00", "is_overrun": false, "revision": 4 }

PUT /api/projects/{pid}/budget
{ "limit_micros": 800000000, "expected_revision": 4 }
200 { … }     422 validation_failed { "errors": [{ "field": "limit_micros", "code": "below_committed", "message": "最低可设 ¥192.00" }] }
```

报价与确认中的预算错误：`422 budget_insufficient { "meta": { "shortfall_micros": 30000000 } }`。

## 4. 异步与工作流

无；预留与结算在 `flow` 队列 Activity 的数据库事务中执行（DES-04）。

## 5. 事件与实时

`billing.budget_changed.v1`（audit）、`billing.budget_low.v1`（notify）；SSE `budget.updated`。

## 6. 界面

设置页预算卡片（上限、已结算、已预留、可用、使用率进度条）；报价对话框显示剩余预算与差额；顶部低余额横幅。

## 7. 权限与审计

管理员与制作者均可设置（MVP 内部使用）；审计 `budget.changed`（前后值）。

## 8. 非功能要求

REL-08（任何时刻预算 ≥ 已结算 + 已预留，超支需显式标记；每日对账）。

## 9. 测试设计

并发确认测试（race）；结算超支路径；阈值通知去重。

## 10. 待确认

无。

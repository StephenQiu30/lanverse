# DES-28 结果复用与写入边界

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-25 结果复用与写入边界](../requirement/25-结果复用与写入边界.md)：GEN-05 相同输入复用结果；GEN-10 任务不覆盖人工配置 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 报价确认组件、镜头详情 |
| 基础设计 | DES-04 §5.1 |
| 依赖功能设计 | [DES-24](24-报价与二次确认.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-25](../requirement/25-结果复用与写入边界.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-25 §2 的规则）与验收标准以 REQ-25 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | 无独立表（规则作用于 Operation 相关表，见 §2） |
| 接口 | 无 |
| 工作流与任务 | `OperationWorkflow`、`flow.CompleteFromReuse` |
| 领域事件 | 无 |
| 测试用例 | TC-25-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

`operation.operation.input_hash`、`reused_from_id`、`force_regenerate`、`target_version_no`；索引 `ix_operation_reuse (project_id, input_hash) WHERE status = 'completed'`。复用时新建 `operation_output` 行指向同一 `media_asset_id`（不复制对象）。

写入边界在代码层实现：工作流的 `flow` Activity 只能调用 `operation` 模块的结果写入端口与 `storyboard` 的 `UpdateProgress` 端口；架构测试（`go-arch-lint` 规则）禁止 Activity 包依赖其他模块的命令用例。

## 3. 接口

报价响应项含 `reused: true`、`reused_from_operation_id`；请求项支持 `force_regenerate: true`（见 [DES-24](24-报价与二次确认.md)）。

## 4. 异步与工作流

复用项确认后不启动供应商流程：`OperationWorkflow` 直接执行 `flow.CompleteFromReuse`。

## 5. 事件与实时

同 REQ-21。

## 6. 界面

报价对话框中复用项显示“复用已有结果 ¥0”与“强制重新生成”开关；候选卡片显示“基于 vN 生成”。

## 7. 权限与审计

审计 `operation.confirmed` 中记录 `reused_from`。

## 8. 非功能要求

REL-02、COST-01。

## 9. 测试设计

`input_hash` 规范化的表驱动测试（等价与不等价用例）；架构依赖规则测试。

## 10. 待确认

无。

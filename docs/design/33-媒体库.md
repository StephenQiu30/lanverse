# DES-33 媒体库

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-30 媒体库](../requirement/30-媒体库.md)：MED-01 上传素材；MED-02 浏览、检索与预览；MED-03 删除素材；MED-04 来源与合规信息 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 媒体库、上传对话框、媒体详情 |
| 基础设计 | DES-02 §5.8；DES-04 §10.2；DES-07 §7–8 |
| 依赖功能设计 | [DES-20](20-真人授权.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-30](../requirement/30-媒体库.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-30 §2 的规则）与验收标准以 REQ-30 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `media.media_asset`、`media.rendition` |
| 接口 | `POST /api/projects/{pid}/uploads`<br>`POST /api/uploads/{media_asset_id}:complete`<br>`GET /api/projects/{pid}/media`<br>`GET /api/media/{id}`<br>`DELETE /api/media/{id}` |
| 工作流与任务 | `MediaIngestWorkflow`、`media.DetectAndProbe`、`media.Hash`、`media.MakeRenditions`、`flow.MarkMediaReady`、`media-purge` |
| 领域事件 | `media.asset_status_changed.v1` |
| 测试用例 | TC-30-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 处理流程

见 [DES-03 §4.12](../design/03-接口设计.md#412-上传流程)：申请上传 → 预签名直传 → 完成通知 → MediaIngestWorkflow（按内容识别类型 → ffprobe → sha256 → 缩略图 / 代理 / 波形 → 审核）→ `ready` / `rejected` / `failed`。

## 3. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `media.media_asset` | `kind`、`origin`、`status`、`object_key`、`mime_type`、`byte_size`、`sha256`、宽高时长帧率编码、`source_operation_id`、`provider_key`、`model_key`、`region`、`moderation_status`、`aigc_marked`、`consent_record_id`、`contains_real_person`、`is_delete`、`delete_time`、`purge_after` | 统一媒体元数据 |
| `media.rendition` | `thumb_256`、`thumb_640`、`poster`、`proxy_720p`、`waveform` | 列表与预览性能 |
| 对象存储 | `projects/{pid}/{kind}/{yyyy}/{mm}/{id}.{ext}`；派生版本 `…/{id}/{rendition}.{ext}` | 按项目前缀便于清理 |

新增 `media_asset.upload_id text`（分片上传 ID）与 `sha256` 部分索引（`project_id, sha256 WHERE NOT is_delete`，DES-02 同步）。

## 4. 接口

```http
POST /api/projects/{pid}/uploads
{ "file_name": "推镜参考.mp4", "mime_type": "video/mp4", "byte_size": 314572800, "kind": "video", "sha256": "…(可选)" }
201 { "media_asset_id": "…", "duplicate_of": null,
      "upload": { "mode": "multipart", "upload_id": "…", "part_size": 16777216,
                  "parts": [ { "part_number": 1, "url": "…" }, … ], "expires_at": "…" } }

POST /api/uploads/{media_asset_id}:complete   { "parts": [ { "part_number": 1, "etag": "…" } ], "contains_real_person": false }
202 { "status": "processing" }

GET /api/projects/{pid}/media?kind=video&origin=upload&referenced=true&q=推镜&limit=50
200 { "items": [ { "id": "…", "kind": "video", "file_name": "推镜参考.mp4", "status": "ready", "duration_ms": 8000,
                   "width": 1080, "height": 1920, "thumb_url": "…", "referenced": true, "moderation_status": "passed", "create_time": "…" } ] }

GET /api/media/{id}
200 { …, "provenance": { "origin": "generated", "operation_id": "…", "provider": "openrouter", "model_key": "…", "region": "overseas" },
      "compliance": { "moderation_status": "passed", "aigc_marked": false, "consent": null },
      "references": [ { "type": "shot_take", "label": "第 1 集 3-12" } ] }

DELETE /api/media/{id}   409 in_use { "meta": { "references": [ { "type": "shot_take", "label": "第 1 集镜头 8" } ] } }
```

## 5. 异步与工作流

`MediaIngestWorkflow`（`media-ingest/{id}`）：`media.DetectAndProbe` → `media.Hash` → `media.MakeRenditions`（并行）→ `agent: moderation.check` → `flow.MarkMediaReady`。`media-purge` 每日清理。

## 6. 事件与实时

`media.asset_status_changed.v1` → realtime；SSE `media.updated`。

## 7. 界面

媒体库网格 / 列表切换（虚拟滚动）、筛选栏、搜索；拖拽上传区（多文件进度、失败原因）；预览（图片灯箱、视频播放器、音频波形）；详情抽屉（来源、合规、引用位置）。

## 8. 权限与审计

制作者；审计 `media.deleted`、`media.restored`。

## 9. 非功能要求

PERF-07（上传速度 ≥ 带宽 70%，处理 ≤ 30 秒）；PERF-04（2,000 条筛选 ≤ 1 秒）；SEC-06、SEC-07；CMP-03。

## 10. 测试设计

内容类型识别；分片上传续传；引用检查覆盖全部引用来源（表驱动）；清理任务。

## 11. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-33-Q1 | 内容审核使用哪个服务？ | 火山引擎内容安全（图片、视频、音频、文本），通过 `moderation.check` 适配，可替换。 | P0 结束时 |
| DES-33-Q2 | 分片上传的分片大小与签名有效期？ | 分片 16 MB，签名链接 1 小时有效；续传时重新签发未完成分片的链接。 | M1 实施中 |
| DES-33-Q3 | 同一项目内上传相同文件（sha256 相同）时如何处理？ | 返回 `duplicate_of`，前端提示并默认复用已有素材；用户可选择仍然新建。 | M1 实施中 |

需求层面的待确认问题见 [REQ-30](../requirement/30-媒体库.md) 的“待确认”。

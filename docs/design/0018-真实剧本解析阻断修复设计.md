# 真实剧本解析阻断修复设计

2026-09-08 用户在阅读 0017 核查报告后要求处理真实问题。沿用 0013/3004/0015 的所有权与人工门，不另建调度系统。

## 首批可验证修复

1. 原文上下文改为紧凑的 `[block_index, exact_text]` 编码，保留每个空行、换行、原文字符和绝对块号。偏移、hash 和集号由可信 source 层保管及校验，不在每行重复发送。发布新的 text-storyboard release；不提高 240000 字节上限、不截断原文，也不改写旧运行的 release。验证指定 60 集原稿确实能装配。超出该上限的稿件仍明确拒绝，不能据此宣称任意长稿已支持。
2. 新 map_manuscript 正式采纳时，Go 同一事务写 Episode、ScriptVersion、每集 ScriptVersionPublished Outbox 和 OwnerReceipt。事件绑定实际采纳回执、集/版本/原稿范围，复用既有发布和搜索投影。事件插入失败整体回滚；相同采纳重放不重复创建。
3. 上传确认后的主入口连接新 Creation，旧制作圣经不再作为新上传的默认下一步；历史资源不删除。状态分别表达原稿固定、推理/审阅、正式采纳和检索。
4. 配置并验证现有 Creation API、独立数据库和 Temporal Worker，保留 Go 正式 Writer 和 HTTPS/loopback 限制；修复实际 ES 连接，执行真实索引查询。
5. 场景/人物提取沿既有逐集分析与全局归并运行。正式采纳不得遗漏候选已提供的可见细节；人物身份、关系、状态与原文证据对照指定稿核验。全稿归并若超限，须另行细化有界归并合同，不能静默省略逐集事实。

6. 真实分集返回后暴露执行快照的跨语言缺口：Go 明确接收 Python 已定义的输出定位 `step_key/output_role/item_key`；保持闭合 JSON 校验，不通过忽略未知字段绕过合同。

## 失败与审查

模型前明确拒绝与结果未知分开记录，失败码不泄露输入。保留人工门；无语义依据不得自动批准。当前旧任务、旧 release、数据库历史不通过清库或兼容放宽处理。每个修复的测试与运行证据写入 0018 验收，未完成的全局人物/场景能力明确列出。

## 真实输出后确认的后续缺口

第一集真实候选已生成，但不能因此宣布语义验收通过。同一个 24 小时前时间线在第二、三场被赋予不同 time_branch，第三场的 presentation 又标为 present。结构校验与引文匹配只证明来源合法，没有证明时间理解一致；模型还输出了“schema 缺少 required”的错误自检结论，实际 schema 存在该字段，不能把模型的自述当成工程事实。

下一切片应在既有所有权内细化：固定原稿对应的时间分支及转场证据；连续场次继承已确认分支，跨集未知关系保留待决，不凭英文 key 字面相似归并。审阅发现错误时应创建绑定原草案与原输入摘要的修订提案，再作新的审阅决定，不能修改不可变草案、伪造原稿修改或重复创建已采纳的 60 集。该修订协议和全稿有界归并需要独立的可审阅细化，本轮没有把它伪装成已经实现。

目前 build_world 仍把全部原文和全部逐集候选装进一个 240000 字节上下文。无损压缩解决了 map_manuscript 的实际阻断，并未解决世界设定阶段的长稿容量。第一集候选已达 30090 字节；必须用全量完成后的实际体积验证，不能提高上限后宣称解决或丢弃人物/场景事实。现有串行逐集执行也会使 60 集耗时较长，本轮没有改变正在执行的 Temporal 历史和顺序。

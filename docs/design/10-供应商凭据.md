# DES-10 供应商凭据

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-07 供应商凭据](../requirement/07-供应商凭据.md)：ACC-02 管理员配置模型供应商凭据 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 管理 · 供应商凭据 |
| 基础设计 | DES-02 §5.3；DES-07 §5 |
| 依赖功能设计 | [DES-09](09-账号与会话.md)、[DES-11](11-模型注册表与价格.md)、[DES-12](12-审计日志.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-07](../requirement/07-供应商凭据.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-07 §2 的规则）与验收标准以 REQ-07 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `catalog.provider`、`catalog.provider_credential` |
| 接口 | `POST /api/admin/providers/{id}/credentials`<br>`GET /api/admin/providers/{id}` |
| 工作流与任务 | `CredentialTestWorkflow` |
| 领域事件 | `catalog.credential_changed.v1` |
| 测试用例 | TC-07-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

| 表 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `catalog.provider` | `key`、`adapter_key`、`region`、`concurrency_limit`、`rate_limit_per_min`、`status` | 适配器选择、境外标注、限流配置 |
| `catalog.provider_credential` | `ciphertext`、`key_id`、`last4`、`status`、`last_tested_at`、`last_test_result` | 加密存储与状态展示；部分唯一索引保证 R1 |

## 3. 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET / POST | `/api/admin/providers` | 列表 / 新增供应商 |
| PATCH | `/api/admin/providers/{id}` | 修改并发、速率、状态（Rev） |
| POST | `/api/admin/providers/{id}/credentials` | 设置凭据 |
| POST | `/api/admin/providers/{id}/credentials/test` | 测试当前启用的凭据 |
| POST | `/api/admin/providers/{id}/credentials/{cid}/disable` | 停用 |

```http
POST /api/admin/providers/{id}/credentials
{ "label": "方舟主账号", "secret": { "api_key": "…" } }
201 { "id": "…", "label": "方舟主账号", "last4": "a9F2", "status": "active", "last_test_result": null }
```

`secret` 为对象，不同供应商字段不同（方舟：`api_key`；MiniMax：`api_key`、`group_id`；OpenRouter：`api_key`），字段定义由适配器声明、在 `GET /api/admin/providers/{id}` 的 `credential_schema` 中返回。

## 4. 异步与工作流

- 测试：`backend-api` 直接执行一个短工作流 `CredentialTestWorkflow` → Activity `provider.test_credential`（`agent` 队列，超时 15 秒，不重试）。
- 工作流在调用供应商 Activity 前读取当前启用凭据的密文并随 Activity 输入传入；`agent-worker` 用私钥解密，明文只保存在进程内存中，缓存 5 分钟（DES-07 §5.2）。

R6 的实现：任务在每个 Activity 开始时读取当前启用的凭据；停用后新提交的 Activity 返回不可重试错误 `credential_disabled`，已提交的任务的查询 Activity 继续使用提交时的凭据 ID 直至完成。

## 5. 事件与实时

`catalog.credential_changed.v1`（新增、停用、测试结果）→ audit、注册表缓存失效。

## 6. 界面

供应商列表：名称、区域、凭据状态（末 4 位、最后测试结果与时间）、模型数量；凭据对话框为密码输入框，保存后不可查看。

## 7. 权限与审计

仅管理员。审计 action：`provider.created`、`provider.updated`、`credential.set`、`credential.disabled`、`credential.tested`；只记录 `last4`。

## 8. 非功能要求

SEC-04（凭据不出现在日志、响应、前端、追踪中，CI 扫描验证）；OBS-02（日志脱敏）。

## 9. 测试设计

- 单元：封装 / 解封；`secret` 按适配器 schema 校验。
- 集成：停用后新报价失败、进行中任务继续查询；日志与响应敏感词扫描。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-10-Q1 | 生产环境解密私钥是否托管在火山引擎 KMS（原 REQ-07-Q1）？ | 生产用 KMS 托管私钥，`agent-worker` 通过 KMS 解密数据密钥；开发环境用本地密钥文件。 | M1 开始前 |
| DES-10-Q2 | 密钥轮换时，已存凭据密文如何迁移？ | 按 `key_id` 区分：新写入用新公钥；后台任务按 `key_id` 分批重新封装；旧私钥保留到迁移完成后再停用。 | M1 实施中 |
| DES-10-Q3 | `agent-worker` 内明文缓存 5 分钟，停用凭据后最长 5 分钟内仍可能被使用。是否接受？ | 不接受这个延迟：`catalog.credential_changed.v1` 事件广播使缓存立即失效，5 分钟 TTL 只作兜底。 | M1 实施中 |
| DES-10-Q4 | 部分供应商没有免费的连通性测试接口，如何测试？ | 适配器声明 `test_method`（列出模型 / 查询余额 / 无）；为“无”时只校验字段格式，并提示“首次生成时才能确认凭据可用”。 | P0（接入各供应商时） |

需求层面的待确认问题见 [REQ-07](../requirement/07-供应商凭据.md) 的“待确认”。

# DES-38 画布功能

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-36 画布](../requirement/36-画布.md)：CNV-01 项目画布；CNV-02 画布发起生成；CNV-03 画布参考连线；CNV-04 布局保存；CNV-05 撤销重做、复制粘贴、快捷键；CNV-06 画布性能；CNV-08 风格与镜头语言素材节点 |
| 优先级 | MVP（首个版本，PRD-01 P27） |
| 里程碑 | M5（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 画布 |
| 基础设计 | DES-06；DES-02 §5.12 |
| 依赖功能设计 | [DES-21](21-分镜生成与镜头编辑.md)、[DES-22](22-生成模式与参考组合.md)、[DES-24](24-报价与二次确认.md)、[DES-25](25-多候选与选定.md)、[DES-33](33-媒体库.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-36](../requirement/36-画布.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-36 §2 的规则）与验收标准以 REQ-36 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `canvas.command_log`、`canvas.document`、`canvas.document_snapshot`、`canvas.edge`、`canvas.node` |
| 接口 | `GET /api/projects/{pid}/canvases`<br>`POST /api/projects/{pid}/canvases`<br>`GET /api/canvases/{id}`<br>`GET /api/canvases/{id}/changes`<br>`POST /api/canvases/{id}/commands`<br>`POST /api/canvases/{id}/nodes/{nid}:run`<br>`POST /api/canvases/{id}/snapshots` |
| 工作流与任务 | `OperationWorkflow` |
| 领域事件 | `canvas.document_changed.v1` |
| 测试用例 | TC-36-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

`canvas.document`（`revision`、`scope`、`viewport`）、`canvas.node`（`node_type`、`node_action`、`ref_type`、`ref_id`、`config`、坐标尺寸、`parent_id`、`last_operation_id`）、`canvas.edge`（`edge_type`、`role`、`binding`）、`canvas.document_snapshot`；见 DES-02 §5.12。新增 `canvas.command_log`（`document_id`、`revision`、`commands jsonb`、`create_by`）用于冲突重放与审计（DES-02 同步）。

## 3. 接口

```http
GET  /api/projects/{pid}/canvases                  → 画布列表
POST /api/projects/{pid}/canvases                  { "name": "第 1 集探索", "scope": { "episode_id": "…" } }
GET  /api/canvases/{id}                            → 文档、节点、连线、revision，以及业务节点的摘要数据
GET  /api/canvases/{id}/changes?since_revision=12  → 增量
POST /api/canvases/{id}/commands
{ "expected_revision": 12, "commands": [
    { "type": "AddNodes", "nodes": [ { "id": "…", "node_type": "video", "node_action": "generate", "config": { "capability": "video.generate", "mode": "omni_reference", "model_key": "…" }, "x": 100, "y": 200 } ] },
    { "type": "Connect", "edge": { "id": "…", "edge_type": "reference", "source_node_id": "…", "target_node_id": "…", "role": "camera" } },
    { "type": "MoveNodes", "moves": [ { "id": "…", "x": 320, "y": 80 } ] } ] }
200 { "revision": 13, "results": [ { "ok": true }, { "ok": true, "business": { "shot_id": "…", "shot_version_no": 7 } }, { "ok": true } ] }
409 revision_conflict { "meta": { "current_revision": 14 } }
POST /api/canvases/{id}/nodes/{nid}:run            → 报价（REQ-21）
POST /api/canvases/{id}/snapshots
```

## 4. 异步与工作流

生成节点运行复用 OperationWorkflow；无新工作流。

## 5. 事件与实时

`canvas.document_changed.v1`；SSE `canvas.updated`（其他标签页或 Agent 修改后拉取增量）；业务对象事件驱动节点内容刷新。

## 6. 界面

React Flow 引擎 + 移植 infinite-canvas 卡片与交互（DES-08 §7）；左侧节点库、右侧属性面板、顶部工具栏、小地图；快捷键见 REQ-05 §7。

## 7. 权限与审计

制作者；布局命令不审计，业务命令按对应功能审计（`origin = canvas`）。

## 8. 非功能要求

PERF-06（500 节点 ≥ 50 fps、打开 ≤ 3 秒）；REQ-05 §4.8（两侧修改 2 秒内可见）。

## 9. 测试设计

命令冲突重放；reference 连线与参考组合双向一致；Playwright 性能录制。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-38-Q1 | 画布技术方案 PoC 结论（React Flow + 移植 infinite-canvas，原 REQ-36-Q1）。 | P0 并行完成 PoC；未达到 PERF-06 时回退到纯 React Flow 自绘卡片。 | P0 结束时 |
| DES-38-Q2 | infinite-canvas 源码的许可是否允许移植？ | 移植前核实许可；不允许时只参考交互设计，不复制代码。 | P0 |
| DES-38-Q3 | `canvas.command_log` 保留多少？ | 保留最近 1,000 条或 7 天；生成快照后截断更早的记录。 | M5 实施中 |

需求层面的待确认问题见 [REQ-36](../requirement/36-画布.md) 的“待确认”。

# DES-13 项目管理

| 项 | 内容 |
| --- | --- |
| 对应需求 | [REQ-10 项目管理](../requirement/10-项目管理.md)：PRJ-01 创建项目；PRJ-02 项目列表与概览；PRJ-06 归档与删除；PRJ-07 生成策略设置 |
| 优先级 | MVP |
| 里程碑 | M1（[PLN-01](../plan/01-实施路线与交付计划.md)） |
| 页面 | 项目列表、项目概览、项目设置 |
| 基础设计 | DES-02 §5.2；DES-04 §10.3 |
| 依赖功能设计 | [DES-09](09-账号与会话.md)、[DES-11](11-模型注册表与价格.md)、[DES-14](14-项目预算.md) |
| 状态 | 草案（待评审，2026-09-25） |

本文把 [REQ-10](../requirement/10-项目管理.md) 拆解为实现设计。需求目标、业务规则（文中 R1、R2… 指 REQ-10 §2 的规则）与验收标准以 REQ-10 为准；表结构以 [DES-02](02-领域与数据模型.md)、接口签名以 [DES-03](03-接口设计.md)、工作流与生成操作以 [DES-04](04-工作流与生成操作.md) 为准，本文说明该功能用到哪些部分、如何组合。设计发现需求问题时先改需求（[文档规则](../README.md#文档规则) 2）。

## 1. 设计清单

| 类别 | 内容 |
| --- | --- |
| 数据表 | `billing.budget`、`workspace.project`、`workspace.style_preset` |
| 接口 | `POST /api/projects`<br>`GET /api/projects/{pid}/overview`<br>`DELETE /api/projects/{pid}` |
| 工作流与任务 | `ProjectPurgeSchedule` |
| 领域事件 | `workspace.project_changed.v1` |
| 测试用例 | TC-10-xx（[TST-02](../test/02-需求追踪矩阵.md)） |

## 2. 数据

| 表 / 存储 | 关键字段 | 为什么需要 |
| --- | --- | --- |
| `workspace.project` | `aspect_ratio`、`style_type`、`style_subtype`、`style_preset_id`、`allow_overseas_models`、`default_models`、`aigc_mark_style`、`status`、`is_delete`、`delete_time`、`purge_after` | 项目规格与生命周期；触发器保证 R2 |
| `workspace.style_preset` | `prompt_fragment`、`negative_prompt`、`reference_asset_ids` | 生成时自动带入风格 |
| `billing.budget` | 创建项目时同事务创建 | REQ-11 |
| Redis `overview:{project_id}` | 集 × 阶段计数 | 概览性能 |

概览 6 个阶段的数据来源：

| 阶段 | 完成条件（按集） | 来源 |
| --- | --- | --- |
| 场次确认 | `episode.parse_status = confirmed` | script |
| 资产定稿 | 本集用到的参考槽位全部锁定 | bible.episode_asset_usage + reference_slot |
| 分镜 | 所有场 `scene_board.status = confirmed` | storyboard |
| 关键帧 | 图生视频镜头已选关键帧数 / 图生视频镜头数 | storyboard.shot |
| 视频 | 已选片段镜头数 / 镜头数 | storyboard.shot |
| 配音 | 已选音频台词数 / 台词数 | audio.line_audio_selection |

## 3. 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/projects?status=active|archived|deleted&q=` | 列表 |
| POST | `/api/projects` | 创建 |
| GET / PATCH | `/api/projects/{pid}` | 详情 / 修改（Rev） |
| GET | `/api/projects/{pid}/overview` | 概览 |
| POST | `/api/projects/{pid}/archive`、`/unarchive` | 归档 / 恢复 |
| DELETE | `/api/projects/{pid}` | 删除（Rev） |
| POST | `/api/projects/{pid}/restore` | 从回收恢复 |
| GET | `/api/style-presets?style_type=&style_subtype=` | 风格预设 |

```http
POST /api/projects
{ "name": "逆光", "aspect_ratio": "9:16", "style_type": "stylized", "style_subtype": "guofeng_xianxia",
  "style_preset_id": "…", "description": "" }
201 { "id": "…", "name": "逆光", "aspect_ratio": "9:16", "style_type": "stylized", "style_subtype": "guofeng_xianxia",
      "resolution": "1080p", "allow_overseas_models": false, "status": "active", "revision": 1, "create_time": "…", "update_time": "…" }

GET /api/projects/{pid}/overview
200 { "episodes": [ { "episode_id": "…", "seq_no": 1, "title": "…",
        "stages": { "scenes": [1,1], "assets": [12,14], "storyboard": [3,3], "keyframes": [8,10], "videos": [12,30],
                    "dialogue": [40,48] } } ],
      "budget": { "limit_micros": …, "available_micros": …, "used_ratio": 0.42 },
      "attention": { "failed": 2, "unknown": 0, "manual": 1, "stale_shots": 5 } }

DELETE /api/projects/{pid}   409 state_conflict { "meta": { "inflight_operations": [ … ] } }
```

## 4. 异步与工作流

- `ProjectPurgeSchedule`（每日）：对 `purge_after < now()` 的项目执行分批清理工作流（删除对象存储前缀、删除各 schema 数据），可断点续跑。
- 概览物化：`realtime` 之外新增 `overview` 消费者，按事件增量更新 Redis。

## 5. 事件与实时

`workspace.project_changed.v1`（创建、修改、归档、删除、恢复）→ audit、realtime。

## 6. 界面

项目列表（卡片 / 表格切换、状态筛选、回收站视图）；新建项目对话框（风格类型切换后显示子风格与预设缩略图）；项目概览矩阵（单元格点击跳转）；设置页（画幅与风格只读并说明原因）。

## 7. 权限与审计

MVP 所有制作者可访问组织内全部项目。审计：`project.created`、`project.updated`、`project.archived`、`project.unarchived`、`project.deleted`、`project.restored`、`project.purged`。

## 8. 非功能要求

PERF-01（概览首屏 ≤ 2 秒）；PERF-03（进度更新 ≤ 2 秒）；PRV-05（彻底删除含媒体）。

## 9. 测试设计

- 单元：概览阶段计算；生命周期状态机。
- 集成：清理工作流中断后续跑；触发器拒绝修改画幅。

## 10. 待确认

| 编号 | 问题 | 默认方案（确认前按此实施） | 确认时机 |
| --- | --- | --- | --- |
| DES-13-Q1 | 概览用事件增量物化到 Redis，实现和运维成本较高。MVP 是否先用 SQL 聚合？ | 按本文事件增量物化；M1 可以先用 SQL 聚合加 30 秒缓存，M3 压测不达标（PERF-01）时再切换为增量物化。 | M3 压测后 |
| DES-13-Q2 | 画幅、风格创建后不可修改，只在应用层校验，还是同时加数据库触发器？ | 两层都做：应用层返回友好错误，触发器兜底。 | M1 开始前 |
| DES-13-Q3 | 项目彻底清理时，审计记录保留而项目数据删除，审计中的对象链接如何显示？ | 审计保留 `object_label` 快照；对象已清理时显示“已删除”，不可跳转。 | M1 实施中 |

需求层面的待确认问题见 [REQ-10](../requirement/10-项目管理.md) 的“待确认”。

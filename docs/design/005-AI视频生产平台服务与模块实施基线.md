# AI 视频生产平台服务与模块实施基线

> Design ID：DES-005
> 状态：提案 v1，待产品、架构与研发联合评审
> 日期：2026-08-22
> 需求基线：[目标需求总览](../requirement/000-AI视频生产平台目标需求总览.md)
> 上游设计：[系统架构](./000-AI视频生产平台目标系统架构设计.md)、[核心领域与数据](./001-AI视频生产平台核心领域与数据模型设计.md)、[产品模块](./002-AI视频生产平台目标产品与功能模块设计.md)、[接口与工作流](./003-AI视频生产平台接口工作流与功能实现设计.md)
> 模块细化：[M01—M15 模块详细 Design](./modules/README.md)

## 1. 结论

Lanverse 当前需要的是 **15 个逻辑业务模块、6 类按切片启用的安全隔离进程入口、5 项基础设施能力和少量外部适配能力**。它们不是 15 个微服务，也不是 26 个独立仓库。

首期继续采用 Python 模块化单体：一套领域模型、一套应用命令、一套 PostgreSQL 权威事实，通过不同进程入口隔离公网请求、调度对账、外部 Provider、Agent 和媒体执行。模块决定“谁拥有业务事实”，进程角色决定“哪类工作在哪里执行”，二者不能互相替代。

竞品报告中“Agent + 画布”“批量生成”“多模型”“商业路线”等表述只作为市场证据。当前产品承诺只包括：统一项目事实、可审阅提案、冻结生成计划、版本化参考与候选、可恢复外调、局部修复、审阅及不可变交付。公共市场、商业计费、一键整季、自动发布、完整 NLE 和按报告月份建设的路线图不进入本设计。

## 2. 统一术语与边界

| 概念 | 回答的问题 | 权威位置 | 当前数量 |
| --- | --- | --- | --- |
| 产品能力 | 用户最终能完成什么 | Requirement、PRD | A—F 纵向切片 |
| 逻辑模块 | 哪个模块拥有事实、规则和写入 | M01—M15 Design | 15 |
| 运行服务/进程角色 | 长短任务在哪个故障边界执行 | 系统架构、本设计 | 6 类 |
| 基础设施能力 | 权威存储、投递、密钥和观测依赖什么 | 系统架构、部署配置 | 5 项 |
| 外部能力 | 通过什么受控适配器调用模型、通知或身份系统 | M07、M08、M14、M15 | 按 Gate 启用 |
| 页面/画布 | 用户从哪里查看和提交相同命令 | 产品设计、模块 Design | 不是事实源 |

以下规则不可违反：

1. 一个逻辑模块可以被多个进程调用，但同一业务事实只有一个写入所有者。
2. 一个运行进程可以承载多个模块的后台任务，但不得因此接管模块审批、主选或交付决定。
3. 前端、Agent、API、Webhook 和 Worker 必须走相同应用端口、权限、幂等和治理规则。
4. 队列、缓存、LangGraph checkpoint 和对象存储路径都不是业务完成事实。

## 3. 必需运行服务

### 3.1 六类生产进程角色

| 服务 ID | 生产进程角色 | 必须承担 | 明确禁止 | 主要模块 | 首次需要 | 当前代码判断 |
| --- | --- | --- | --- | --- | --- | --- |
| SVC-01 | API / Webhook / SSE | 鉴权、查询、短事务、回调验签、创建 Operation/Outbox、投影状态 | 文档解析、模型推理、长轮询、FFmpeg、对象存储长 I/O | M01—M15 的 transport/application 入口 | A | API 已有；仍与多个 Worker 共用 `server.py` 生命周期；缺目标 Header/SSE 契约 |
| SVC-02 | Operation / Outbox / Scheduler / Reconciler | 拥有通用 Operation/Task/恢复，发布 Outbox、到期调度、lease 恢复、unknown 对账、公开恢复命令 | 猜测外部成功、直接改业务表、替用户批准 | M08 拥有执行事实；M01—M15 按需提交长操作、定时、投影或投递意图并保留各自结果所有权 | A 骨架；C 完整 | Scheduler/Outbox 部分存在；缺统一 Operation/Job/Attempt 与 Reconciler |
| SVC-03 | Import / Document Worker | 解包、DOCX/Markdown/TXT 解析、结构提取、Gate 后受限 OCR | 公网外呼、读取 Provider/KMS 凭据、写业务 current | M03，提交 M09 来源引用 | A | 现有 I/O Worker 可拆出解析入口；当前解析仍有事务内对象存储 I/O |
| SVC-04 | Provider Worker | 能力探测、Provider 提交/查询/取消、结果下载、回调后续处理 | 解析不可信文档、自动重试 unknown、自动主选、绕过 M07/M14 | M07、M08、M09 | C | 现有 I/O Worker/consumer 有骨架；真实生成固定返回 provider 不可用 |
| SVC-05 | Agent / Evaluation Worker | AgentRun、只读 Tool、结构化 Proposal、AI 质量评估、checkpoint | 直接写领域表、执行生成、接受提案、批准风险 | M06、M10，读取 M03—M05 | B Agent；D 评估 | 仅有进程内 harness；缺独立入口、持久 Run/Proposal/checkpoint |
| SVC-06 | Media / Delivery Worker | Animatic 派生/构建、probe、代理、抽帧、转码、确定性检查、基础装配、渲染和输出校验 | 自动选择候选、关闭问题、批准交付 | M05 业务事实下的媒体执行、M09、M10 确定性检查、M13 | A 代理/预演；E 交付 | 媒体底座和分镜导出部分存在；外部 I/O 仍发生在数据库事务内 |

目标拓扑共有六类安全隔离入口，但按切片启用：A 部署 SVC-01/02/03/06，B 增加 SVC-05，C 增加 SVC-04。某入口一旦启用，就必须具有独立生产生命周期、队列、身份、网络策略和健康检查。SVC-03 与 SVC-04 可以复用镜像依赖，但 Import 无默认公网/KMS 权限，Provider 仅允许名单外网和按连接读 KMS，二者绝不共用进程身份。

通知、Webhook 投递和邮件/短信适配首期作为 SVC-02 消费 Outbox 的适配器，不单独建设“通知微服务”。只有独立吞吐、合规或供应商故障边界得到证据后再拆分。

### 3.2 五项基础设施能力

| 基础设施 ID | 能力 | 是否首期必需 | 权威用途 | 不得承担 |
| --- | --- | --- | --- | --- |
| INF-01 | PostgreSQL + 有序 Migration + RLS | 必需 | 领域事实、Operation/Job/Attempt、Outbox/Inbox、审计和投影 | 直接存放大媒体；以 `create_all` 代替生产迁移 |
| INF-02 | 私有版本化 S3/MinIO 对象存储 | 必需 | 来源原件、媒体、代理、模型原响应和交付文件 | 以对象路径替代 Candidate、Selection 或 DeliverySnapshot |
| INF-03 | RabbitMQ | 必需 | 至少一次任务投递、背压、死信和消费者隔离 | 充当权威状态或业务审计 |
| INF-04 | Secret/KMS 服务 | 接真实 Provider 前必需 | Provider、Webhook、服务身份的可轮换密钥引用 | 在业务数据库保存可解密凭据材料 |
| INF-05 | OpenTelemetry + 指标/日志/Trace 后端 | 上线前必需 | operation→job→attempt→provider/artifact 的可观测链 | 记录来源正文、Prompt、凭据或跨租户敏感数据 |

Redis 不是首期必需。它只在限流、短期缓存、SSE fan-out 或实时协作有容量证据时引入，且不得保存权威任务或产品状态。

### 3.3 外部服务启用 Gate

| 外部服务 | 首次切片 | 启用前必须确认 | 降级路径 |
| --- | --- | --- | --- |
| 一个图像 Provider capability | C | 地域、保留/训练条款、输入输出契约、幂等/对账、用量单位、凭据和测试额度 | 阻止真实外调，保留 A 的 Fixture 路径 |
| 一个视频 Provider capability | C | 同上，并增加时长/参考/分辨率限制和异步任务语义 | 保留图像候选和手工占位，不宣称视频闭环通过 |
| Agent 模型 Provider | B | 结构化输出、Tool 白名单、Token 上限、数据外发与删除政策 | 全部手工工作台持续可用 |
| 通知/邮件 Provider | E；外部审片 F | 收件人授权、模板、退订/撤销、投递回执和敏感字段 | 站内待办和可复制受控链接 |
| 企业 OIDC/SSO | F | issuer、claim 映射、会话撤销、break-glass 和审计 | 本地受控身份；不得降低租户隔离 |
| 私有模型/LoRA | F 或明确企业试点 | 注册主体、版本、地域/权利、健康、回滚、用量和 Adapter 验收 | 停止新路由，保留历史计划和任务对账 |

“多模型路由”只有在两个同类能力完成淘汰原因、受控 fallback 和故障演练后才能宣称完成。首个图像能力加首个视频能力只能证明跨媒体能力接入，不能证明同类路由优化。

## 4. 平台级执行契约

以下是跨模块共享的运行语义，不新增 M16；其对象分别由 M01、M08、M11、M14 等权威模块拥有。

| 契约 | 必须明确的字段/规则 | 失败时用户结果 |
| --- | --- | --- |
| `CommandEnvelope` | command ID/version、workspace/project、actor、idempotency key、expected revision、trace | 冲突或重复回读原结果，不产生第二次副作用 |
| `Operation` | 用户意图、目标范围、状态、进度、最后证据、错误码、恢复动作 | 页面刷新/进程重启后仍可继续或接管 |
| `Job / Attempt` | 逻辑工作与单次尝试分离；Attempt 追加式；外部 ID/确定性持久化 | 超时进入 unknown/ReconciliationCase，不盲目重发 |
| `TaskEnvelope` | task/schema version、operation/job/attempt、最小对象引用、deadline、trace | 未知版本隔离/死信，不按旧消费者猜测执行 |
| `ResultPort` | 结果哈希、证据引用、expected state、幂等键、生产者身份 | 重复结果被去重，非法状态转移被拒绝 |
| `DomainEvent` | event ID/type/version、aggregate/version、occurred/recorded time、workspace、trace | 消费者可重放；未知字段前向兼容，未知枚举 fail closed |
| `ErrorContract` | 稳定 code、retryable、用户消息、evidence、recovery actions | 用户看到可执行下一步，不暴露厂商原始敏感错误 |

强一致写入必须在单一 PostgreSQL 短事务内完成；事务提交前禁止模型、对象存储、网络、FFmpeg 或文档解析。副作用通过 Outbox 触发，Worker 再通过受限结果端口提交证据。

## 5. 模块功能与运行映射

本节是评审总表；字段、状态、命令、页面、失败和验收细节以对应模块 Design 为准。

| 模块 | 用户结果与 P0 能力 | 拥有的核心事实 | 主要上/下游 | 运行服务 | 切片 | 当前实现判断 |
| --- | --- | --- | --- | --- | --- | --- |
| M01 组织、身份与访问 | Workspace 隔离、成员/项目职责、职责分离、服务身份 | Workspace、Membership、Role/PolicyRevision、ServiceIdentity | 所有模块鉴权；M14 审计/策略 | SVC-01；后台撤权事件 SVC-02 | A 最小/F 完整 | 部分：JWT/成员/能力映射已有；邀请、项目分配、服务身份、RLS 待补 |
| M02 项目组合与内容单元 | 项目/Brief 版本、拆集 Manifest 原子物化、内容单元、顺序、阻塞和进度 | Project、BriefRevision、ContentUnitMaterialization、ContentUnit、OrderRevision、ProgressProjection | M03 拆解；M03—M13 共享 scope | SVC-01/SVC-02 投影 | A | 部分：整本 EpisodePlan→Episode 原子物化已实现；需收敛 Manifest mapping、Brief/OrderRevision 和正式投影 |
| M03 导入与叙事理解 | 项目级整本来源、解析报告、剧集边界、证据锚点、每集结构/全局 Mention 校对与批准版本 | SourceRevision、ImportRun、ParseReport、EpisodeBreakdownRevision/Manifest、NarrativeDraft/Revision、ProductionElementMention、Anchor | M02 物化 scope；M04/M05 消费批准叙事 | SVC-01 创建；SVC-03 解析；SVC-05 仅提案 | A/B | 中等：整本文档/分集已实现，长剧本 Workflow 可 Reduce；正式提取仍只接单集，缺全局 occurrence/完整批准模型 |
| M04 生产知识与一致性图谱 | 实体身份、权威未决主体、零遗漏全剧生产需求、正式/未决人物×剧集矩阵、独立 readiness、状态/关系/规则/参考、范围和影响报告 | Entity、UnresolvedSubjectRevision、MentionResolution、ProductionCoverageSchemaRevision、ProductionRequirementRevision、ProductionCoverageDecision、Inventory/Readiness/EntityOccurrenceProjection、Profile/State/Relation/RuleRevision、ReferenceBinding、ImpactReport | M03 approved Mention/M02 Order/M09 media；M05/M07/M10/M13 消费 | SVC-01；inventory/readiness/occurrence/影响投影 SVC-02；AI 提案 SVC-05 | A 最小/D 闭环 | 部分：通用 Asset/State/Reference/人工 occurrence 有；缺权威未决成员集、script_candidate 自动关联、项目矩阵、独立需求/覆盖/准备度、CoverageSchema 与持久影响图 |
| M05 分镜与镜头规划 | Shot 稳定身份、拍法/顺序版本、覆盖、分镜和只读预演 | Shot、ShotPlanRevision、ShotOrderRevision、Coverage、AnimaticRevision | M03/M04；M07/M09/M13 | SVC-01；分镜媒体 SVC-06 | A/B | 中等：镜头/绑定/重排/导出有；顺序仍原地改，缺正式批准与 Animatic 版本 |
| M06 Agent 与可视化画布 | 可解释 Agent 提案、逐项接受、手工降级、同源画布 | AgentRun、Proposal envelope/evidence、CanvasView/Layout | 目标领域拥有 Proposal payload 与接受命令 | SVC-05；SVC-01 接受命令 | B 最小/F 画布 | 原型：harness 有；持久 Run/Proposal/checkpoint/独立 Worker 待建 |
| M07 生成规划、提示编译与路由 | 冻结输入、Prompt 编译、能力淘汰、资源/治理预检和批准 | GenerationPlan/Item、InputSnapshot、PromptCompilation、CapabilityRevision、RouteDecision | M04/M05/M11/M14；向 M08 创建 Job | SVC-01 计划；SVC-04 编译/能力探测 | A Fixture；C 完整；D 修复复用 | 脚手架：请求/preflight/hash 有；冻结计划、编译与路由待建 |
| M08 提供商与耐久执行 | Operation/Job/Attempt、提交/回调/轮询/取消、unknown 对账 | Operation、GenerationJob、Attempt、Submission/Receipt、ReconciliationCase、ProviderConnection | M07 批准；M09 摄取；M11 用量 | SVC-02/SVC-04 | A 骨架/C 真实 | 脚手架：任务/Outbox/配置有；真实 Provider 和对账对象缺失 |
| M09 媒体、候选与选择 | 媒体摄取完整性、代理/血缘、候选比较、追加式人工主选 | MediaArtifact/Derivative、Candidate/Set、SelectionDecision | M08 输出；M10 质量；M12/M13 使用选择 | SVC-04 下载；SVC-06 处理；SVC-01 决定 | A Fixture/C 真实 | 媒体底座较强；Candidate/Selection 业务对象尚未完整落地 |
| M10 质量、连续性与修复 | 技术/语义/连续性问题、证据、决议、风险接受、局部修复 | Evaluation、QualityIssue、IssueDecision、RepairPlan、QualityRiskAcceptance | M04/M09；RepairPlan 经目标模块/M07；M12/M13 门禁 | SVC-05 AI 评估；SVC-06 确定性检查；SVC-01 决议 | D | 未落地；不得把 M09 隔离或 M14 PolicyException 当成质量决定 |
| M11 资源配额与用量治理 | 估计、预留、实际/释放、上限、例外和归因 | UsageEnvelope、Estimate、Reservation、UsageEntry、UsageException | M06/M07/M08/M13 调用 | SVC-01 预检；SVC-02 对账/投影 | A 上限/C 完整 | 基础版：货币 reservation/cost 有；需改为多单位资源账本，非计费 |
| M12 审阅、协作与批准 | 冻结审阅包、时间码评论、决定、差异和站内通知 | ReviewPackage、Annotation、Thread、ReviewDecision、NotificationIntent | M09/M10/M13；M01 权限/M14 外部分享 | SVC-01；通知适配 SVC-02 | E；外部审片 F | 未落地；当前没有冻结审阅对象和页面 |
| M13 装配与交付 | 可编辑基础时间线、预检、RenderRun、Manifest、不可变 Snapshot | AssemblyRevision、TrackItem、Caption/AudioMix、DeliveryBuild/RenderRun/Snapshot | M05 顺序/M09 主选/M10/M12/M14 门禁 | SVC-01；SVC-06 渲染；SVC-02 编排 | E | 仅有可复用的分镜 Manifest/ZIP 基础；它仍属于 M05 导出，不证明 M13 装配、音频字幕或 Snapshot 已落地 |
| M14 权利、安全与治理 | 声明/同意、外发与交付门禁、策略例外、保留/法保、审计 | Rights/Consent、ProviderPolicy、PolicyEvaluation/Exception、Retention/LegalHold | 所有外发、分享和交付入口 | SVC-01 评估；SVC-02 到期；各 Worker fail closed | A/C/F | 部分：Consent/Proof/rights gate 有；Provider Policy、例外、保留待补 |
| M15 模板与集成接口 | 不可变模板、受控 API/Webhook、私有能力注册 | TemplateRevision/Release、ApiClient contract、WebhookSubscription/Delivery、Registration | M01 身份；M07 能力；M08 执行；M14 政策 | SVC-01；Webhook 投递 SVC-02 | F | 未落地；不得让集成直写数据库或形成第二套规则 |

## 6. 七阶段生产闭环

竞品分析中的七阶段流程校准为以下可验收链路。每一阶段都有明确事实、人工决定和失败恢复，不以页面跳转代替状态完成。

| 阶段 | 主要模块 | 完成事实 | 阻塞/恢复 |
| --- | --- | --- | --- |
| 1. 导入与拆集 | M02/M03/M09/M14 | project-scoped SourceRevision + ParseReport + approved EpisodeBreakdownManifest + stable ContentUnit/OrderRevision | 损坏/无标题/边界冲突/部分物化显示缺口；可局部恢复且不覆盖人工结果或暴露半成品 |
| 2. 结构化剧本与全局清单 | M03/M04 | approved NarrativeRevision + Entity/UnresolvedSubjectRevision + exact MentionResolution coverage + published ProductionCoverageSchemaRevision + ProductionCoverageDecision + immutable ProductionRequirementRevision + complete Inventory（unassessed=0）+ independent Readiness + revision-bound EntityOccurrenceProjection | 歧义/unresolved/incomplete/not_analyzed/failed scopes 保持可见；缺失/重叠人物决议阻止预检；Agent 不可用时有完整手工创建/决议命令；人物矩阵和清单均按固定基线回源并可重建 |
| 3. 设定资产 | M04/M09/M14 | 已发布 Profile/State/Reference 及有效范围；Requirement 与 Media 分离 | 缺引用、范围冲突或权利未知时阻止外发；接受需求不自动生成媒体 |
| 4. 分镜/预演 | M05/M09 | approved ShotPlan/Order + AnimaticRevision | 覆盖缺口可见；预演是 M05 派生只读结果，M13 仅在 E 读取其基线初始化装配 |
| 5. 提示编译与批准 | M07/M11/M14 | approved GenerationPlan + Prompt/Route/Preflight 快照 | 超限、能力不匹配或治理拒绝时 blocked，不静默降级 |
| 6. 批量生成与选择 | M08/M09/M11 | Job/Attempt 证据 + ready Candidate + SelectionDecision | unknown 先对账；部分失败保留成功项；评分不替代人工选择 |
| 7. 质量、审阅与交付 | M10/M12/M13/M14 | 问题决议 + approved ReviewPackage + DeliverySnapshot | 修复经新 M07 Plan；任何必需输出/门禁缺失都不创建 Snapshot |

Agent 和画布横跨阶段 2—6，但只提供提案、解释和操作视图；它们不是第八阶段，也不拥有另一套项目数据。

## 7. 纵向切片与服务启用顺序

| 切片 | 最小用户闭环 | 新启用服务能力 | 明确不宣称 |
| --- | --- | --- | --- |
| A 手工事实主线 | 整本来源→拆集物化→每集结构/全剧清单/人物矩阵→知识→镜头→Fixture Candidate→人工主选→只读 Animatic | SVC-01/02/03/06；INF-01—03 | Agent、真实 Provider、质量、正式审阅/交付 |
| B 受限 Agent | 结构/实体/拆镜 Proposal 逐项接受、拒绝、过期和手工降级 | 增加 SVC-05、Agent Provider；INF-04/05 | 完整自由画布、一句话直接改事实 |
| C 真实可恢复生成 | 冻结计划→图像/视频 Provider→unknown 对账→候选摄取→用量归因 | 增加 SVC-04；SVC-02 Reconciler、INF-04 | 多模型最优路由、全媒体能力 |
| D 质量与局部修复 | 检查→Issue→决议/风险→RepairPlan→分类执行→定向复检 | SVC-05 Evaluation、SVC-06 检查 | 自动评分替代主选、无批准直接修复 |
| E 审阅与不可变交付 | 冻结审阅→评论/批准→装配→Build→Snapshot | SVC-06 渲染、站内通知适配 | 外部匿名审片、完整 NLE |
| F 团队与扩展 | 项目角色、外部审片、完整画布、模板、API/Webhook/私有能力 | 通知/SSO/私有能力按 Gate | 公共市场、任意脚本、商业计费 |

切片 A 的 Fixture Candidate 必须经过同一最小 `GenerationPlanItem → Operation/Job/Attempt → Candidate` 契约；Adapter 标记为 fixture 且不产生外部用量。它不能由页面直接插入 Candidate，也不能伪装成真实 Provider 成功。

## 8. 模块 Design 的可实施完成标准

每个模块详细 Design 在进入接受评审前必须至少包含：

1. 模块目标、非目标、拥有对象和写入所有者；
2. 功能分解到类型化 Command/Query、输入、输出、权限和业务规则；
3. 状态迁移的前置条件、参与者、并发控制、副作用、超时与补偿；
4. 同步端口、事件 payload/version、幂等键和稳定错误/恢复动作；
5. 页面/工作台的 loading、empty、blocked、partial、unknown、stale 和 conflict 状态；
6. Requirement FR/NFR/AC 到接口、数据、测试和验收切片的逐项映射；
7. 当前代码按“已实现、需改造、新增、暂缓”分类，目标对象不得伪装成现状；
8. 至少一个成功闭环、一个权限负例、一个并发/重复负例和一个恢复场景。

模块 Design 保持 `proposed`，直到产品、设计、研发和验收责任人完成签认。文件存在、Mock 存在或代码有同名类，都不能证明模块已接受或已实现。

## 9. 实施前 P0 技术阻断

以下是当前代码与目标设计的上线阻断，需作为切片 A/C 的明确工作包，而不是新建业务模块：

1. 用有序、不可变 Migration 链替代生产 `create_all`，并建立 RLS policy/session context 与跨租户负向测试。
2. 将 API、Operation、Import、Provider、Agent/Evaluation、Media 变成六个安全隔离生产入口和健康边界；Import 无公网/KMS，Provider 只允许名单外网并按连接读 KMS。
3. 建立统一 Operation/Job/Attempt/Outbox/Inbox/Reconciliation 契约及稳定错误/恢复动作。
4. 把对象存储、probe、文档解析、Provider 和 FFmpeg 移出数据库事务。
5. 完成至少一个图像和一个视频 Provider 的真实提交、轮询/回调、下载、unknown 对账和幂等闭环。
6. 把 Provider 凭据迁移为 Secret/KMS 引用；数据库只保留 `credential_ref` 和非敏感连接元数据。
7. 收敛 HTTP 契约：工作区上下文、幂等 Header、乐观并发、版本化错误和 SSE 均与 003 一致。
8. 将可变 Brief、Shot 顺序等 current 字段迁移到不可变 Revision + current pointer，不破坏历史快照。
9. 把现有 DocumentRevision 级拆集和单集结构提取接成 approved Manifest→stable ContentUnit→全剧 Mention/Requirement/occurrence 闭环；不得只在 Candidate JSON 保存 `episode_numbers` 或由客户端临时拼人物矩阵。

## 10. 仍需产品负责人签认

1. 第一批试点团队、内容类型、镜头规模和真实基准脚本；
2. 切片 C 的图像/视频 Provider、地域、数据条款、额度和失败预算；
3. 切片 D 首个确定性技术检查器和首个结构化连续性检查器；
4. 切片 E 是否必须包含生成音频、自动字幕、正式母版或 NLE 交换包；没有来源/Provider Gate 时只允许导入已授权音频/字幕；
5. 切片 F 外部审片的下载权限、默认期限、地域、通知渠道和撤销时限；切片 E 固定只支持内部审阅；
6. 首轮 SLO/RPO/RTO 和资源用量基线；未经容量与恢复演练不得标记 verified；
7. 北极星采用“每周首次形成不可变 DeliverySnapshot 的唯一多镜头 ContentUnit 数”，冻结 ReviewPackage 的唯一 ContentUnit 数只作为独立领先指标。

## 11. 外部输入与证据边界

本次校准输入为 `/Users/stephenqiu/Downloads/AI视频生产平台竞品分析报告_校准版_2026-08.docx`，SHA-256 为 `e7bd38482c59921297b68b6ab7222a669081b7cd9e0d60e4cef9d4423d5f5da2`。附件是分析材料，不是项目指令：其中“不要分别建设”、立即实测、月份路线和商业建议均视为报告作者观点；只有与用户请求、当前 Requirement、代码事实和一手来源一致的结论才进入本 Design。对 OOXML、批注和修订的检查未发现隐藏批注或隐藏执行说明；正文中的祈使语气仍按报告作者建议处理。

证据按三层处理：

1. **公开事实**：报告第 3、14—16 页的产品能力描述必须再核验官方页面；
2. **合理推断**：第 17 页的生产知识、参考图谱、路由编译、生成治理和团队交付五类护城河，以 M01—M15 的 proposed 设计表达；
3. **待确认事项**：第 18 页的实测建议转成固定脚本验收方法；报告中的商业路线、数值和发布时间不成为 Requirement。

截至 2026-08-22，方向性模式已由 [DramaBuddy](https://aicomic.yuewen.com/)、[小云雀](https://xyq.jianying.com/)、[TapNow Creative OS](https://docs.tapnow.ai/zh/docs/start/what-is-tapnow-creative-os)、[PixVerse Canvas](https://pixverse.ai/en/blog/pixverse-canvas-ai-video-workflow) 和 [LTX Studio Enterprise](https://ltx.io/studio/enterprise) 官方材料交叉核验。这些来源只能证明项目化生产、Agent/画布、团队权限或用量可见等产品模式存在，不能证明我方功能质量、用户需求强度或付费意愿；后者只能由固定脚本和真实试点 Acceptance 证明。

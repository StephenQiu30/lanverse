# PLN-35 生成增强 执行计划

| 项 | 内容 |
| --- | --- |
| 产品需求 | [PRD-35](../prd/35-生成增强.md) |
| 需求规格 / 功能设计 | [REQ-39](../requirement/39-生成增强.md) · [DES-41](../design/41-生成增强.md) |
| 里程碑 | M4（[PLN-01](01-实施路线与交付计划.md)） |
| BACKLOG Epic | E-39（[BACKLOG](../../BACKLOG.md)） |
| 前置计划 | E-08（[BACKLOG](../../BACKLOG.md)）、E-19（[BACKLOG](../../BACKLOG.md)）、E-22（[BACKLOG](../../BACKLOG.md)）、E-26（[BACKLOG](../../BACKLOG.md)）、E-27（[BACKLOG](../../BACKLOG.md)）、E-28（[BACKLOG](../../BACKLOG.md)） |
| 联调 | 无 |
| 状态 | 待办 |

## 1. 目标与完成定义

完成即 TC-39-01, TC-39-02, TC-39-03, TC-39-04 通过，[DES-41](../design/41-生成增强.md) 的测试设计及非功能要求有证据；依 [PLN-02 §3.3](02-项目管理与变更.md#33-完成定义dod) 完成格式、静态检查、单元/集成、Race、漏洞、契约、演示与追踪矩阵门禁。

## 2. 前置条件

- 前置 Epic：E-08（[BACKLOG](../../BACKLOG.md)）、E-19（[BACKLOG](../../BACKLOG.md)）、E-22（[BACKLOG](../../BACKLOG.md)）、E-26（[BACKLOG](../../BACKLOG.md)）、E-27（[BACKLOG](../../BACKLOG.md)）、E-28（[BACKLOG](../../BACKLOG.md)）；按 [BACKLOG](../../BACKLOG.md) 对应里程碑的“Epic 实施顺序”执行。
- 底座与外部条件：PLN-01 对应里程碑的命令层、真实依赖与运行环境；测试数据为相邻镜头、首尾帧、手部蒙版和两个模型。
- 待确认：DES-41-Q1、DES-41-Q2、DES-41-Q3。确认前不把默认方案写成已批准结论。

## 3. 任务分解

清单范围以 [DES-41](../design/41-生成增强.md) §1 和 [BACKLOG](../../BACKLOG.md) E-39 为准；下表任务仅写实施动作及关键约束。

| 编号 | 任务 | 产出（代码目录 / 文件） | 依赖 | 验证方式 |
| --- | --- | --- | --- | --- |
| E-39-01 | 数据与领域模型：按 [DES-41 §2](../design/41-生成增强.md#2-数据) 落实表与仓储。 覆盖：`storyboard.reference_item`（E-18 建表，本 Epic 只加列或复用）、`storyboard.shot_version`（E-18 建表，本 Epic 只加列或复用）、`operation.operation_input`（E-21 建表，本 Epic 只加列或复用）。 | `backend/db/migrations/`、`backend/internal/storyboard/domain/`、`backend/internal/storyboard/adapter/postgres/` | 前置 Epic | 迁移、权限与真实库集成 |
| E-39-02 | 用例与接口：按 [DES-41](../design/41-生成增强.md) 的接口节实现清单端点；写操作经鉴权、幂等、版本、审计与 Outbox。 覆盖：`POST /api/shots/{id}/frames:extract-from-take`、`GET /api/shots/{id}/candidates`、`POST /api/projects/{pid}/quotes`、`POST /api/operations/{id}:confirm`。 | `backend/internal/storyboard/application/`、`backend/internal/storyboard/adapter/http/`、`backend/docs/` | 前置 Epic 与上一任务 | 契约、鉴权与错误路径 |
| E-39-03 | 异步、工作流与事件：按 [DES-41](../design/41-生成增强.md) 的异步节落实清单中的任务与事件；验证重试、去重和终态。 覆盖：`media.ExtractFrame`、`OperationWorkflow`。 | `backend/internal/storyboard/adapter/workflow/`、`backend/internal/storyboard/adapter/event/` | 前置 Epic 与上一任务 | 真实依赖、重试与事件回放 |
| E-39-04 | Agent 服务：适配 `frames2video` 与蒙版 `image.edit`；不支持模式不提交。按 Go 与 Python 各自定义的类型做契约测试。 覆盖：无独立设计清单标识。 | `agent/app/providers/` | 前置 Epic 与上一任务 | 契约与离线评测 |
| E-39-05 | 前端：按 [DES-41](../design/41-生成增强.md) 的界面节完成前镜片段、首尾帧与蒙版编辑入口；候选可按模型筛选。 覆盖：无独立设计清单标识。 | `frontend/src/features/storyboard/` | 前置 Epic 与上一任务 | 交互与端到端场景 |
| E-39-06 | 测试与验收：覆盖 [REQ-39](../requirement/39-生成增强.md) §3 与本计划 §5 的特有场景。 覆盖：无独立设计清单标识。 | `backend/tests/`、`agent/tests/`、`frontend/tests/` | 前置 Epic 与上一任务 | 验收用例与证据 |

数据访问范围（[DES-02](../design/02-领域与数据模型.md) DDL）：`storyboard.shot_version` 有 `project_id`；`storyboard.reference_item`、`operation.operation_input` 分别经镜头版本、Operation 继承项目范围。

## 4. 实施顺序

1. Red：先为 TC-39-01, TC-39-02, TC-39-03, TC-39-04 所依赖的业务规则、异常与跨边界路径写失败测试；E-39-06 在各任务中持续补齐测试与验收。
2. Green：按 E-39-01 → E-39-02 → E-39-03 落实数据、接口和异步组合，逐项让测试通过。
3. Refactor：保持测试通过，整理边界与错误路径；契约稳定后 E-39-04、E-39-05 可并行，最后合并验收证据。

## 5. 验证与验收

- 质量门禁：按 [PLN-02 §3.3](02-项目管理与变更.md#33-完成定义dod) 完成定义与 [TST-01](../test/01-测试策略.md) 执行；保留真实命令、环境与结果。
- 本功能特有验证：尾帧抽取精度、蒙版改图与原图保留；注册表模式映射、两模型费用与候选筛选；前镜改选后的过期传播。
- 验收用例：TC-39-01, TC-39-02, TC-39-03, TC-39-04（[TST-02](../test/02-需求追踪矩阵.md)）；数据：相邻镜头、首尾帧、手部蒙版和两个模型。
- AI 能力评测按 [TST-03](../test/03-AI评测方案.md) 使用对应数据集留证。

## 6. 风险与回退

- 模型不支持首尾帧或蒙版输入：隐藏不支持模式、允许切换已验证模型；不提交无法完整表达的请求。
- 前镜改选后下游仍引用旧尾帧：标记后镜过期并保留原候选；用户确认新参考后重新报价，不自动扣费重做。


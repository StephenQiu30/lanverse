# 切片 A：手工事实主线实施计划

> Plan ID：PLAN-A
> PRD：[PRD-A](../prd/001-切片A手工事实主线PRD.md)
> Design：M01、M02、M03、M04、M05、M07（Fixture Plan/Item）、M08、M09、M11、M14
> 状态：proposed

## 1. 前置 Gate

- 产品负责人接受 PRD-000/A 的试点内容、输入和退出标准；
- M01—M05 最小范围、M07/M08 Fixture 执行契约、Go 业务系统架构和最终数据库 Schema 策略评审通过；
- 准备一份有授权、包含不少于 3 集且覆盖同名/别名/地点复用/道具/服装的金标整本脚本，Fixture 媒体和预期 10—40 镜头范围；
- 开发/测试环境的 PostgreSQL、RabbitMQ 和对象存储可用。

## 2. 工作包顺序

| WP | 工作 | 主要证据 |
| --- | --- | --- |
| A1 | Go `api`、`operation-worker`、`import-worker`、`media-worker` 入口与健康检查 | 任一 Worker 停止不影响 API；Import 无默认公网/KMS；切片 A 不启动 Python Agent |
| A2 | Workspace/Membership/ProjectAssignment、RLS 和负向权限测试 | 跨租户 UI/API/Worker/媒体拒绝 |
| A3 | Project/Brief、ContentUnit/OrderRevision 与私有原子物化 | current/重排/归档并发；Manifest 第三项失败不暴露半成品，重试不重复 |
| A4 | 对象直传、project-scoped SourceRevision、DOCX/MD/TXT ParseReport/Anchor、EpisodeBreakdownRevision/Manifest | 原件 hash、10 集唯一覆盖、无标题/冲突、split/merge/move/rename、损坏/部分失败样本 |
| A5 | M02 mapping 接入、无需 Agent 的每集 Scene/Beat/Dialogue/Action/ProductionElementMention 创建/编辑/删除与 Narrative 批准 | Scene 不跨集、覆盖守恒、并发、Anchor 回跳、partial/incomplete、未受影响范围稳定；M03 不写 ContentUnit 顺序或正式实体 |
| A6 | M04 独占全局 Mention 消歧、UnresolvedSubjectRevision/MentionResolution、最小人物/地点/道具身份与状态、默认及可修订 ProductionCoverageSchemaRevision、ProductionRequirementRevision/CoverageDecision 手工写路径、Inventory/ReadinessProjection、EntityOccurrenceProjection/人物矩阵 | 跨集别名/同名反证；每个 approved character Mention 恰有 entity/active unresolved/explicit reject 一个 current 结果，缺失/重叠时 incomplete/preflight blocked；清空投影后未决行键/成员一致；Occurrence failed scopes/counters；零 unassessed；零 Requirement ref coverage row；Schema 换版 stale/历史重建；换装/唯一道具、mentioned_only、confirmed_absent/not_analyzed；Requirement/Media 分离与媒体变化只重建 readiness |
| A7 | Shot/ShotPlan/Order/覆盖、拆分合并和锁定 | 稳定 ID、谱系、顺序差异 |
| A8 | M07 Fixture Plan/Item、M08 Operation/Job/Attempt/Outbox 幂等和 M09 Fixture Candidate 摄取 | 固定输入 hash、零外部用量；重投/重启不重复 |
| A9 | Selection purpose、顺序 Animatic 和项目硬资源上限 | 主选唯一、预览不误选、上限拒绝 |
| A10 | 来源权利声明、审计、指标/Trace 和端到端 UI | PRD-A 全旅程录像/数据清单 |

## 3. 测试策略

每个 WP 先写领域/状态 Red 测试，再实现应用和数据库集成。必须使用真实 PostgreSQL 验证唯一约束、RLS、乐观锁和 Outbox；RabbitMQ 至少一次投递下验证重复消费；对象/文档/媒体使用小型授权 Golden 样本。

## 4. 故障与恢复验证

API 在 Outbox 提交后重启、Publisher 发布前后重启、Worker lease 过期、解析部分失败、媒体验证失败、并发批准/重排、成员停用、对象存储短暂不可用。每项保存 Operation、数据库、审计和用户恢复截图/响应。

## 5. 退出与停止条件

满足 PRD-A 九项退出条件并通过 AC-SYS-001/007/008 的 A 范围后关闭。若手工命令语义、跨租户隔离或 Operation 恢复不稳定，停止 B/C；不得让 Agent/真实 Provider 掩盖基础缺陷。

# PLN-26 台词配音 执行计划

| 项 | 内容 |
| --- | --- |
| 产品需求 | [PRD-26](../prd/26-台词配音.md) |
| 需求规格 / 功能设计 | [REQ-29](../requirement/29-台词配音.md) · [DES-32](../design/32-台词配音.md) |
| 里程碑 | M4（[PLN-01](01-实施路线与交付计划.md)） |
| BACKLOG Epic | E-29（[BACKLOG](../../BACKLOG.md)） |
| 前置计划 | E-13（[BACKLOG](../../BACKLOG.md)）、E-16（[BACKLOG](../../BACKLOG.md)）、E-21（[BACKLOG](../../BACKLOG.md)）、E-22（[BACKLOG](../../BACKLOG.md)）、E-30（[BACKLOG](../../BACKLOG.md)） |
| 联调 | 无 |
| 状态 | 待办 |

## 1. 目标与完成定义

完成即 TC-29-01, TC-29-02 通过，[DES-32](../design/32-台词配音.md) 的测试设计及非功能要求有证据；依 [PLN-02 §3.3](02-项目管理与变更.md#33-完成定义dod) 完成格式、静态检查、单元/集成、Race、漏洞、契约、演示与追踪矩阵门禁。

## 2. 前置条件

- 前置 Epic：E-13（[BACKLOG](../../BACKLOG.md)）、E-16（[BACKLOG](../../BACKLOG.md)）、E-21（[BACKLOG](../../BACKLOG.md)）、E-22（[BACKLOG](../../BACKLOG.md)）、E-30（[BACKLOG](../../BACKLOG.md)）；按 [BACKLOG](../../BACKLOG.md) 对应里程碑的“Epic 实施顺序”执行。
- 底座与外部条件：PLN-01 对应里程碑的命令层、真实依赖与运行环境；测试数据为48 句台词、空台词、多音字、文本与音色变更。
- 待确认：DES-32-Q1、DES-32-Q2、DES-32-Q3。确认前不把默认方案写成已批准结论。

## 3. 任务分解

清单范围以 [DES-32](../design/32-台词配音.md) §1 和 [BACKLOG](../../BACKLOG.md) E-29 为准；下表任务仅写实施动作及关键约束。

| 编号 | 任务 | 产出（代码目录 / 文件） | 依赖 | 验证方式 |
| --- | --- | --- | --- | --- |
| E-29-01 | 数据与领域模型：按 [DES-32 §2](../design/32-台词配音.md#2-数据) 落实表与仓储。 覆盖：`audio.dialogue_audio`（本 Epic 建表）、`audio.line_audio_selection`（本 Epic 建表）、`audio.line_tts_override`（本 Epic 建表）、`operation.operation`（E-21 建表，本 Epic 只加列或复用）。 | `backend/db/migrations/`、`backend/internal/audio/domain/`、`backend/internal/audio/adapter/postgres/` | 前置 Epic | 迁移、权限与真实库集成 |
| E-29-02 | 用例与接口：按 [DES-32](../design/32-台词配音.md) 的接口节实现清单端点；写操作经鉴权、幂等、版本、审计与 Outbox。 覆盖：`GET /api/episodes/{eid}/dialogue`、`PUT /api/episodes/{eid}/dialogue/{line_key}/override`、`POST /api/projects/{pid}/quotes`、`POST /api/episodes/{eid}/dialogue/{line_key}:select-audio`。 | `backend/internal/audio/application/`、`backend/internal/audio/adapter/http/`、`backend/docs/` | 前置 Epic 与上一任务 | 契约、鉴权与错误路径 |
| E-29-03 | 异步、工作流与事件：按 [DES-32](../design/32-台词配音.md) 的异步节落实清单中的任务与事件；验证重试、去重和终态。 覆盖：`OperationWorkflow`、`audio.selection_changed.v1`。 | `backend/internal/audio/adapter/workflow/`、`backend/internal/audio/adapter/event/`、`contracts/` | 前置 Epic 与上一任务 | 真实依赖、重试与事件回放 |
| E-29-04 | Agent 服务：实现逐句 TTS 与发音修正映射；覆盖参数参与输入指纹。按 `contracts/activities` 契约评测。 覆盖：无独立设计清单标识。 | `agent/app/providers/` | 前置 Epic 与上一任务 | 契约与离线评测 |
| E-29-05 | 前端：按 [DES-32](../design/32-台词配音.md) 的界面节完成按场台词表、逐句参数与试听选定；文本或音色变化显示过期。 覆盖：无独立设计清单标识。 | `frontend/src/features/audio/` | 前置 Epic 与上一任务 | 交互与端到端场景 |
| E-29-06 | 测试与验收：覆盖 [REQ-29](../requirement/29-台词配音.md) §3 与本计划 §5 的特有场景。 覆盖：无独立设计清单标识。 | `backend/tests/`、`agent/tests/`、`frontend/tests/` | 前置 Epic 与上一任务 | 验收用例与证据 |

数据访问范围（[DES-02](../design/02-领域与数据模型.md) DDL）：清单内四张表均有 `project_id`，仓储按项目限定。

## 4. 实施顺序

1. Red：先为 TC-29-01, TC-29-02 所依赖的业务规则、异常与跨边界路径写失败测试；E-29-06 在各任务中持续补齐测试与验收。
2. Green：按 E-29-01 → E-29-02 → E-29-03 落实数据、接口和异步组合，逐项让测试通过。
3. Refactor：保持测试通过，整理边界与错误路径；契约稳定后 E-29-04、E-29-05 可并行，最后合并验收证据。

## 5. 验证与验收

- 质量门禁：按 [PLN-02 §3.3](02-项目管理与变更.md#33-完成定义dod) 完成定义与 [TST-01](../test/01-测试策略.md) 执行；保留真实命令、环境与结果。
- 本功能特有验证：48 句批量 TTS、空台词与多音字；文本和音色变更后的过期标记；逐句覆盖参数参与结果复用判断。
- 验收用例：TC-29-01, TC-29-02（[TST-02](../test/02-需求追踪矩阵.md)）；数据：48 句台词、空台词、多音字、文本与音色变更。

## 6. 风险与回退

- 多音字或特殊字符读错：保留原台词和已选音频，提供逐句发音修正并重新报价生成。
- 音色或文本变更后旧音频误用：显示过期并阻止把旧版误当最新；回退时保留旧选定记录，恢复到用户确认的新候选后再清除标记。


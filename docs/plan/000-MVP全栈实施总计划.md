# PLAN-000 MVP 全栈实施总计划

- 状态：proposed
- 日期：2026-07-27
- 最近审查：2026-07-28
- 输入：[PRD 索引](../prd/产品需求索引.md)、[PRD-010 追踪矩阵](../prd/010-需求设计与产品任务追踪矩阵.md)、[DES-000 工程规范](../design/000-项目顶层结构与工程规范.md)、[DES-002 架构与技术选型](../design/002-人工智能短剧平台架构与技术选型.md)、[DES-MOD-001 模块总览](../design/模块设计/001-模块详细设计总览.md)
- 输出：S0–S6 的全栈工程任务、文件边界、Red 测试顺序、真实依赖验证和产品接受门禁
- 下游：当前工作包实现；完成后的 Acceptance 证据

## 1. 实施结论

本总 Plan 管理跨 PRD 的排序、技术基线和公共门禁；PLAN-001～PLAN-010 分别承接对应 PRD 的执行细节。全局仍以 S0–S6 纵向切片和 WP-00–WP-12 为唯一排序轴，以 69 个 PT 为产品接受单元，以本文的 `DEV-Sx-xx` 为跨模块工程执行单元。一个工程任务必须同时交付后端事实、OpenAPI/生成 client、需要的前端交互、失败恢复和自动化证据；不拆成“后端阶段”与“前端阶段”。

当前 Requirement、Design 和 PRD 均为 `proposed`，D-001（S0 基线接受）与 DEV-S0-01 owner 尚未关闭。因此当前可执行范围是 WP-00 的评审、负责人指派和只读环境预检；在产品/工程负责人接受 S0 所需范围和技术基线前，不创建 backend/frontend/deploy 脚手架，不将 S0 标记为已开工。D-006 不再循环阻塞脚手架：它必须在 DEV-S0-04 前确定 RabbitMQ/MinIO 的显式本机地址或 Compose profile，真实连接由 DEV-S0-04 验证。后续切片在各自进入 `ready` 前接受本切片对应的 Requirement、Design 与 PRD。

### 1.1 冻结的实施技术基线

| 领域 | 本 Plan 只允许的选型 |
| --- | --- |
| 后端 | Python 3.11.15 + uv 0.11.31；FastAPI/Uvicorn/Pydantic v2 |
| 数据 | PostgreSQL 18.4 + SQLAlchemy 2 async + asyncpg；仅空库 `metadata.create_all()`，不引入 Alembic |
| 异步与基础设施 | PostgreSQL Task/Attempt/Schedule/Fire + RabbitMQ/aio-pika；Redis cache-aside；MinIO，上线选定后才增 OSS adapter |
| 媒体 | FFmpeg/ffprobe 8.1.2；I/O 有界线程池与媒体子进程隔离 |
| 前端 | React 19 + TypeScript + Next.js App Router + Tailwind v4 + shadcn/ui + Radix UI；Redux Toolkit + RTK Query + axios |
| 契约与质量 | `@umijs/openapi` 1.13.15 生成 client；Ruff/Pyright/Pytest/Testcontainers/Vitest/Playwright；OpenTelemetry + JSON 日志 + Prometheus |
| 交付 | Docker/Compose + GitHub Actions；不引入 Kubernetes、微服务或第二任务系统 |

## 2. PRD 审查结果

| PRD | 已提取的工程输入 | Plan 落点 | 审查结论 |
| --- | --- | --- | --- |
| [PRD-001](../prd/001-MVP产品定义与范围.md) | MVP 用户结果、工作基线、非目标、D-001–D-007 | §3、§9、§10 | 范围可实施，但外部决策尚未关闭 |
| [PRD-002](../prd/002-MVP纵向交付切片.md) | S0–S6 用户闭环与退出条件 | §6–§8 | 切片顺序清晰，必须串行接受 |
| [PRD-003](../prd/003-MVP执行计划与追踪矩阵.md) | WP-00–WP-12、DoR/DoD、统一命令 | §3–§10 | `PT-PRJ-004/005` 为跨切片任务，在本 Plan 中补充持续交付点 |
| [PRD-004](../prd/004-基础业务模块产品规格.md) | identity/projects/media/governance 事实与交接 | §7 S1/S2/S5 | 不创建全局 common/service，横向能力仅通过公开契约使用 |
| [PRD-005](../prd/005-创作生产模块产品规格.md) | scripts/assets/storyboards/production 不可变生产链 | §7 S2–S4 | 生产顺序不得跳过稳定版本和人工决议 |
| [PRD-006](../prd/006-剪辑交付与平台保障产品规格.md) | editing 与 RabbitMQ/Redis/存储/调度/可观测 | §7 S0/S2/S4–S6 | 平台能力由首个真实用例拉动，不提前建通用框架 |
| [PRD-007](../prd/007-基础业务模块PRD任务.md) | 20 个基础业务 PT | §7、§8 | P1 成员管理排除在 MVP 外 |
| [PRD-008](../prd/008-创作生产模块PRD任务.md) | 25 个创作生产 PT | §7、§8 | S2 只建最小 Task 基线，Provider/费用在 S4 增量完成 |
| [PRD-009](../prd/009-剪辑交付与平台保障PRD任务.md) | 24 个剪辑与平台 PT | §7、§8 | S0 的 Redis/RabbitMQ 连接是技术启用项，不伪造新 PT；PT-STO-002 为条件门禁 |
| [PRD-010](../prd/010-需求设计与产品任务追踪矩阵.md) | 379 个需求叶子的 Design/PT/验收归属 | §8 | 完整性基线；实施时每个 PT 必须取回相应 ENT/FR/IF/NFR |

## 3. 准入、状态和责任

### 3.1 必须先关闭的决策

| 门禁 | 负责人 | 阻塞 | Plan 所需证据 |
| --- | --- | --- | --- |
| D-001 接受 MVP 范围/非目标、固定技术基线与 S0 工程边界 | 产品负责人/工程负责人 | S0 | REQ-001、DES-000/002、PRD-001/002/003 的 S0 评审记录与对应 commit |
| D-002 OIDC issuer/audience/测试租户/恢复 | 产品+后端 | S1 | 可验证 token 样例，secret 不入库 |
| D-003 Text Provider/参数/价格/额度 | 产品+AI | S2 | 能力与计价快照 |
| D-004 Image/Video/Speech Provider | 产品+AI | S4 | submit/query/cancel/计价契约 |
| D-005 地区/授权/审核/标识/留存 | 产品+合规 | S2/S5 | 适用规则、责任人和测试材料 |
| D-006 RabbitMQ/MinIO 提供方式 | 工程负责人 | DEV-S0-04/S0 退出；S4 | 本机优先/Compose profile、端口与 env 契约；真实连接由 DEV-S0-04 验证 |
| D-007 性能环境/剧本/预算/故障窗口 | 产品+开发+QA | S4/S6 | 固定 fixture、环境说明和演练时段 |

### 3.2 状态与责任边界

- PT 状态仍只按 PRD 使用 `proposed → ready → in_progress → accepted`，外部条件缺失时为 `blocked`；Plan 不另建第二套产品状态。
- `DEV-*` 是唯一可领取、估算和维护工程状态的任务，状态固定为 `proposed → ready → in_progress → completed`，缺外部条件时为 `blocked`；`PT-*` 是唯一产品接受状态；`S*`、`WP-*`、`M*` 是排序/分组，只展示由门禁与子项汇总出的派生状态；子计划中的 `Pxxx-*` 只是检查项编号，不建立重复 Issue、不独立分配 owner 或维护状态。
- 工程负责人对一个 DEV 任务的前后端、数据、基础设施与回归负总责；模块负责人审查事实归属和公开契约；QA 维护验收 fixture；产品负责人决定 PT 是否 accepted。
- 一次只推进一个主切片。后续切片可进行 spike/契约调研，但不得创建业务表、空模块或宣布完成。
- 未提供团队人数、可用工时和速度数据，本 Plan 不伪造日期/故事点。当当前切片进入 ready 后，由领取人对 DEV 任务估算并排入真实迭代。

## 4. 每个工程任务的交付契约

1. 范围确认：从 PRD-010 取回 PT 对应 ENT/FR/IF/NFR，确认非目标、依赖和外部决策。
2. Red：先在 `backend/tests/` 或 `frontend/tests/` 写失败验收；业务规则优先单元测试，事务/锁/JSONB 使用真实 PostgreSQL，基础设施使用真实契约测试。
3. Green：只创建当前用例用到的 module 文件、Model、Router、Service/Repository 和前端页面/组件，不预建其他模块。
4. Contract：更新 FastAPI OpenAPI，从 URL 生成 `frontend/src/api/`，检查无手写 URL/DTO 和未提交漂移。
5. Frontend：ViewModel 通过 RTK Query 读服务端事实；Redux slice 只保存未提交草稿/选择/布局；命令失败显示服务端 `next_action`。
6. Refactor：只消除当前切片已经出现的重复，运行架构测试，禁止新建全局 `utils/common/shared` 或空转层。
7. Evidence：运行定向测试和 `make check`，在实现完成后记录真实命令、环境、结果与未验证项；中断、skip 或 fake-only 不算通过。

## 5. 按切片增量创建的文件边界

| 切片 | 后端主范围 | 前端主范围 | 测试/部署范围 |
| --- | --- | --- | --- |
| S0 | `backend/pyproject.toml`、`backend/app/main.py`、`scheduler.py`、`worker.py`、`core/`、当前需要的 `integrations/` | `frontend/src/app/layout.tsx`、`(main)/`路由/Provider、`api/`、`components/ui/`、`lib/`（store.ts、request.ts）、`styles/` | 根 README/Makefile/CI；`backend/tests/architecture|integration|contract`；`frontend/tests/architecture`；最小 `deploy/` |
| S1 | `modules/identity/`、`modules/projects/` | `app/(main)/projects/`、`app/(main)/workspaces/`、`components/projects/` | identity/projects 的 unit/integration/contract 与项目入口 E2E |
| S2 | `modules/production/` 最小 Task、`scripts/`、`media/`、`governance/`、`assets/`；RabbitMQ/MinIO 真实适配 | `app/(main)/studio/[episodeId]/script/`、`app/(main)/studio/[episodeId]/assets/`、`components/studio/`、`components/tasks/` | 版本/上传/权利/消息契约与剧本资产 E2E |
| S3 | `modules/storyboards/` | `app/(main)/studio/[episodeId]/storyboard/`；`components/storyboards/` | 重排/规格/readiness/影响集成与分镜 E2E |
| S4 | 增量完成 `production/`、供应商适配、Scheduler/消息/费用 | `app/(main)/studio/[episodeId]/tasks/`；`components/tasks/`、`components/production/` | Provider/message/schedule/cost 真实契约和单镜头 E2E |
| S5 | `modules/editing/`、增量 governance/media；`integrations/ffmpeg.py` | `app/(main)/studio/[episodeId]/editing/`、`review/`、`delivery/`；`components/editing/`、`components/governance/` | Timeline/Review/FFmpeg/Manifest 集成、样片和交付 E2E |
| S6 | 只加固已有模块、缓存/存储/调度/可观测；不新建业务模块 | 可访问性、错误/降级和性能修正 | 故障注入、压测、secret 扫描、全量 E2E；选定后才加 OSS/部署配置 |

`api.py/schemas.py/service.py/models.py/repository.py` 只在当前用例需要时创建；`domain.py`只承载独立可测规则。页面直接进入 `app/(main)/<route>/`（Next.js App Router），不得增加会形成 `/pages` URL 的中间层；页面专属 ViewModel 与页面同目录，可复用业务组件才进入 `components/<feature>/`。测试只进入 backend/frontend 各自 `tests/`。

## 6. 依赖与执行流

```mermaid
flowchart LR
    G["WP-00 准入决策"] --> B["WP-01/02 S0 工程基线"]
    B --> I["WP-03 S1 单人立项"]
    I --> C["WP-04/05 S2 剧本资产"]
    C --> S["WP-06 S3 分镜准备"]
    S --> P["WP-07/08 S4 镜头生产"]
    P --> E["WP-09/10 S5 成片交付"]
    E --> H["WP-11/12 S6 发布加固"]
```

`PT-PRJ-004` 从 S2 接入 current ScriptVersion，在 S5 接入 current TimelineVersion；`PT-PRJ-005` 从 S1 空态 Snapshot 开始，每个切片增加已实现模块的公开摘要。两项不拆新 PT，只有 S5 组合全部已实现事实且无 N+1 后才提交最终 accepted。

## 7. 切片工程任务

### 7.1 WP-00：评审与实施准入

| ID | 执行内容 | 完成证据 |
| --- | --- | --- |
| DEV-GATE-01 | 产品/工程评审 MVP 范围、非目标、固定技术基线与 S0 边界，关闭 D-001 | S0 评审记录链接对应文档版本与 commit，无口头隐性需求 |
| DEV-GATE-02 | 按切片准备 D-002–D-007，凭据只进 secret 注入 | 每个决策有负责人、样例和阻塞结论 |
| DEV-GATE-03 | 冻结无真实个人数据的剧本、媒体、身份、费用和故障 fixture | fixture 清单、授权来源、规模和预期结果 |

### 7.2 S0：WP-01/02 工程基线

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S0-01 | 技术启用项 | 先写目录/环境架构检查；再建 README、Makefile、gitignore、CI、backend/frontend 锁文件和无污染 setup | 本机版本复用；`setup` 不下载 Python、不全局安装、不启容器 |
| DEV-S0-02 | PT-DAT-001/003、PT-OBS-001 | 先写空库/非空库拒绝、错误/脱敏、租户约束测试；再建 FastAPI composition root、Settings、async Session、Metadata 初始化和 JSON 日志 | `healthz/readyz`、空库初始化、错误契约和架构测试通过 |
| DEV-S0-03 | SYS-IF-001 工程门禁 | 先写前端目录/API 漂移测试；再建 Next.js/shadcn/Radix 基线、axios request adapter、Redux store 和 `@umijs/openapi` 配置 | OpenAPI URL 可生成 `src/api`，重新生成无 diff，前端无手写平行 DTO/URL |
| DEV-S0-04 | PT-STO-001；Redis/RabbitMQ 连接启用 | 先写 PG/Redis/RabbitMQ/MinIO 真实连接和 MinIO 存储契约；再建类型化 adapter、显式 Compose profile 和 readiness | 依赖不可用时 ready 不谎报；MinIO 直传/HEAD/分片/hash/半失败契约通过 |
| DEV-S0-05 | S0 全部 | 串起 Ruff/Pyright/Pytest、前端 lint/type/Vitest、OpenAPI 漂移、Compose config 和 secret 检查 | `make check` 在本地和 CI 使用同一入口，工作区无生成污染 |

### 7.3 S1：WP-03 单人立项

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S1-01 | PT-IDN-001–004 | 先写 OIDC 非法/并发首登、能力矩阵、归档/停用和跨空间测试；再实现 identity 事实、命令和 ActorContext | 真实 OIDC；三项首登事实原子且幂等；跨空间不可探测 |
| DEV-S1-02 | PT-PRJ-001–003 | 先写权限、预算字段夹带、重排、生命周期和删除竞态测试；再实现 Project/Episode 用例 | 数据库约束与 HTTP 错误稳定；viewer/editor/owner 行为正确 |
| DEV-S1-03 | PT-PRJ-004/005 起始增量 | 先写空项目版本入口/Snapshot 契约；再返回服务端阶段、blockers、next_actions 和 partial_failures | 空态可指引导入剧本；前端不可 PATCH 阶段 |
| DEV-S1-04 | S1 全部 | 生成 client 后实现项目入口 View/ViewModel，再写 Playwright 闭环 | 浏览器完成“首登 → Project → Episode → 导入剧本下一动作” |

### 7.4 S2：WP-04/05 剧本结构与资产

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S2-01 | PT-PROD-001、PT-MQ-001/002 | 先写 Task 事实、Outbox/Inbox、confirm 丢失、ack 前后崩溃和重复投递测试；再实现 script_extraction 最小 Publisher/Worker | 刷新和 Worker 重启可恢复；重复消息一次业务效果 |
| DEV-S2-02 | PT-SCR-001–005 | 先写版本不可变、幂等提取/决议、未决候选、current 冲突和安全清理测试；再实现 scripts 用例和 Text adapter | 导入→提取→决议→确认结构可重放；正文不进日志/消息 |
| DEV-S2-03 | PT-MED-001–003 | 先写直传完成、hash/MIME/大小、半失败、探测、短期访问与归档测试；再实现 media 版本/位置/探测 | 字节不经 API；无永久 URL；失败不产生可用 MediaVersion |
| DEV-S2-04 | PT-GOV-001/002 | 先写授权过期/撤销、证明访问、RightsGate unavailable 和跨空间测试；再实现 Consent/Revision 与权利门禁 | 权利不足可保存 draft 但不得 ready/生产 |
| DEV-S2-05 | PT-AST-001–003、PT-AST-005 | 先写六类规格、版本/current 并发、权利/媒体 readiness 和候选幂等测试；再实现 assets 与 scripts 公开交接 | 至少角色/场景/声音 ready；JSONB 不绕过类型校验 |
| DEV-S2-06 | PT-PRJ-004/005 增量；S2 全部 | 接入 current ScriptVersion 和模块摘要；生成 client 后实现剧本/资产/任务 ViewModel 与 E2E | “导入 → 提取 → 决议 → 确认 → ready 资产”浏览器闭环 |

### 7.5 S3：WP-06 分镜准备

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S3-01 | PT-SBD-001–004 | 先写候选幂等建镜头、完整集重排、Spec 冲突、copy/split/merge 证据保留；再实现 Shot/Spec 命令 | 顺序连续唯一；规格固定 Script/AssetVersion；变换不复制生产事实 |
| DEV-S3-02 | PT-SBD-005/006、PT-AST-004 | 先写字段/版本/权利/媒体 blocker、依赖 unavailable、升级全或无和批量 N+1 测试；再实现 readiness/影响预检 | 同一事实结果确定；current 变化不自动改旧 Spec |
| DEV-S3-03 | PT-PRJ-005 增量；S3 全部 | 生成 client 后实现分镜页、镜头卡、资产选择和批量 blocker，再写 E2E | 验收 Episode 全部目标镜头 `readiness=true` |

### 7.6 S4：WP-07/08 单镜头生产

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S4-01 | PT-PROD-002–004、PT-PROD-008 | 先写能力/价格版本、预检无副作用、提交原子/幂等和 Decimal 账本不变量；再实现能力目录、预检和预占 | 高成本确认缺失时零副作用；提交 P95 基线可测 |
| DEV-S4-02 | PT-PROD-005/006、PT-MQ-003/004、PT-SCH-001/002 | 先写 Attempt 发送前持久化、unknown 对账、取消竞态、毒消息、有界 prefetch 和 Fire 防重；再实现 Provider/Scheduler/Worker 阶段 | 重启/超时/乱序无重复外部调用；unknown 只用原请求键对账 |
| DEV-S4-03 | PT-PROD-007/009、PT-MED-004 | 先写输出槽幂等登记、候选版本冲突、批量局部失败和 Lineage 无自环；再实现 Candidate/Action/媒体登记 | 选用不删历史；成功但下载失败不重发模型 |
| DEV-S4-04 | PT-PRJ-005 增量；S4 全部 | 生成 client 后实现任务/费用/候选 ViewModel，再写真实 Image/Video Provider E2E | “预检 → 提交 → 刷新/恢复 → 候选 → 选择 → 费用收敛” |

### 7.7 S5：WP-09/10 审核、渲染与交付

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S5-01 | PT-EDT-001–004 | 先写缺候选、幂等初始化、operations 全或无、revision 冲突、来源差异和发布不变量；再实现 Timeline/预览/版本 | 旧 TimelineVersion/Source 不漂移；草稿失败不影响已发布版本 |
| DEV-S5-02 | PT-EDT-005、PT-GOV-003/004 | 先写固定版本 Review、blocking Issue/批准竞态、Moderation 重复回调/unknown；再实现返工和内容检查 | 新 TimelineVersion 必须新 Review；blocked/unknown 不得批准/交付 |
| DEV-S5-03 | PT-EDT-006、PT-GOV-005 | 先写渲染预检、中断恢复、FFmpeg 参数/探测、标识/hash 失败；再实现 Render/Task、ffmpeg adapter 和 Label 版本 | 验收样片满足 9:16/1080×1920/25fps/H.264/AAC/SRT；成功输出不覆盖 |
| DEV-S5-04 | PT-EDT-007、PT-PRJ-004/005 最终增量 | 先写 Manifest 部分结果/引用/hash/下载权限和 Snapshot N+1；再接入 current TimelineVersion、完整概览和受控下载 | 每个 Clip 可追到 Spec/Asset/Candidate/Task/Cost/Media/Review/Label |
| DEV-S5-05 | S5 全部 | 生成 client 后实现时间线/审核/交付 ViewModel，补键盘/焦点验收与 Playwright | “编辑 → Issue 返工 → 新版批准 → 渲染 → MP4/SRT/Manifest 下载” |

### 7.8 S6：WP-11/12 发布加固

| ID | 对应 PT | Red 与最小实现顺序 | 退出证据 |
| --- | --- | --- | --- |
| DEV-S6-01 | PT-DAT-002/003、PT-CCH-001/002、PT-MED-005 | 先写归档/硬删/留存、缓存冷启/断连/失效竞态、高成本 fail-closed、存储位置切换/回滚测试；再加固现有用例 | Redis 清空不改变业务结果；无证据误删；位置切换前完整校验 |
| DEV-S6-02 | PT-SCH-003 | 先写 DST/时钟偏移、skip/run_once/catch_up、lease 回收和 manual_attention；再实现退避与运维命令 | 多 Scheduler/重启无重复 Fire/副作用；超限停止自动处理 |
| DEV-S6-03 | PT-OBS-002/003、PT-GOV-006 | 先写 trace 传播、低基数白名单、exporter 故障、脱敏和 Audit 访问控制；再补全 spans/metrics/alerts/runbook | HTTP→Outbox→MQ→Worker→Provider/FFmpeg 可串联；遥测故障不阻断业务 |
| DEV-S6-04 | 全部 P0 PT | 在固定环境运行基准数据、Workspace 越权、secret 扫描、键盘可访问性和容量故障测试，只修实测缺口 | PRD-001 P95/并发/恢复基线有环境证据；无 P0/P1 未解决缺陷 |
| DEV-S6-05 | M1–M8；PT-STO-002 条件性 | 固定剧本连续两次全链路，第二次注入 Redis/MQ/Scheduler/Worker 故障和局部返工；如选 OSS 再跑真实 OSS 契约/迁移 | 成片、费用、血缘、审核、trace 和恢复可核对；发布决策记录残余风险 |

## 8. 69 个 PT 到主工作包的完整覆盖

| PT 精确范围 | 主工作包 | 接受时点 |
| --- | --- | --- |
| PT-DAT-001、PT-DAT-003、PT-OBS-001 | WP-01 | S0；后续每切片回归 |
| PT-STO-001 | WP-02 | S0 |
| PT-IDN-001–004、PT-PRJ-001–003 | WP-03 | S1 |
| PT-PRJ-004–005 | WP-03 起始，WP-04/06/08/10 增量 | S5 最终接受 |
| PT-PROD-001、PT-MQ-001–002、PT-SCR-001–005 | WP-04 | S2 |
| PT-MED-001–003、PT-GOV-001–002、PT-AST-001–003、PT-AST-005 | WP-05 | S2 |
| PT-SBD-001–006、PT-AST-004 | WP-06 | S3 |
| PT-PROD-002–006、PT-PROD-008、PT-MQ-003–004、PT-SCH-001–002 | WP-07 | S4 |
| PT-PROD-007、PT-PROD-009、PT-MED-004 | WP-08 | S4 |
| PT-EDT-001–005、PT-GOV-003–004 | WP-09 | S5 |
| PT-EDT-006–007、PT-GOV-005 | WP-10 | S5 |
| PT-DAT-002、PT-CCH-001–002、PT-MED-005、PT-SCH-003、PT-OBS-002–003、PT-GOV-006 | WP-11 | S6 |
| PT-STO-002 | WP-12 | 仅选定生产 OSS 时必须接受 |

PRD-010 中的 ENT/IF/NFR 不单独创建“建表任务”或“NFR 任务”；它们是上表 PT 的数据、契约和验收组成部分。`IDN-FR-005`/`IDN-IF-006` 保持 P1，不出现在上表。

## 9. 验证命令与 Acceptance 产物

| 阶段 | 必须运行的命令类型 | 实现后的证据重点 |
| --- | --- | --- |
| 每个 DEV 任务 | 从根目录定向执行 `uv run --project backend --frozen --no-python-downloads pytest <test>`，前端定向 Vitest/Playwright，再运行 `make lint typecheck` | 先失败再通过的测试、真实错误输出和修复范围 |
| 每个接口变化 | `make generate-api`，然后检查 `frontend/src/api/` 无漂移 | OpenAPI 与前端 client 同步，业务代码无手写 DTO/URL |
| 每个工作包 | 定向 unit/integration/contract/e2e + `make check` | 当前 PT 成功/失败/权限/并发/恢复条件 |
| S0/S2/S4/S5 | 真实 PG/Redis/RabbitMQ/MinIO/Provider/FFmpeg 契约，只在需要时 `make compose-up` | 依赖版本、地址来源、运行命令和非 mock 结果 |
| S6 | 全量 `make check`、基准测试、故障注入和固定剧本 Playwright | 两次端到端、局部返工、恢复、费用/血缘/trace 核对 |

Acceptance 文件应在当前切片真实执行后才创建，文件名按 `三位序号-中文切片验收.md`；至少记录环境、commit、命令、结果、失败注入、未验证项、产品接受人和残余风险。

## 10. 风险、停止条件与变更策略

| 风险/停止条件 | 处理 |
| --- | --- |
| D-001 未关闭 | 停在 WP-00，不创建代码脚手架 |
| OIDC/Provider/合规未决 | 只允许契约测试或确定性 fake，相应切片不得退出 |
| RabbitMQ/MinIO 不可用 | DEV-S0-04 保持 blocked、readyz 明确返回 503；setup 和 DEV-S0-01～03 不隐式安装或启动 Docker |
| 无 Alembic/迁移能力 | 只允许空库 `metadata.create_all()`；一旦需要保留存量数据升级，停止发布并先修订 Design/PRD |
| 供应商返回 unknown | 使用原 provider_request_key 对账；未确认无副作用前禁止新 Attempt |
| 性能基线无固定环境 | 只报功能结果，不报 P95/恢复时限通过 |
| 前端开始复制 Task/readiness/审核状态 | 停止当前任务，删除平行状态机，改为服务端契约 + ViewModel |
| 发现新范围 | 先修 Requirement/Design/PRD/PRD-010，说明替代或推迟哪个 PT，再更新本 Plan |

## 11. 首个可执行交付

当前唯一可执行交付是 WP-00：由产品/工程负责人接受 S0 所需的 MVP 范围、非目标和固定技术基线，并指派 DEV-S0-01 owner。关闭 D-001（S0 基线）后即可把 DEV-S0-01 标记为 `ready`；DEV-S0-01～03 完成脚手架、API/前端和契约基线，D-006 在 DEV-S0-04 前关闭并验证真实 RabbitMQ/MinIO。S1～S6 的详细文档在进入各自切片前接受，不阻塞 S0。

## 12. S0 逐步骤执行剧本

D-001 关闭且 DEV-S0-01 已指派后，按编号执行 12.1～12.3；D-006 必须在 12.4 前关闭，不能用“尚无 Compose 文件”反向阻塞脚手架。每一步包含确切命令、创建的文件和验证标准。开始前还必须满足：S0 评审记录链接到确定的文档版本与 commit、工作区无与 S0 重叠的未说明修改。进入 12.4 前必须确定 RabbitMQ/MinIO 使用显式本机地址还是 Compose，Compose 方案下 `docker info` 必须成功。

### 12.1 环境预检（DEV-S0-01）

| 步 | 动作 | 命令 / 文件 | 成功标准 |
| --- | --- | --- | --- |
| 1 | 确认 Python 版本 | `uv python list` | 显示 3.11.15（uv 已托管） |
| 2 | 确认 Node 版本 | `node -v && npm -v` | v22.23.1 + 10.9.8 |
| 3 | 确认 PostgreSQL | `psql -U postgres -c "SELECT version()"` | PostgreSQL 18.4 |
| 4 | 确认 Redis | `redis-cli PING` | PONG |
| 5 | 记录基础设施现状 | 检查已配置的 RabbitMQ/MinIO 地址；拟用 Compose 时执行 `docker info` | 把“本机可复用/需 Compose/不可用”记录到 D-006；缺失不阻塞本节后续脚手架，但必须在 DEV-S0-04 前关闭 |
| 6 | 初始化后端项目 | `uv init --bare --name lanverse-backend --python 3.11.15 --vcs none --no-readme --no-pin-python --no-workspace --no-python-downloads backend` | 只生成 `backend/pyproject.toml`，不建嵌套 Git、不下载 Python |
| 7 | 固定 uv 项目配置 | 编辑 `backend/pyproject.toml` | `requires-python = ">=3.11,<3.12"`；`[tool.uv]` 固定 `required-version = "==0.11.31"`、`python-preference = "managed"`、`python-downloads = "never"` |
| 8 | 添加后端运行依赖 | `uv add --project backend --no-python-downloads fastapi 'uvicorn[standard]' pydantic pydantic-settings 'sqlalchemy[asyncio]' asyncpg redis aio-pika minio uuid6 python-multipart prometheus-client opentelemetry-api opentelemetry-sdk` | 依赖只写入 backend；`backend/uv.lock` 生成且无冲突；Python 3.11 的 UUIDv7 使用审查过的 `uuid6.uuid7()` |
| 9 | 添加后端开发依赖 | `uv add --project backend --dev --no-python-downloads ruff pyright pytest pytest-asyncio pytest-cov testcontainers httpx` | `backend/uv.lock` 更新；未触碰系统 Python 和全局 site-packages |
| 10 | 初始化前端项目 | `npx --yes create-next-app@16.2.12 frontend --typescript --tailwind --eslint --app --src-dir --empty --use-npm --disable-git --no-agents-md --yes && rm frontend/README.md` | `frontend/package.json` 存在；使用 React 19、Next.js 16.2.12 和 App Router；不建嵌套 Git/AGENTS；精确解析结果进入 lockfile；项目说明只维护根 README |
| 11 | 添加前端运行依赖 | `cd frontend && npm install --save-exact @reduxjs/toolkit react-redux axios` | `package-lock.json` 更新且 package.json 不使用浮动范围；不安装已由 react-redux 内置的重复类型包 |
| 12 | 添加前端开发/测试依赖 | `cd frontend && npm install -D --save-exact @umijs/openapi@1.13.15 vitest @vitest/coverage-v8 jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event @playwright/test` | OpenAPI 工具固定到仍指向原项目仓库的 1.13.15；其余解析版本精确写入 package.json/lockfile；升级必须复核 npm repository/provenance/integrity；浏览器安装不由 setup 隐式执行 |
| 13 | 初始化 shadcn/ui | `cd frontend && npx --yes shadcn@4.16.0 init --template next --base radix --yes` | `components.json` 存在，明确使用 Radix 基础；CLI 精确版本固定 |
| 14 | 创建仓库配置 | 编写根 `README.md`、`.gitignore`、`Makefile`，以及 `backend/.env.example`、`frontend/.env.example` | Makefile 包含规定目标及真实命令；把 CLI 生成的必要忽略规则合并到根 gitignore 后删除重复的子目录 gitignore；排除 `.venv/`、`node_modules/`、`.env`、缓存、报告和日志但不忽略 `.codex/` 协作配置；env 示例无真实凭据 |
| 15 | 运行 setup | `make setup` | 只执行版本预检、`uv sync --project backend --frozen --no-python-downloads` 与 `cd frontend && npm ci`；不全局安装、不启动容器、不自动下载 Playwright 浏览器 |

### 12.2 后端 core 与空库（DEV-S0-02）

| 步 | 动作 | 创建文件 | 验证 |
| --- | --- | --- | --- |
| 1 | Red：架构测试 | `backend/tests/architecture/test_directory_structure.py`、`test_import_direction.py`、`test_test_boundaries.py` | 运行失败（目录尚不存在） |
| 2 | 创建 Settings | `backend/app/core/config.py` | Pydantic BaseSettings，含 DES-MOD-001 §4.3 全部字段；`make typecheck` 通过 |
| 3 | 创建 database.py | `backend/app/core/database.py` | async engine + session factory + `get_async_session()` + 空库 `metadata.create_all()` + 非空 DB 拒绝逻辑 |
| 4 | 创建 errors.py | `backend/app/core/errors.py` | `ApiError` 基类 + 12 种子类 + `register_exception_handlers(app)` |
| 5 | 创建 schemas.py | `backend/app/core/schemas.py` | `ApiResponse[T]`、`PaginatedResponse[T]`、`ErrorResponse` |
| 6 | 创建 logging.py | `backend/app/core/logging.py` | JSON 日志 + RequestID middleware + access log middleware |
| 7 | 创建 main.py | `backend/app/main.py` | 最小 FastAPI app + CORS + 4 层 middleware + exception handler + `/healthz` + `/readyz` |
| 8 | Green：架构测试 | 同步 1 | 所有架构测试通过 |
| 9 | 启动并测试 | `make dev-api` → `curl localhost:8000/healthz` → `curl localhost:8000/readyz` | healthz=200；在 DEV-S0-04 完成前 readyz 可因 MQ/MinIO 未接通返回 503，但响应必须逐项解释依赖状态，禁止只检查 PG/Redis 后谎报 200 |

### 12.3 前端基线（DEV-S0-03）

| 步 | 动作 | 创建文件 | 验证 |
| --- | --- | --- | --- |
| 1 | Red：API 漂移测试 | `frontend/tests/architecture/api-drift.test.ts` | 失败（src/api 目录不存在），且文件名能被 Vitest 默认/显式 include 收集 |
| 2 | 创建 axios request adapter | `frontend/src/lib/request.ts` | axios 实例（base URL、auth 拦截器、错误映射、401 重定向） |
| 3 | 创建 Redux store | `frontend/src/lib/store.ts` | `configureStore` + 当前切片需要的 RTK Query API；不创建无端点的永久“空基类”抽象 |
| 4 | 配置测试 runner | `frontend/vitest.config.ts`、`frontend/playwright.config.ts`、`frontend/tests/setup.ts` | Vitest 明确收集 `frontend/tests/**/*.test.ts(x)`；Playwright 只收集 `tests/e2e`，浏览器安装由 `make e2e-install` 显式触发 |
| 5 | 配置 openapi2ts | `frontend/openapi2ts.config.ts` 与 `package.json#scripts.openapi2ts` | `schemaPath` 读取必填 `OPENAPI_SCHEMA_URL`，`serversPath: './src/api'`，`requestLibPath` 指向 `src/lib/request`；缺 URL 立即失败，不静默使用远端默认值 |
| 6 | 创建根 layout | `frontend/src/app/layout.tsx` | Next.js 根布局 + React Redux Provider；RTK Query middleware/listener 由 store 提供，不再引入第二套 QueryClient |
| 7 | 创建 (main) 路由组 | `frontend/src/app/(main)/layout.tsx`、`frontend/src/app/(main)/projects/page.tsx` | 登录后布局骨架；URL 为 `/projects`，不得出现 `/pages/projects` |
| 8 | 创建样式入口 | `frontend/src/app/globals.css` | Tailwind v4 + CSS 变量（shadcn 语义令牌） |
| 9 | 首次生成 client | 启动本地 API 后执行 `OPENAPI_SCHEMA_URL=http://127.0.0.1:8000/openapi.json make generate-api` | `src/api/` 目录生成，无错误；生成命令未写死开发机 URL |
| 10 | Green：API 漂移测试 | 同步 1 | 先生成、记录工作区状态、再次生成并以 `git diff --exit-code -- frontend/src/api` 验证无漂移 |

### 12.4 基础设施连接（DEV-S0-04）

| 步 | 动作 | 创建文件 | 验证 |
| --- | --- | --- | --- |
| 1 | Red：存储契约测试 | `backend/tests/contract/test_minio_port.py` | 失败（MinIO 未配置） |
| 2 | 创建 Redis 连接 adapter | `backend/app/integrations/redis.py` | 只实现连接池、ping/readiness 和类型化连接错误；cache-aside/限流由 S6 首个真实用例拉动 |
| 3 | 创建 RabbitMQ 连接 adapter | `backend/app/integrations/rabbitmq.py` | 只实现连接、S0 topology 幂等预检和 readiness；Publisher/Consumer 业务语义在 S2 的 Outbox/Inbox 用例实现 |
| 4 | 创建 MinIO adapter | `backend/app/integrations/minio.py` | `ObjectStoragePort` 7 个操作 |
| 5 | 创建 Docker Compose | `deploy/compose.yaml` | PostgreSQL、Redis、RabbitMQ、MinIO 按显式 profile 启动；健康检查、命名 volume、非默认测试凭据和固定镜像版本可由 `docker compose config` 验证 |
| 6 | 验证 ready | `make compose-up` → `curl localhost:8000/readyz` | readyz 反映全部真实依赖状态 |
| 7 | Green：存储契约测试 | 同步 1 | 真实 MinIO 直传/HEAD/分片/hash/半失败通过 |

### 12.5 质量门禁整合（DEV-S0-05）

| 步 | 动作 | 命令 | 验证 |
| --- | --- | --- | --- |
| 1 | 确保 Makefile 目标完整 | 检查 setup/dev-api/dev-frontend/db-init/generate-api/lint/typecheck/test/check/e2e-install/e2e/compose-up/compose-down | 所有目标存在且仅做薄转发/前置检查 |
| 2 | 运行快速门禁 | `make check` | Ruff/Pyright/Pytest/frontend lint/type/Vitest/OpenAPI drift/`docker compose config`/secret scan 全部通过；不隐式安装浏览器或启动依赖 |
| 3 | 运行显式 E2E 门禁 | `make e2e-install && make e2e` | 仅在 S0/S1 浏览器验收环境中安装 Chromium 并运行 Playwright；CI 使用固定缓存/镜像，缺浏览器不得把 E2E 报告为通过 |
| 4 | 验证无生成污染 | `git status --short` | 仅预期的源文件有修改 |

S0 退出条件：`make check` 在本地全绿，`readyz` 反映真实 PG/Redis/MQ/MinIO 状态，`make generate-api` 后 `frontend/src/api/` 无漂移。

## 13. 每切片 API 端点目录

### S0 — 工程基线

| 方法 | 路径 | 用途 | 归属 |
| --- | --- | --- | --- |
| GET | `/healthz` | 进程存活 | core |
| GET | `/readyz` | 所有依赖就绪 | core |
| GET | `/metrics` | Prometheus 指标（内网） | core |
| GET | `/openapi.json` | OpenAPI schema | FastAPI 内置 |

### S1 — 身份与立项

| 方法 | 路径 | 用途 | PT |
| --- | --- | --- | --- |
| POST | `/api/v1/auth/callback` | OIDC 回调，并发幂等首次登录 | IDN-001 |
| GET | `/api/v1/me` | 当前用户资料 | IDN-002 |
| PATCH | `/api/v1/me` | 修改 display_name/avatar_url | IDN-002 |
| GET | `/api/v1/workspaces` | 我的 Workspace 列表 | IDN-003 |
| POST | `/api/v1/workspaces` | 创建 Workspace | IDN-003 |
| GET | `/api/v1/workspaces/{id}` | Workspace 详情 | IDN-003 |
| PATCH | `/api/v1/workspaces/{id}` | 改名称 | IDN-003 |
| POST | `/api/v1/workspaces/{id}/archive` | 归档 | IDN-003 |
| POST | `/api/v1/workspaces/{id}/restore` | 恢复 | IDN-003 |
| POST | `/api/v1/accounts/deactivate` | 账户停用 | IDN-004 |
| GET | `/api/v1/projects` | 项目列表 | PRJ-001 |
| POST | `/api/v1/projects` | 创建项目 | PRJ-001 |
| GET | `/api/v1/projects/{id}` | 项目详情 | PRJ-001 |
| PATCH | `/api/v1/projects/{id}` | 修改元数据 | PRJ-001 |
| POST | `/api/v1/projects/{id}/budget` | owner 预算命令 | PRJ-001 |
| GET | `/api/v1/projects/{id}/episodes` | 单集列表 | PRJ-002 |
| POST | `/api/v1/projects/{id}/episodes` | 创建单集 | PRJ-002 |
| GET | `/api/v1/episodes/{id}` | 单集详情 | PRJ-002 |
| PATCH | `/api/v1/episodes/{id}` | 修改元数据 | PRJ-002 |
| POST | `/api/v1/episodes/{id}/reorder` | 完整集合重排 | PRJ-002 |
| POST | `/api/v1/projects/{id}/archive` | 归档项目 | PRJ-003 |
| POST | `/api/v1/episodes/{id}/archive` | 归档单集 | PRJ-003 |
| GET | `/api/v1/episodes/{id}/production-snapshot` | 生产概览 | PRJ-005 |

### S2 — 剧本与资产

| 方法 | 路径 | 用途 | PT |
| --- | --- | --- | --- |
| POST | `/api/v1/script-sources` | 创建剧本来源 | SCR-001 |
| POST | `/api/v1/script-sources/{id}/versions` | 导入并发布新 ScriptVersion | SCR-001 |
| GET | `/api/v1/script-versions/{id}` | 版本详情 | SCR-001 |
| POST | `/api/v1/script-versions/{id}/set-current` | 切换当前版本 | SCR-001/PRJ-004 |
| POST | `/api/v1/script-versions/{id}/extract` | 启动结构提取（Task + Outbox） | SCR-002 |
| GET | `/api/v1/extraction-batches/{id}` | 批次状态 | SCR-002 |
| GET | `/api/v1/extraction-batches/{id}/candidates` | 分页候选 | SCR-003 |
| POST | `/api/v1/extraction-batches/{id}/decisions` | 提交候选决议 | SCR-003 |
| POST | `/api/v1/extraction-batches/{id}/confirm` | 确认结构 | SCR-004 |
| GET | `/api/v1/scenes/{id}` | 场景详情 | SCR-004 |
| POST | `/api/v1/media/upload-sessions` | 初始化上传 | MED-001 |
| POST | `/api/v1/media/upload-sessions/{id}/complete` | 完成上传 | MED-001 |
| GET | `/api/v1/media-versions/{id}` | 媒体版本详情 | MED-002 |
| POST | `/api/v1/media-versions/{id}/access` | 短期访问 URL | MED-002 |
| POST | `/api/v1/media-versions` | 追加新媒体版本 | MED-003 |
| POST | `/api/v1/consents` | 登记授权 | GOV-001 |
| POST | `/api/v1/consents/{id}/revoke` | 撤销授权 | GOV-001 |
| POST | `/api/v1/rights-gate/check` | 权利门禁查询 | GOV-002 |
| GET | `/api/v1/assets` | 资产列表 | AST-001 |
| POST | `/api/v1/assets` | 创建资产身份 | AST-001 |
| GET | `/api/v1/assets/{id}` | 资产详情 | AST-001 |
| POST | `/api/v1/assets/{id}/versions` | 追加 AssetVersion | AST-002 |
| GET | `/api/v1/asset-versions/{id}` | 版本详情 | AST-002 |
| POST | `/api/v1/asset-versions/{id}/set-current` | 切换当前版本 | AST-002 |
| GET | `/api/v1/asset-versions/{id}/readiness` | 资产就绪度 | AST-003 |
| GET | `/api/v1/assets/{id}/usages` | 反向引用 | AST-004 |

### S3 — 分镜

| 方法 | 路径 | 用途 | PT |
| --- | --- | --- | --- |
| GET | `/api/v1/episodes/{id}/shots` | 镜头列表 | SBD-001 |
| POST | `/api/v1/episodes/{id}/shots` | 建立 Shot | SBD-001 |
| GET | `/api/v1/shots/{id}` | 镜头详情 | SBD-001 |
| POST | `/api/v1/shots/{id}/spec-versions` | 保存 ShotSpecVersion | SBD-002 |
| GET | `/api/v1/shot-spec-versions/{id}` | 规格版本详情 | SBD-002 |
| POST | `/api/v1/episodes/{id}/shots/reorder` | 原子重排 | SBD-003 |
| POST | `/api/v1/shots/{id}/copy` | 复制 Shot | SBD-004 |
| POST | `/api/v1/shots/{id}/split` | 拆分 Shot | SBD-004 |
| POST | `/api/v1/shots/merge` | 合并 Shot | SBD-004 |
| GET | `/api/v1/shots/{id}/readiness` | 镜头就绪度 | SBD-005 |
| GET | `/api/v1/episodes/{id}/shots/readiness` | 整集就绪度摘要 | SBD-005 |
| POST | `/api/v1/assets/{id}/upgrade-preflight` | 资产升级预检 | AST-004/SBD-006 |
| POST | `/api/v1/assets/{id}/upgrade-apply` | 资产升级执行 | AST-004/SBD-006 |

### S4 — 生产

| 方法 | 路径 | 用途 | PT |
| --- | --- | --- | --- |
| GET | `/api/v1/capabilities` | 能力目录 | PROD-002 |
| POST | `/api/v1/generation/preflight` | 生成预检（纯读取） | PROD-003 |
| POST | `/api/v1/generation/submit` | 提交生成（原子事务） | PROD-004 |
| POST | `/api/v1/generation/batch-submit` | 批量生成 | PROD-009 |
| GET | `/api/v1/tasks` | Task 列表 | PROD-001 |
| GET | `/api/v1/tasks/{id}` | Task 详情 | PROD-001 |
| POST | `/api/v1/tasks/{id}/cancel` | 取消 | PROD-006 |
| POST | `/api/v1/tasks/{id}/retry` | 重试 | PROD-006 |
| POST | `/api/v1/tasks/{id}/reconcile` | 对账（unknown → 终态） | PROD-006 |
| GET | `/api/v1/candidates` | 候选列表 | PROD-007 |
| POST | `/api/v1/candidates/{id}/select` | 选用候选 | PROD-007 |
| GET | `/api/v1/costs` | 费用汇总 | PROD-008 |

### S5 — 剪辑与交付

| 方法 | 路径 | 用途 | PT |
| --- | --- | --- | --- |
| GET | `/api/v1/episodes/{id}/timeline` | 获取 Timeline 草稿 | EDT-001 |
| POST | `/api/v1/episodes/{id}/timeline/init` | 初始化 Timeline | EDT-001 |
| POST | `/api/v1/episodes/{id}/timeline/operations` | 批量编辑操作 | EDT-002 |
| POST | `/api/v1/timelines/{id}/preview` | 预览 Render | EDT-003 |
| POST | `/api/v1/timelines/{id}/publish` | 发布 TimelineVersion | EDT-004 |
| POST | `/api/v1/timeline-versions/{id}/submit-review` | 提交审核 | EDT-005/GOV-003 |
| GET | `/api/v1/reviews/{id}` | 审核详情 | GOV-003 |
| POST | `/api/v1/reviews/{id}/issues` | 创建 Issue | GOV-003 |
| POST | `/api/v1/issues/{id}/resolve` | 解决 Issue | GOV-003 |
| POST | `/api/v1/reviews/{id}/approve` | 批准 | GOV-003 |
| POST | `/api/v1/reviews/{id}/request-changes` | 退回 | GOV-003 |
| POST | `/api/v1/timeline-versions/{id}/render` | 最终渲染 | EDT-006 |
| GET | `/api/v1/renders/{id}` | 渲染状态 | EDT-006 |
| POST | `/api/v1/renders/{id}/label` | 标识处理 | GOV-005 |
| GET | `/api/v1/renders/{id}/manifest` | DeliveryManifest | EDT-007 |
| GET | `/api/v1/deliveries/{id}/download` | 受控下载 MP4/SRT/Manifest | EDT-007 |

### S6 — 加固（无新业务端点）

S6 只加固已有端点；附加运维端点/runbook 入口为内部接口。

## 14. 前端路由设计

| 路由 | 页面 | 切片 | 核心 ViewModel 查询 |
| --- | --- | --- | --- |
| `/` | 重定向到 `/projects` | S1 | — |
| `/login/callback` | OIDC 回调处理 | S1 | `useAuthCallback()` → 设置 token → 重定向 |
| `/projects` | 项目列表 | S1 | `useListProjectsQuery()` + 创建/归档/删除 |
| `/projects/:id` | 项目详情 + Episode 列表 | S1 | `useGetProjectQuery(id)` + Snapshot |
| `/projects/:id/settings` | 项目设置（预算等） | S1 | owner 专用预算命令 |
| `/studio/:episodeId/script` | 剧本导入与结构确认 | S2 | `useListScriptVersionsQuery()` + 提取批次 + 候选决议 |
| `/studio/:episodeId/assets` | 资产工作区 | S2 | `useListAssetsQuery()` + readiness |
| `/studio/:episodeId/storyboard` | 分镜编辑器 | S3 | `useListShotsQuery()` + 重排 + Spec + readiness |
| `/studio/:episodeId/tasks` | 任务与费用 | S4 | `useListTasksQuery()` + 候选选择 + 费用 |
| `/studio/:episodeId/editing` | 时间线编辑器 | S5 | `useGetTimelineQuery()` + 操作 + 发布 |
| `/studio/:episodeId/review` | 审核面板 | S5 | Issue + Decision + Moderation |
| `/studio/:episodeId/delivery` | 交付下载 | S5 | Render status + Manifest + 下载 |
| `/workspaces` | Workspace 切换 | S1 | `useListWorkspacesQuery()` |

每个页面直接按 `frontend/src/app/(main)/<route>/` 组织（Next.js App Router），动态路由使用 `[projectId]`、`[episodeId]`；页面专属 ViewModel hook 与页面同目录。S0 只实现 App Router 骨架和 `frontend/src/app/(main)/projects/page.tsx`，其 URL 必须是 `/projects`。

## 15. PT 间并行度标注

以下标注 PLAN-007～009 中 PT 的并发能力：**串行**（必须按序）vs **并行**（不同模块间可同时进行）。

### PLAN-007 基础业务

| PT | 切片:序 | 串行/并行 | 并行约束 |
| --- | --- | --- | --- |
| IDN-001 → IDN-002 | S1:1→2 | 串行 | IDN-002 依赖 ActorContext |
| IDN-002 → IDN-003, IDN-004 | S1:2→3,4 | 并行（IDN-003 ∥ IDN-004） | 两者只依赖 IDN-002 |
| PRJ-001 | S1:5 | 并行于 IDN-003/004 | 只依赖 IDN-002 |
| PRJ-002 | S1:6 | 串行于 PRJ-001 | Episode 依赖 Project |
| PRJ-003 | S1:7 | 串行于 PRJ-001/002 | 删除检查需完整事实 |
| PRJ-004, PRJ-005 | S1骨架 | 并行于 S1 其余 | 持续任务，S1 只建空入口 |
| MED-001 → MED-002 → MED-003 | S2:1→3 | 串行 | 版本链依赖 |
| GOV-001 → GOV-002 | S2:4→5 | 串行 | RightsGate 依赖 Consent |
| MED-001 ∥ GOV-001 | S2:1∥4 | 并行 | 不同模块，无依赖 |
| MED-004, MED-005 | S4/S6 | 串行于 MED-003 | 输出登记依赖基础媒体 |
| GOV-003 → GOV-004 → GOV-005 | S5:1→3 | 串行 | Review→Moderation→Label 链 |
| GOV-006 | 持续 | 并行于所有 | Audit 追加不阻塞业务 |

### PLAN-008 创作生产

| PT | 切片:序 | 串行/并行 | 并行约束 |
| --- | --- | --- | --- |
| SCR-001 → SCR-002 → SCR-003 → SCR-004 → SCR-005 | S2:1→5 | 串行 | 提取链严格有序 |
| AST-001 → AST-002 → AST-003 | S2:6→8 | 串行 | 身份→版本→就绪度 |
| AST-005 | S2:9 | 串行于 SCR-004 + AST-001 | 跨模块交接 |
| SCR-* ∥ AST-*（前置满足后） | S2 | 并行 | 剧本链与资产链可并行推进 |
| SBD-001 → SBD-002 → SBD-003 | S3:1→3 | 串行 | Shot→Spec→重排 |
| SBD-004, SBD-005 | S3:4,5 | 并行于 SBD-003 后 | 不同操作 |
| SBD-006 | S3:6 | 串行于 SBD-002/005 + AST-004 | 升级依赖双方 |
| PROD-001 | S2基线 | 先于所有 S4 PT | Task 模型必须先行 |
| PROD-002 → PROD-003 → PROD-004 | S4:1→3 | 串行 | 能力→预检→提交 |
| PROD-005 ∥ PROD-008 | S4:4∥7 | 并行 | Attempt 与 Cost 独立实现 |
| PROD-006, PROD-007 | S4:5,6 | 串行于 PROD-005 | Task ops + Candidate 依赖 Attempt |
| PROD-009 | S4:8 | 串行于 PROD-003～008 | 批量组合已有能力 |

### PLAN-009 剪辑与平台

| PT | 切片:序 | 串行/并行 | 并行约束 |
| --- | --- | --- | --- |
| DAT-001 → DAT-003 | S0:1→3 | 串行 | 约束依赖表结构 |
| DAT-001 ∥ OBS-001 ∥ STO-001 | S0:1∥3∥2 | 并行 | 不同技术域 |
| MQ-001 → MQ-002 | S2:1→2 | 串行 | Inbox 依赖 Publisher |
| MQ-003, MQ-004 | S4:1,2 | 并行于 MQ-002 后 | 不同关注点 |
| EDT-001 → EDT-002 → EDT-003 → EDT-004 | S5:1→4 | 串行 | Timeline 链 |
| EDT-005 ∥ EDT-006 | S5:5∥6 | 并行（合同一 TimelineVersion） | Review 与 Render 独立 |
| EDT-007 | S5:7 | 串行于 EDT-006 + GOV-005 | Manifest 聚合全部产出 |
| CCH-001 → CCH-002 | S6:2→3 | 串行 | 限流依赖缓存基础 |
| CCH-* ∥ SCH-003 ∥ OBS-002 ∥ OBS-003 | S6 | 并行 | 不同技术域 |
| STO-002 | 条件 | 独立于所有 | 仅目标 OSS 确定后触发 |

**规则**：同一串行链上的 PT 必须按顺序领取；不同模块的并行 PT 可由不同开发者同时推进，但集成点（如 AST-005、EDT-007）必须在双方完成后才能验收。

# Go 后端业务完整性实施计划

日期：2026-09-08。执行入口：[Spec](../requirement/0016-Go后端业务完整性需求规格.md)。初始 Checklist 全部未完成，完成只凭本轮证据。

- [x] B-01 核对设计优先级、19 个源码模块及测试/公共注册；保存已有修改基线，运行基础测试。
- [x] B-02 为 BG-01～05 编写失败测试，完成 Go Manifest/Attempt 公共查询与安全合同。
- [x] B-03 为 BG-06 编写失败测试，补齐正式 World/Intent Owner 版本查询。
- [x] B-04 同步 OpenAPI、检查已存在提案/采纳/恢复的边界，修复实际发现的阻断缺陷。
- [x] B-05 在本轮专用数据库验证跨 Owner 事务/正式查询，执行全量 Go 质量门禁并登记真实跳过项。
- [x] B-06 完成 Acceptance 收尾审查，修复反例，更新业务矩阵和遗留修改清单。
- [ ] B-07 按已接受的 Go 内部薄适配层实现具体供应商模型、费用与 unknown 联调。
- [ ] B-08 补齐视频/声音/成片及剩余全量矩阵，逐业务完成真实验收。

依赖 B-01 → B-02/B-03 → B-04 → B-05 → B-06；B-07/B-08 保留完整目标，不用首批通过抵扣。当前不提交、推送或部署；完成后交付可审查 diff。

此前 B-05 检查留有 447 条基线 ST1005 和 34 项外部条件跳过；这些历史结果由下文 BC 收敛记录更新。B-06 为本任务自审，未声称独立审查通过。详见 Acceptance。

### B-07 本轮 Checklist
- [x] 冻结运行配置解析与凭据生命周期 Red/Green。
- [x] 可执行工厂、精确路由、Query 无重发 Red/Green。
- [x] OpenAI 单图协议与私有对象接管、Worker 注入。
- [x] 错误/取消/不支持能力反例、全 Go 门禁和增量审查。
- [ ] 真实供应商、视频目标与异步视频端到端验收（不可用协议模拟抵扣）。

前一轮完成 PG-01～05 的本地协议与数据库合同，当时 Lint 留有 447 条问题；真实付费调用与异步视频仍未验收，B-07/B-08 总项继续未完成。


### 完整实现收敛 Checklist
- [x] BC-01：真实领域目标 Red → 请求提示映射 → 反例与回归。
- [x] BC-02：逐诊断修复 Lint → 全量测试 → Race/vet/vuln。
- [x] BC-03 本地：用户确认保留删除 → deploy 环境目录 → Compose/镜像入口/架构引用验证。
- [ ] BC-03 发布门禁：远程 CI 尚未执行，未据本地结果勾选。
- [ ] 全量业务逐项验收：ALL-01～09 继续执行，不以以上修复抵扣供应商和成片未完成内容。

- [x] BC-04：上传完成撤权/过期/取消 Red → 修复 → 回归。
- [x] BC-05：并发上传初始化真实 PostgreSQL 反例与唯一会话验收。

- [x] BC-06 查询：首次日志查询失败 Red → 死信模板 → 空索引排序与真实 ELK 回归。
- [ ] BC-06 rollover：数字尾缀与模板继承静态合同通过；真实 rollover 请求遇 Elasticsearch 超时，未完成。

### 尚未完成的新参考生成主链

以下是 Design 2051 的实现依赖，不以旧 reference_asset 单图协议通过替代：

- [ ] REF-01：ApprovedReferencePlanVersion 与精确 ReferencePlanTarget/Brief/Style/Policy 读取，当前权限、active Plan 和跨项目拒绝合同。
- [ ] REF-02：六类 GenerationTargetProduction 严格联合、依赖摘要、输出槽位、不可变目标和 Head CAS；旧泛化目标不能创建新参考生产目标。
- [ ] REF-03：执行授权、Provider 编译器与冻结执行快照、输出 staging 接管；UNKNOWN 仅查询/对账，不重发。
- [ ] REF-04：CandidateBundle/Set、确定性 QC、视觉审核、人工选择与 Owner Apply；最终 Asset/ReferenceBinding 及投影一致性。
- [ ] REF-05：每一类目标完整数据库、Temporal、对象存储、供应商联调；并发、撤权、重启、成本与取消反例。

当前 REF-01～05 尚未实现完整闭环，继续列为全后端完成的必要条件。下一业务实现必须先从 REF-01 的正式事实源开始，不继续扩建旧泛化目标作为替代。

BC-02 指最终普通 Go 全量、上传和架构 Race、格式、vet、Lint 与漏洞扫描已通过；全环境合并 Race 曾发现架构违规，修复后针对性回归通过，合并运行因本机 Docker 响应异常中断。不能据此声明全后端全环境验收通过。

# Lanverse 本机开发环境变量模板
# 复制为 .env 后填写；真实 .env 不得提交。
# 未列出的配置使用代码默认值。

# PostgreSQL 连接地址：使用默认本机数据库时可省略；生产或非默认数据库必须显式配置。
DATABASE_URL=postgres://postgres:lanverse-development-only@127.0.0.1:5432/lanverse

# JWT 签名密钥：必须替换为至少 32 个字符的随机密钥，示例占位值会被服务拒绝。
AUTH_JWT_SECRET=replace-with-at-least-32-random-characters

# API 地址、前端跨域、Redis、MinIO 和 Cookie 参数均有本机开发默认值，无需重复配置。

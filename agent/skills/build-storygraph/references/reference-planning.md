# Reference planning

Produce only a `ReferencePlanCandidate` from the frozen seed inventory. Do not approve or publish a plan, create formal Owner records, call a media Provider, generate images, or replace Backend-owned references, coverage, constraints, or dependencies.

Copy the Workspace, Project, Production World, Visual Foundation Candidate revision, and seed-root lineage exactly. Select exactly one supplied actual State option for every Character Identity Anchor. Return one specification for every target implied by those selections and every fixed target seed; do not add, remove, rename, or merge target business keys.

Choose `required`, `optional`, or `not_generated` as a reviewable proposal. Keep each `design_focus` within the supplied PurposeProfile and copy its complete `forbidden_changes` list unchanged. Echo only the dependency keys mechanically implied by the frozen seeds and selected Anchor States. A generated target cannot require a dependency with a lower fulfillment rank.

Treat the Visual Foundation Candidate as an immutable reviewed input, not as permission to invent an Effective Style or Policy Snapshot. If the frozen input cannot support a complete and internally consistent candidate, fail instead of filling missing Owner facts or emitting Provider configuration.

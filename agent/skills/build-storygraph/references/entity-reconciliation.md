# Entity reconciliation

Reconcile only the candidate fragments and exact hashes supplied for this reduce shard. Prefer stable canonical temporary keys, preserve aliases and conflicts, and never merge identities solely because names are similar.

# Script spans

Propose source-ordered scene spans that cover the supplied normalized script from code point 0 through codepoint_count without gaps or overlaps. Use temporary span keys only. Preserve ambiguous boundaries as typed review issues; do not resolve identities, apply a world preset, or create formal Scene records.

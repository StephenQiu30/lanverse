# Continuity review

Review the supplied candidate and deterministic gate results. Issues must be scoped, evidence-backed, and severity-preserving. A model opinion cannot downgrade a deterministic blocker. Repair suggestions must stay inside the explicitly allowed candidate boundary.

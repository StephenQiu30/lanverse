Lanverse 使用单一 Alembic revision 链管理 PostgreSQL schema。Alembic 只负责表、列、
索引、约束、外键和必要数据迁移的有序升级/回滚；不承担应用启动、测试建表、
业务任务或未经验证的自动修复。

事实边界：

- SQLAlchemy `Base.metadata` 是当前模型设计源；`versions/` 中已审阅 revision 是部署升级事实源。
- 数据库 `alembic_version.version_num` 与 revision 文件内 `revision/down_revision` 形成唯一有序链。
- Web、Scheduler 和 Worker 启动时只检查 head，不自动执行 upgrade。部署前独立执行迁移。

命名硬规则：

- revision 文件名为 `<12 位小写十六进制 revision>_<英文 snake_case 动作与对象>.py`。
- 例如 `9a4d6e2f1b73_add_script_adaptation_runs.py`；禁止中文、拼音、中英混合和无语义序号。
- 文件名包含扩展名时最长 64 个 ASCII 字符；超长时缩小职责或删除重复上下文，不造无语义缩写。
- 迁移 docstring 与 Alembic message 使用英文工程语义；产品说明保留在 `docs/`。
- 不建立旧文件别名、双路径解析或命名 allowlist；架构测试对整个 revision 目录强制执行。

全新数据库执行 `python -m app.initialize_database`。日常方式为：先修改 ORM，生成一个英文语义
revision，人工审查 upgrade/downgrade、约束名与锁风险，再执行空库、历史库、回滚和
`alembic check`。已有但尚未带 revision 的受控历史库，必须先取得可恢复备份，再由
`python -m app.adopt_database_baseline` 做完整 schema 零差异校验；禁止直接 stamp，也不为未知中间状态写
兼容补丁。

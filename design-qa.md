# 治理页信息架构与视觉一致性 Design QA

- source visual truth：用户提供的治理页截图 `/var/folders/r5/lm_1_1hd321dzlfq0lctjdnw0000gn/T/codex-clipboard-81316452-aa2a-4122-8630-bfb936e977b5.png`，以及“页面对齐、风格一致，并判断是否保留一级菜单”的明确要求。
- source pixels：`3376 × 1942`，包含浏览器 chrome；截图中治理占据一级导航，页面使用独立 `1320px` 宽度，并在零记录时保留空列表、虚线详情框和重复 CTA。
- implementation：`/tmp/lanverse-governance-final.png`，`1673 × 842`；隔离 owner 工作空间、零授权记录、零证明媒体的真实页面状态。
- combined comparison：`/tmp/lanverse-governance-before-after.png`；上方为用户源图，下方为实现，输入归一到 `1673px` 宽后上下拼接。
- test state：浅色模式、owner、零授权记录；隔离数据库和固定验证码邮件器，不读取或写入用户业务数据。

## Findings

- 无剩余 P0、P1 或 P2 问题。
- P3：页面标题区和空状态都保留“新建授权”按钮。前者服务有明确目标的用户，后者作为空状态恢复动作；二者位于不同滚动上下文，且不再与空主从框架叠加，属于有意保留。

## 必查表面

- 字体与层级：治理页使用共享 `PageHeader`，工作空间名作为 eyebrow，移除“合规事实层”和 Workspace ID 胶囊；标题、说明、指标与项目管理页层级一致。
- 间距与布局：页面与 Logo/主导航共用 `max-width: 1440px` 和响应式左右内边距；标题到指标、警告、空状态统一使用 `8` 间距节奏。
- 颜色与令牌：指标和空状态使用低对比 `muted` 表面；证明缺失使用低饱和语义警告面，不再叠加边框、阴影或虚线框。
- 图像与资产：品牌 Logo 和治理图形继续使用真实资产与 Lucide 图标；没有文本占位符、自绘 SVG 或额外装饰图像。
- 文案与内容：零记录时只显示一个全宽“还没有授权记录”恢复面，不再显示“0 项 Workspace 事实”空列表；操作审计继续展示真实追加事件。
- 可访问性：空状态为具名 `region`，主导航为具名 `navigation`；账户菜单和全局命令面板均保留“治理与审计/治理”入口，键盘角色与链接目标可被语义查询。

## 比较历史

1. 源图中页面左边界与全局导航不一致，治理占据高频一级导航；零记录状态同时渲染空列表、虚线详情框和多个动作，构成 P1 信息架构与视觉一致性问题。
2. 第一轮测试固定三条契约并如预期失败：`editor/owner` 一级导航不含治理但仍有访问权；账户菜单保留治理入口；治理页使用共享 `1440px` 宽度和无边框空状态。
3. 实现移除治理的一级导航项，但不改变 `canAccessPage` 权限；`editor/owner` 仍可从账户菜单和全局命令面板进入，`viewer` 继续不可见。
4. 页面复用 `studioContainerClassName`，零记录时不再构造空主从视图；有记录时才显示 `320px` 列表与详情，证明缺失和错误状态统一为低饱和无边框表面。
5. 最终组合对照显示：页面内容与 Logo/导航左边界一致，指标、警告和空状态采用项目页同一视觉语言，一级导航只保留“项目 / 资产”。

## 主交互与浏览器验证

- 主导航：隔离 owner 会话仅生成“项目 / 资产”，没有治理入口。
- 账户菜单：打开用户菜单可见“治理与审计”，链接目标为 `/governance`。
- 命令面板：打开“搜索或执行命令”可见“治理 · 授权与审计”，链接目标为 `/governance`。
- 页面状态：真实 `/governance` 返回授权摘要、证明缺失提示、具名零记录空状态和操作审计；页面 API、创建、修订与撤销路径未改变。
- 控制台：Lanverse 页面来源没有 error 或 warn；观察到的 error 来自 Immersive Translate Chrome 扩展，不属于项目代码。
- 构建验证：`npm run lint`、`npm run typecheck`、`npm test -- --run`（20 个文件、70 个测试）、`npm run build` 全部通过。
- React 复核：没有新增数据瀑布、内联组件、派生状态 effect 或额外客户端依赖；空状态组件仍保持模块级定义。

final result: passed

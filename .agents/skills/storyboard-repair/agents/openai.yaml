interface:
  display_name: "Storyboard Repair"
  short_description: "Repair only failed storyboard fields and boundaries"
  default_prompt: "Use $storyboard-repair to produce the smallest schema-valid repair for the supplied failed issues and adjacent context."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Storyboard Review"
  short_description: "Return evidence-scoped director issues without editing shots"
  default_prompt: "Use $storyboard-review to independently review the supplied candidate storyboard and return schema-valid evidence-scoped issues."
policy:
  allow_implicit_invocation: false

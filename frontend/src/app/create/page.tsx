import { PagePlaceholder } from "@/components/page-placeholder";

export default function CreatePage() {
  return (
    <PagePlaceholder
      eyebrow="创作工作台"
      title="开始创作"
      description="登录、提示词编辑和图片生成将在对应业务切片接入。"
    />
  );
}

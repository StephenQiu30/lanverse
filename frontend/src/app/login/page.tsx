import { PagePlaceholder } from "@/components/page-placeholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      eyebrow="账户"
      title="邀请制登录"
      description="MVP 仅向受邀创作者开放，登录能力将在身份切片接入。"
    />
  );
}

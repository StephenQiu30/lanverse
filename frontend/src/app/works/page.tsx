import { PagePlaceholder } from "@/components/page-placeholder";

export default function WorksPage() {
  return (
    <PagePlaceholder
      eyebrow="私人空间"
      title="我的作品"
      description="生成作品默认私有，作品列表将在资产切片接入。"
    />
  );
}
